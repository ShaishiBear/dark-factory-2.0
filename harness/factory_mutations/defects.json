{
  "_what_this_is": [
    "Deliberately weaken copied factory trust-root code and require focused anti-bypass tests to go red.",
    "These mutations NEVER edit the live worktree: every defect runs in a minimal temporary repo-shaped copy.",
    "A mutation only counts when its exact anchor is unique and the previously-green focused suite fails after injection."
  ],
  "defects": [
    {
      "id": "test-author-probe-wrong-role-route",
      "file": "scripts/factory_test_author_probe.py",
      "find": "model_for_role(\"test_author\", policy)",
      "replace": "model_for_role(\"context\", policy)",
      "why": "Measuring a different default route cannot establish the test-author route's thinking behavior."
    },
    {
      "id": "test-author-probe-inherited-secrets",
      "file": "scripts/factory_test_author_probe.py",
      "find": "environment = {\"PATH\": source[\"PATH\"],",
      "replace": "environment = {**source, \"PATH\": source[\"PATH\"],",
      "why": "A tools-free diagnostic must not inherit repository credentials or retired login configuration."
    },
    {
      "id": "test-author-probe-repeated-attempt",
      "file": "scripts/factory_test_author_probe.py",
      "find": "source.get(\"GITHUB_RUN_ATTEMPT\") != \"1\"",
      "replace": "False",
      "why": "A rerun is not the authorized first attempt of the bounded measurement."
    },
    {
      "id": "security-verdict-forced-pass",
      "file": "scripts/factory_security.py",
      "find": "    verdict = \"pass\" if not findings else \"fail\"\n",
      "replace": "    verdict = \"pass\"\n",
      "why": "The deterministic security guard could make all findings advisory."
    },
    {
      "id": "security-github-protection-removed",
      "file": "scripts/factory_security.py",
      "find": "or path.startswith(\".github/\")",
      "replace": "or False",
      "why": "An autonomous PR could rewrite CI without the protected-path veto."
    },
    {
      "id": "security-kernel-protection-removed",
      "file": "scripts/factory_security.py",
      "find": "or path.startswith(\"factory_kernel/\")",
      "replace": "or False",
      "why": "An autonomous PR could rewrite the repo-owned dispatcher and merge authority boundary."
    },
    {
      "id": "security-backend-lock-coupling-removed",
      "file": "scripts/factory_security.py",
      "find": "if py_changes and BACKEND_LOCK not in changed_files:",
      "replace": "if False and py_changes and BACKEND_LOCK not in changed_files:",
      "why": "A backend manifest could change while its exact lock stayed stale."
    },
    {
      "id": "security-direct-python-source-allowed",
      "file": "scripts/factory_security.py",
      "find": "if change[\"ecosystem\"] == \"python\" and DISALLOWED_PY_SOURCE.search(spec):",
      "replace": "if False and change[\"ecosystem\"] == \"python\" and DISALLOWED_PY_SOURCE.search(spec):",
      "why": "A mutable or credential-bearing direct dependency URL could bypass provenance."
    },
    {
      "id": "security-secret-scan-disabled",
      "file": "scripts/factory_security.py",
      "find": "secrets = secret_findings(added_lines(diff))",
      "replace": "secrets = []",
      "why": "High-confidence credentials in added source would no longer veto merge."
    },
    {
      "id": "worker-shell-tool-enabled",
      "file": "factory_kernel/worker_policy.py",
      "find": "WRITE_TOOLS = (*READ_TOOLS, \"Write\", \"Edit\")",
      "replace": "WRITE_TOOLS = (*READ_TOOLS, \"Write\", \"Edit\", \"Bash\")",
      "why": "An issue-influenced model worker could regain arbitrary shell and Git authority."
    },
    {
      "id": "worker-env-secret-filter-disabled",
      "file": "factory_kernel/providers.py",
      "find": "if key in exact or key.startswith(prefixes)",
      "replace": "if True",
      "why": "GitHub and application validation secrets could leak into untrusted model subprocesses."
    },
    {
      "id": "worker-bare-isolation-disabled",
      "file": "factory_kernel/providers.py",
      "find": "ISOLATION_FLAGS: tuple[str, ...] = (\"--safe-mode\", \"--setting-sources\", \"\")",
      "replace": "ISOLATION_FLAGS: tuple[str, ...] = (\"--setting-sources\", \"\")",
      "why": "Project plugins, hooks, skills, commands or auto-memory could silently expand the headless worker authority surface. `--safe-mode` is what disables them since D-065 replaced `--bare`, which also forced the CLI into simple mode."
    },
    {
      "id": "worker-test-envelope-disabled",
      "file": "factory_kernel/git_authority.py",
      "find": "    if stray:\n",
      "replace": "    if False and stray:\n",
      "why": "The acceptance-test worker could smuggle product changes into its kernel-created RED commit: a stray write outside every checkpoint's files would be staged and committed as an acceptance test; caught by tests/factory/test_factory_guard_files_untouched.py (a stray write is refused and named) (D-064)."
    },
    {
      "id": "worker-design-envelope-disabled",
      "file": "factory_kernel/git_authority.py",
      "find": "    if outside:",
      "replace": "    if False and outside:",
      "why": "The implementation worker could commit files outside the compiled design envelope."
    },
    {
      "id": "architecture-layer-direction-disabled",
      "file": "scripts/factory_architecture_guard.py",
      "find": "if target_layer not in allowed[source_layer]:",
      "replace": "if False and target_layer not in allowed[source_layer]:",
      "why": "A new reverse/cross-layer dependency could be introduced without deterministic rejection."
    },
    {
      "id": "architecture-design-envelope-disabled",
      "file": "scripts/factory_architecture_guard.py",
      "find": "unplanned = sorted(set(product_changed) - planned_set)",
      "replace": "unplanned = []",
      "why": "The coder could change production files the compiled design never authorized."
    },
    {
      "id": "architecture-frontend-scope-narrowed",
      "file": "scripts/factory_architecture_guard.py",
      "find": "PRODUCT_PREFIXES = (\"app/backend/\", \"app/frontend/\")",
      "replace": "PRODUCT_PREFIXES = (\"app/backend/\", \"app/frontend/src/\")",
      "why": "Frontend manifests and build/config files could drift outside the design-authorized architecture envelope."
    },
    {
      "id": "architecture-unclassified-source-admitted",
      "file": "scripts/factory_architecture_guard.py",
      "find": "if unclassified_new:",
      "replace": "if False and unclassified_new:",
      "why": "An agent could create a new source namespace outside every declared architecture layer."
    },
    {
      "id": "architecture-cycle-detection-disabled",
      "file": "scripts/factory_architecture_guard.py",
      "find": "if len(component) > 1 or (len(component) == 1 and node in graph.get(node, set())):",
      "replace": "if False and (len(component) > 1 or (len(component) == 1 and node in graph.get(node, set()))):",
      "why": "New dependency cycles could accumulate while architecture validation still reported clean."
    },
    {
      "id": "architecture-no-growth-per-path-disabled",
      "file": "scripts/factory_architecture_guard.py",
      "find": "if touched and delta > 0:",
      "replace": "if False and touched and delta > 0:",
      "why": "Growth in one governed hotspot could be hidden by shrinkage in another path in the same debt group."
    },
    {
      "id": "architecture-no-growth-disabled",
      "file": "scripts/factory_architecture_guard.py",
      "find": "if graph_cfg[\"enforce_no_growth_debt\"] and growth:",
      "replace": "if False and graph_cfg[\"enforce_no_growth_debt\"] and growth:",
      "why": "Known architecture hotspots could grow on every ticket despite a no-growth policy."
    },
    {
      "id": "evidence-security-fail-accepted",
      "file": "scripts/factory_evidence.py",
      "find": "if value.get(\"verdict\") != \"pass\":\n        die(\"deterministic security guard did not authorize merge\")",
      "replace": "if False and value.get(\"verdict\") != \"pass\":\n        die(\"deterministic security guard did not authorize merge\")",
      "why": "Final merge authority could accept a deterministic security failure."
    },
    {
      "id": "evidence-contradictory-security-pass-accepted",
      "file": "scripts/factory_evidence.py",
      "find": "if collections[\"findings\"] or collections[\"protected_paths\"] or collections[\"secret_findings\"]:",
      "replace": "if False and (collections[\"findings\"] or collections[\"protected_paths\"] or collections[\"secret_findings\"]):",
      "why": "A forged pass could carry blocking security findings into evidence."
    },
    {
      "id": "evidence-stale-architecture-policy-accepted",
      "file": "scripts/factory_evidence.py",
      "find": "if arch.get(\"policy_sha256\") != digest(policy):",
      "replace": "if False and arch.get(\"policy_sha256\") != digest(policy):",
      "why": "Architecture evidence could be bound to a stale or substituted policy."
    },
    {
      "id": "evidence-architecture-guard-not-recomputed",
      "file": "scripts/factory_evidence.py",
      "find": "if recomputed_hash != builder[\"sha256\"]:",
      "replace": "if False and recomputed_hash != builder[\"sha256\"]:",
      "why": "A builder could forge deterministic architecture-guard provenance without independent final recomputation."
    },
    {
      "id": "evidence-architecture-holdout-nonpass-accepted",
      "file": "scripts/factory_evidence.py",
      "find": "if value.get(\"verdict\") != \"pass\" or value.get(\"convergence\") == \"regresses\":",
      "replace": "if False and (value.get(\"verdict\") != \"pass\" or value.get(\"convergence\") == \"regresses\"):",
      "why": "A blinded architecture rejection could be ignored by final merge authority."
    },
    {
      "id": "evidence-forged-test-plan-hash-accepted",
      "file": "scripts/factory_evidence.py",
      "find": "if digest(plan) != proof[\"test_plan_sha256\"]:",
      "replace": "if False and digest(plan) != proof[\"test_plan_sha256\"]:",
      "why": "Acceptance checkpoints could be rewritten without invalidating their proof-plan binding."
    },
    {
      "id": "merge-parent-check-disabled",
      "file": "harness/merge_verify.py",
      "find": "if len(merge_parents) != 1 or merge_parents[0] != base:",
      "replace": "if False and (len(merge_parents) != 1 or merge_parents[0] != base):",
      "why": "A squash created on a moved or wrong base could be blessed after merge."
    },
    {
      "id": "merge-tree-check-disabled",
      "file": "harness/merge_verify.py",
      "find": "if merge_tree != auth_tree:",
      "replace": "if False and merge_tree != auth_tree:",
      "why": "The server-created merge tree could differ from the tree that actually passed evidence."
    },
    {
      "id": "merge-main-reachability-check-disabled",
      "file": "harness/merge_verify.py",
      "find": "if not merge_is_ancestor:",
      "replace": "if False and not merge_is_ancestor:",
      "why": "A reported merge SHA not contained in current main history could be accepted."
    },
    {
      "id": "immunity-text-assertion-disabled",
      "file": "harness/immunity.py",
      "find": "if needle not in path.read_text(encoding=\"utf-8\"):",
      "replace": "if False and needle not in path.read_text(encoding=\"utf-8\"):",
      "why": "A learned structural lesson could disappear while the immunity checker still reported pass."
    },
    {
      "id": "immunity-numeric-floor-disabled",
      "file": "harness/immunity.py",
      "find": "if not isinstance(selected, (int, float)) or isinstance(selected, bool) or selected < minimum:",
      "replace": "if False and (not isinstance(selected, (int, float)) or isinstance(selected, bool) or selected < minimum):",
      "why": "A protected learned floor could regress without reopening the lesson."
    },
    {
      "id": "immunity-array-floor-disabled",
      "file": "harness/immunity.py",
      "find": "if matches < minimum:",
      "replace": "if False and matches < minimum:",
      "why": "Required independent detector probes could vanish while immunity still reported pass."
    },
    {
      "id": "worker-kernel-subprocess-secret-scrub-disabled",
      "file": "factory_kernel/runtime.py",
      "find": "        merged = scoped_environment(env, scope=credential_scope)",
      "replace": "        merged = dict(os.environ)\n        if env:\n            merged.update(env)",
      "why": "PR-controlled dependency and test subprocesses could inherit GitHub, model and validation credentials."
    },
    {
      "id": "worker-evidence-child-secret-scrub-disabled",
      "file": "scripts/factory_evidence.py",
      "find": "    child = {key: value for key, value in original.items() if not sensitive(key)}",
      "replace": "    child = dict(original)",
      "why": "Independent RED/GREEN replay could inherit the repository write token or validation credentials."
    },
    {
      "id": "worker-git-push-ambient-secret-env-restored",
      "file": "factory_kernel/github_cli.py",
      "find": "            env = scoped_environment(scope=\"none\")",
      "replace": "            env = dict(os.environ)",
      "why": "The trusted git push child could unnecessarily inherit application and model credentials."
    },
    {
      "id": "worker-scheduler-persists-checkout-token",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "          ref: main\n          fetch-depth: 0\n          persist-credentials: false",
      "replace": "          ref: main\n          fetch-depth: 0\n          persist-credentials: true",
      "why": "A write-capable GitHub token could be stored in Git config where PR-controlled subprocesses can read it."
    },
    {
      "id": "worker-scheduler-disables-merge-authority",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "        run: python -m factory_kernel dispatch --once",
      "replace": "        run: python -m factory_kernel dispatch --once --no-merge",
      "why": "The production scheduler could silently stop exercising the intended exact-head merge authority path."
    },
    {
      "id": "worker-scheduler-claude-version-floats",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      - name: Install pinned worker and browser CLIs\n        run: |\n          npm install -g @anthropic-ai/claude-code@2.1.245",
      "replace": "      - name: Install pinned worker and browser CLIs\n        run: |\n          npm install -g @anthropic-ai/claude-code",
      "why": "The reasoning-worker executable could drift between unattended runs without review or revalidation."
    },
    {
      "id": "worker-scheduler-main-protection-check-disabled",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "          test \"$main_protected\" = \"true\" || {",
      "replace": "          true || {",
      "why": "Scheduled autonomous operation could proceed on an unprotected main where direct pushes bypass PR and evidence authority."
    },
    {
      "id": "e2e-bootstrap-local-database-guard-disabled",
      "file": "harness/bootstrap_e2e.py",
      "find": "        and parsed.hostname in {\"127.0.0.1\", \"localhost\"}",
      "replace": "        and True",
      "why": "Disposable validation bootstrap could be pointed at a remote or production database and delete/replace real rows."
    },
    {
      "id": "e2e-server-backend-venv-bypassed",
      "file": "harness/serve.py",
      "find": "    python = BACKEND / \".venv\" / rel",
      "replace": "    python = Path(sys.executable)",
      "why": "The full gate could launch the backend with an unrelated interpreter instead of the exact locked backend environment."
    },
    {
      "id": "e2e-backend-migration-interpreter-bypassed",
      "file": "app/backend/main.py",
      "find": "            sys.executable,\n            \"-m\",\n            \"alembic\",",
      "replace": "            \"uv\",\n            \"run\",\n            \"alembic\",",
      "why": "Backend startup migrations could escape the exact locked interpreter and fail or drift through a second environment resolver."
    },
    {
      "id": "worker-scheduler-external-database-secret-restored",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "            echo \"DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/dark_factory_validation\"",
      "replace": "            echo \"DATABASE_URL=${{ secrets.DATABASE_URL }}\"",
      "why": "The GitHub worker could regain authority over an external database instead of a disposable loopback validation database."
    },
    {
      "id": "runtime-stop-not-rechecked-before-merge",
      "file": "factory_kernel/effect_broker.py",
      "find": "        # Fresh controls immediately before the remote call: a stop or fence observed now means\n        # no call, recorded as a failure that spent nothing remote.\n        try:\n            self.stop_check()\n        except Exception as exc:\n",
      "replace": "        # Fresh controls immediately before the remote call: a stop or fence observed now means\n        # no call, recorded as a failure that spent nothing remote.\n        try:\n            pass\n        except Exception as exc:\n",
      "why": "A stop pressed during programme admission would no longer prevent the shared inline/deferred irreversible merge: the broker's recheck immediately before the one remote call is the last observation of the controls, and the earlier inline check cannot protect this later interval. Detector: tests/factory/test_factory_programme.py, tests/factory/test_factory_merge_authorized.py"
    },
    {
      "id": "runtime-stop-rechecked-before-programme-admission",
      "file": "factory_kernel/runtime.py",
      "find": "            stop_check=self.check_stop, epoch=str(DEFAULT_POLICY[\"revocation_epoch\"]),\n",
      "replace": "            stop_check=lambda: None, epoch=str(DEFAULT_POLICY[\"revocation_epoch\"]),\n",
      "why": "A stop arriving during the final remote programme read (admission) would be missed if the last stop observation ran before it. The runtime must hand the broker its own fresh stop check; a no-op check leaves the pre-admission check as the last observation. Detector: tests/factory/test_factory_programme.py"
    },
    {
      "id": "runtime-merges-even-when-merging-is-disabled",
      "file": "factory_kernel/runtime.py",
      "find": "            if not merge:",
      "replace": "            if False and not merge:",
      "why": "A validation-only run would squash-merge the pull request anyway."
    },
    {
      "id": "runtime-rejecting-holdout-tolerated",
      "file": "factory_kernel/runtime.py",
      "find": "            if verdict.get(\"verdict\") != \"pass\":",
      "replace": "            if False and verdict.get(\"verdict\") != \"pass\":",
      "why": "A blinded holdout that rejected the change would not stop the merge."
    },
    {
      "id": "runtime-post-merge-failure-raises-no-incident",
      "file": "factory_kernel/runtime.py",
      "find": "            self._raise_post_merge_incident(pr_number, linked_issue, incident)\n            raise incident from exc\n",
      "replace": "            raise incident from exc\n",
      "why": "An unverified merge on main would be reported as an ordinary failure, telling humans no merge was authorized and leaving the hourly worker free to build on it."
    },
    {
      "id": "runtime-post-merge-incident-does-not-stop-the-factory",
      "file": "factory_kernel/runtime.py",
      "find": "                self.github.create_issue(",
      "replace": "                (lambda **kw: 0)(",
      "why": "No stop issue is opened, so the next hourly dispatch proceeds from an untrusted main."
    },
    {
      "id": "runtime-post-merge-verification-failure-swallowed",
      "file": "factory_kernel/runtime.py",
      "find": "        except Exception as exc:  # the merge is already on main; this is an incident\n            incident = PostMergeUnverified(\n",
      "replace": "        except Exception:  # the merge is already on main\n            pass\n        if False:\n            incident = PostMergeUnverified(\n",
      "why": "A failed post-merge verification would be indistinguishable from a verified merge."
    },
    {
      "id": "rehearsal-evidence-seam-skips-the-architecture-holdout-rule",
      "file": "harness/rehearsal.py",
      "find": "                rules.verify_architecture_holdout(value, list(CHANGED_FILES), policy)",
      "replace": "                pass",
      "why": "The rehearsal would stop demonstrating that a rejecting architecture holdout is refused, which is exactly the gap this seam was added to close."
    },
    {
      "id": "security-application-invariant-paths-unguarded",
      "file": "scripts/factory_security.py",
      "find": "or path in APPLICATION_SECURITY_PATHS",
      "replace": "or False",
      "why": "The MISSION ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Â ÃƒÂ¢Ã¢â€šÂ¬Ã¢â€žÂ¢ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¯ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¿ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¡ÃƒÆ’Ã†â€™ÃƒÂ¢Ã¢â€šÂ¬Ã…Â¡ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â½10 application security surface could stop being trust root, letting an autonomous run alter owner-only access, the message cap or the signup guard unrefused."
    },
    {
      "id": "security-auth-package-unguarded",
      "file": "scripts/factory_security.py",
      "find": "or path.startswith(\"app/backend/auth/\")",
      "replace": "or False",
      "why": "Token issuance, verification and password hashing could be edited freely while the routes that gate on them stayed protected."
    },
    {
      "id": "security-signup-guard-dropped-from-trust-root",
      "file": "scripts/factory_security.py",
      "find": "    \"app/backend/signup_rate_limit.py\",\n",
      "replace": "",
      "why": "The signup abuse guard has no behavioural holdout scenario, so removing its path entry would leave it with no detector at all."
    },
    {
      "id": "security-human-lane-granted-to-bots",
      "file": "scripts/factory_security.py",
      "find": "return actor.get(\"type\") == \"User\" and actor.get(\"association\") in HUMAN_ASSOCIATIONS",
      "replace": "return True",
      "why": "The trust-root maintenance lane would open to the factory's own Bot identity, letting an autonomous PR rewrite its judge."
    },
    {
      "id": "security-human-lane-requires-repository-role-removed",
      "file": "scripts/factory_security.py",
      "find": "HUMAN_ASSOCIATIONS = frozenset({\"OWNER\", \"MEMBER\", \"COLLABORATOR\"})",
      "replace": "HUMAN_ASSOCIATIONS = frozenset({\"OWNER\", \"MEMBER\", \"COLLABORATOR\", \"CONTRIBUTOR\", \"FIRST_TIME_CONTRIBUTOR\", \"NONE\", \"\"})",
      "why": "Any GitHub user account, not only one holding a role on this repository, could change the trust root through a PR."
    },
    {
      "id": "security-human-lane-commit-provenance-unchecked",
      "file": "scripts/factory_security.py",
      "find": "        findings.extend(commit_provenance_problems(commits))",
      "replace": "        pass",
      "why": "A factory commit pushed onto a human's branch would inherit the human lane; the second fence behind PR identity would be gone."
    },
    {
      "id": "security-trusted-base-runs-from-pr-head",
      "file": "scripts/factory_security.py",
      "find": "die(\"trusted-base mode refuses to run from the PR head: the change would be judging itself\")",
      "replace": "pass",
      "why": "Trusted-base mode would accept running from the PR head, so a PR could supply the guard that judges it."
    },
    {
      "id": "security-trusted-base-expected-head-unbound",
      "file": "scripts/factory_security.py",
      "find": "if expect_head and head != expect_head:",
      "replace": "if False:",
      "why": "A verdict scheduled for one head could be issued for a later head that was never judged."
    },
    {
      "id": "security-trusted-base-expected-base-unbound",
      "file": "scripts/factory_security.py",
      "find": "if expect_base and local != expect_base:",
      "replace": "if False:",
      "why": "The authority could run from a commit other than the base tip the workflow promised."
    },
    {
      "id": "security-trusted-base-fetched-head-unbound",
      "file": "scripts/factory_security.py",
      "find": "if fetched != head:",
      "replace": "if False:",
      "why": "The diff judged could come from a ref other than the head GitHub reports and will merge."
    },
    {
      "id": "security-trusted-base-off-main-accepted",
      "file": "scripts/factory_security.py",
      "find": "if run([\"git\", \"merge-base\", \"--is-ancestor\", local, f\"origin/{base_ref}\"], check=False).returncode != 0:",
      "replace": "if False:",
      "why": "A commit that never landed on the protected branch could act as the trusted base."
    },
    {
      "id": "security-unattended-merge-armed-for-autonomous-lane",
      "file": "scripts/factory_security.py",
      "find": "\"unattended_merge_eligible\": lane == \"human-maintenance\" and verdict == \"pass\",",
      "replace": "\"unattended_merge_eligible\": verdict == \"pass\",",
      "why": "A factory product PR would be armed for GitHub auto-merge on the quick gate alone, bypassing the evidence ladder."
    },
    {
      "id": "kernel-validator-guard-runs-from-pr-head",
      "file": "factory_kernel/runtime.py",
      "find": "                    \"--trusted-base\", \"--expect-head\", head,\n",
      "replace": "",
      "why": "The kernel validator would run the guard in head mode again, from code the PR supplied."
    },
    {
      "id": "kernel-validator-guard-cwd-is-pr-worktree",
      "file": "factory_kernel/runtime.py",
      "find": "                cwd=self.repo_root,\n                env=env,\n                credential_scope=\"github\",\n                timeout=180,\n                transcript=paths.transcripts / \"security.log\",",
      "replace": "                cwd=worktree.path,\n                env=env,\n                credential_scope=\"github\",\n                timeout=180,\n                transcript=paths.transcripts / \"security.log\",",
      "why": "The guard would execute from the PR head worktree; the trusted-base flag alone does not choose the program."
    },
    {
      "id": "worker-blind-paths-empty",
      "file": "factory_kernel/worker_policy.py",
      "find": "BUILDER_BLIND_PATHS: tuple[str, ...] = (\".factory/holdout/**/*.py\",)",
      "replace": "BUILDER_BLIND_PATHS: tuple[str, ...] = ()",
      "why": "Build worktrees would contain the holdout scenarios again; the builder could read the assertions it is judged by."
    },
    {
      "id": "kernel-build-worktree-sees-holdout",
      "file": "factory_kernel/runtime.py",
      "find": "            base_dir=self.config.runtime.work_root / \"worktrees\",\n            blind=BUILDER_BLIND_PATHS,\n",
      "replace": "            base_dir=self.config.runtime.work_root / \"worktrees\",\n",
      "why": "The build worktree would be created unblinded even though the policy names the paths."
    },
    {
      "id": "worktree-blind-ignored",
      "file": "factory_kernel/worktree.py",
      "find": "    if not patterns:\n        return\n    _run(worktree, [\"sparse-checkout\"",
      "replace": "    if True:\n        return\n    _run(worktree, [\"sparse-checkout\"",
      "why": "The primitive would accept a blind list and silently create a fully visible worktree."
    },
    {
      "id": "worktree-blind-verification-dropped",
      "file": "factory_kernel/worktree.py",
      "find": "    if still_present:\n        raise WorktreeError(f\"blinded paths are still on disk: {sorted(still_present)}\")",
      "replace": "    if False:\n        raise WorktreeError(f\"blinded paths are still on disk: {sorted(still_present)}\")",
      "why": "A sparse checkout that did not take on some git version would hand a visible holdout to the worker without refusing."
    },
    {
      "id": "lifecycle-shadow-command-returns",
      "file": "factory_kernel/cli.py",
      "find": "    sub.add_parser(\"config-check\")\n",
      "replace": "    sub.add_parser(\"state-next\")\n    sub.add_parser(\"config-check\")\n",
      "why": "A lifecycle command the runtime does not consume would return to the control surface, reopening a second representation of the lifecycle."
    },
    {
      "id": "kernel-commit-identity-unattributed",
      "file": "factory_kernel/worker_policy.py",
      "find": "KERNEL_COMMIT_EMAIL = \"41898282+github-actions[bot]@users.noreply.github.com\"",
      "replace": "KERNEL_COMMIT_EMAIL = \"dark-factory@users.noreply.github.com\"",
      "why": "Kernel commits would again resolve to no GitHub account: attributable to nobody and outside what the second fence expects of a factory commit."
    },
    {
      "id": "kernel-config-dead-key-returns",
      "file": "factory_kernel/config.py",
      "find": "    quick_command: tuple[str, ...]\n",
      "replace": "    quick_command: tuple[str, ...]\n    full_command: tuple[str, ...] = ()\n",
      "why": "A validation setting nothing reads would return to the schema: a configuration claim with no enforcer."
    },
    {
      "id": "triage-body-window-shrunk",
      "file": "factory_kernel/triage.py",
      "find": "TRIAGE_BODY_CHARS = 12000",
      "replace": "TRIAGE_BODY_CHARS = 2000",
      "why": "Triage would judge long, well-specified issues on a 2000-character prefix and reject them as underspecified."
    },
    {
      "id": "contract-dependencies-unvalidated",
      "file": "scripts/factory_protocol.py",
      "find": "    validate_dependencies(c.get(\"dependencies\", []))\n",
      "replace": "",
      "why": "A contract could declare a dependency with no justification and the kernel would render an empty section the guard accepts by name alone."
    },
    {
      "id": "pr-body-dependency-heading-renamed",
      "file": "factory_kernel/pr_body.py",
      "find": "DEPENDENCY_HEADING = \"## Dependency justification\"",
      "replace": "DEPENDENCY_HEADING = \"## Dependencies\"",
      "why": "The kernel would render a heading the security guard does not parse; every autonomous dependency change would fail at merge again."
    },
    {
      "id": "pr-body-dependency-names-omitted",
      "file": "factory_kernel/pr_body.py",
      "find": "lines.append(f\"- `{name}` ({dep.get('ecosystem')}): {str(dep.get('purpose') or '').strip()}\")",
      "replace": "lines.append(f\"- ({dep.get('ecosystem')}): {str(dep.get('purpose') or '').strip()}\")",
      "why": "The guard requires each package to be named under the heading; an unnamed justification is a missing justification."
    },
    {
      "id": "kernel-pr-body-ignores-contract",
      "file": "factory_kernel/runtime.py",
      "find": "                issue_number, attempt, self._read_json(paths.artifacts / \"task-contract.json\")\n",
      "replace": "                issue_number, attempt, {}\n",
      "why": "The PR body would stop carrying the contract's declared dependencies and the guard would refuse the manifest change."
    },
    {
      "id": "lockfile-refresh-skipped",
      "file": "factory_kernel/git_authority.py",
      "find": "    refresh_lockfiles(cwd, [str(path) for path in planned_raw], runner=lock_runner)\n",
      "replace": "",
      "why": "A manifest change would be committed without its lockfile; the guard refuses manifest-without-lock, so every dependency change fails."
    },
    {
      "id": "lockfile-not-planned-accepted",
      "file": "factory_kernel/git_authority.py",
      "find": "        if lock not in planned_set:\n",
      "replace": "        if False:\n",
      "why": "The kernel would write a lockfile the compiled design never authorized, outside the implementation envelope."
    },
    {
      "id": "triage-label-outside-vocabulary",
      "file": "factory_kernel/triage.py",
      "find": "                type_label(str(decision[\"classification\"])),\n",
      "replace": "                \"kind:\" + str(decision[\"classification\"]),\n",
      "why": "Triage would apply a label the preflight never required to exist, so an accepted issue would crash the run half-labelled."
    },
    {
      "id": "kernel-red-proof-holds-github-credentials",
      "file": "factory_kernel/runtime.py",
      "find": "                        credential_scope=\"none\",\n                        timeout=600,\n                        transcript=paths.transcripts / log,\n",
      "replace": "                        credential_scope=\"github\",\n                        timeout=600,\n                        transcript=paths.transcripts / log,\n",
      "why": "The RED proof runs model-authored checkpoint commands; giving it GitHub scope hands a repository token to code the model wrote."
    },
    {
      "id": "kernel-lease-heartbeat-without-credentials",
      "file": "factory_kernel/runtime.py",
      "find": "            credential_scope=\"github\",\n            timeout=120,\n            transcript=paths.transcripts / f\"lease-{stage}.log\",",
      "replace": "            credential_scope=\"none\",\n            timeout=120,\n            transcript=paths.transcripts / f\"lease-{stage}.log\",",
      "why": "The kernel's own heartbeat would fail the same way the script-owned one did, and every build would die at the contract stage."
    },
    {
      "id": "proof-checkpoint-env-unscrubbed",
      "file": "scripts/factory_proof.py",
      "find": "        p=subprocess.run(argv,cwd=ROOT/cwd,env=checkpoint_env(),capture_output=True,text=True,encoding='utf-8',errors='replace',timeout=CHECKPOINT_TIMEOUT_SECONDS)",
      "replace": "        p=subprocess.run(argv,cwd=ROOT/cwd,capture_output=True,text=True,encoding='utf-8',errors='replace',timeout=CHECKPOINT_TIMEOUT_SECONDS)",
      "why": "The defence-in-depth scrub is gone; a caller that forgets the credential scope would leak GH_TOKEN into a model-authored command."
    },
    {
      "id": "kernel-design-context-heartbeat-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "                self._lease_heartbeat(\n                    \"touch\", issue_number, \"design-context\", paths, cwd=worktree.path\n                )\n",
      "replace": "",
      "why": "A stage would run with no heartbeat, so a long context/design phase could be reaped as stale mid-build."
    },
    {
      "id": "methods-injection-skipped",
      "file": "factory_kernel/worker_runtime.py",
      "find": "            methods=method_block(cwd, role),\n",
      "replace": "",
      "why": "Workers would run on the bare role prompt again while the documentation says they receive the pinned methods."
    },
    {
      "id": "methods-manifest-unknown-role-accepted",
      "file": "factory_kernel/methods.py",
      "find": "            if role not in ROLE_TOOLS:\n                raise ValueError(f\"methods manifest names an unknown role {role!r} for {method_id}\")",
      "replace": "            if False:\n                raise ValueError(f\"methods manifest names an unknown role {role!r} for {method_id}\")",
      "why": "A typo in a role name would silently drop a method from every worker instead of refusing the run."
    },
    {
      "id": "methods-empty-text-accepted",
      "file": "factory_kernel/methods.py",
      "find": "        if not path.read_text(encoding=\"utf-8\").strip():\n            raise ValueError(f\"method text is empty: {file_name}\")",
      "replace": "        if False:\n            raise ValueError(f\"method text is empty: {file_name}\")",
      "why": "An emptied method file would be injected as nothing and the worker would lose the discipline without any signal."
    },
    {
      "id": "security-methods-directory-unprotected",
      "file": "scripts/factory_security.py",
      "find": "        or path.startswith(\".factory/methods/\")\n",
      "replace": "",
      "why": "An autonomous PR could rewrite the method text its own workers are judged against."
    },
    {
      "id": "review-aggregator-ignores-standards",
      "file": "factory_kernel/review.py",
      "find": "    verdict = \"fail\" if any(validated[axis][\"verdict\"] == \"fail\" for axis in AXES) else \"pass\"",
      "replace": "    verdict = validated[\"spec\"][\"verdict\"]",
      "why": "A failing Standards review would no longer block; the two-axis review would collapse back to one."
    },
    {
      "id": "review-aggregator-accepts-missing-axis",
      "file": "factory_kernel/review.py",
      "find": "    if missing:\n        raise ReviewInvalid(f\"review axes missing: {missing}\")",
      "replace": "    if False:\n        raise ReviewInvalid(f\"review axes missing: {missing}\")",
      "why": "An axis that never ran would be treated as absent rather than as a refusal."
    },
    {
      "id": "review-verdict-findings-disagreement-tolerated",
      "file": "factory_kernel/review.py",
      "find": "    if blocking and verdict != \"fail\":\n        raise ReviewInvalid(f\"{axis} review passes despite a blocking finding\")",
      "replace": "    if False:\n        raise ReviewInvalid(f\"{axis} review passes despite a blocking finding\")",
      "why": "A reviewer could report a critical finding and still say pass, and the kernel would believe the verdict."
    },
    {
      "id": "kernel-second-review-runs-one-axis",
      "file": "factory_kernel/runtime.py",
      "find": "        for axis in AXES:\n            self._agent(ROLE_FOR_AXIS[axis], worktree.path, paths, context=full_context, env=env)",
      "replace": "        for axis in AXES[:1]:\n            self._agent(ROLE_FOR_AXIS[axis], worktree.path, paths, context=full_context, env=env)",
      "why": "Only the Spec reviewer would run; the Standards artifact would be stale or absent."
    },
    {
      "id": "repro-passing-command-accepted",
      "file": "factory_kernel/repro.py",
      "find": "    if proc.returncode == 0:\n        raise ReproRefused(\"repro passed; a bug that does not go red cannot be contracted\")",
      "replace": "    if False:\n        raise ReproRefused(\"repro passed; a bug that does not go red cannot be contracted\")",
      "why": "A bug that never went red would be contracted on a hypothesis."
    },
    {
      "id": "repro-symptom-check-dropped",
      "file": "factory_kernel/repro.py",
      "find": "    if repro.expect_failure_containing not in output:",
      "replace": "    if False:",
      "why": "Any failing command, for any reason, would count as reproducing the reported bug."
    },
    {
      "id": "kernel-repro-not-executed-for-bugs",
      "file": "factory_kernel/runtime.py",
      "find": "                if role == \"investigate\":\n                    # The red loop",
      "replace": "                if False:\n                    # The red loop",
      "why": "Bug builds would proceed to the contract without the red loop the rules describe."
    },
    {
      "id": "ticket-blocker-state-ignored",
      "file": "scripts/factory_artifacts.py",
      "find": "        and all(x[\"state\"] == \"CLOSED\" for x in blockers)\n",
      "replace": "        and True\n",
      "why": "An issue whose named blockers are still open would be compiled onto the ready frontier."
    },
    {
      "id": "ticket-accepted-label-not-required",
      "file": "scripts/factory_artifacts.py",
      "find": "        and \"factory:accepted\" in labels\n        and all(",
      "replace": "        and True\n        and all(",
      "why": "An issue triage never accepted would be compiled as ready."
    },
    {
      "id": "kernel-issue-frontier-snapshot-not-written",
      "file": "factory_kernel/runtime.py",
      "find": "            self._write_json(\n                paths.artifacts / ISSUE_FRONTIER_ARTIFACT, self._issue_frontier(issue)\n            )\n",
      "replace": "",
      "why": "The ticket compiler would find no snapshot; the build could only proceed if it went back to calling GitHub itself, from a credential-free process."
    },
    {
      "id": "ticket-snapshot-blockers-unchecked-against-body",
      "file": "scripts/factory_artifacts.py",
      "find": "    if sorted(b[\"issue\"] for b in blockers) != named:\n",
      "replace": "    if False:\n",
      "why": "A snapshot omitting a named blocker would be accepted, so an open blocker could be hidden from the frontier decision."
    },
    {
      "id": "repro-env-denylist-inherits-openrouter-secret",
      "file": "factory_kernel/repro.py",
      "find": "    child = {key: base[key] for key in REPRO_ENV_KEYS if key in base and base[key]}",
      "replace": "    child = {key: value for key, value in base.items() if key not in (\"GH_TOKEN\", \"GITHUB_TOKEN\")}",
      "why": "The child environment would be built by denylist again; OPENROUTER_API_KEY, SUPADATA_API_KEY and every provider credential the worker holds would reach a model-authored command."
    },
    {
      "id": "repro-env-forwards-validation-credentials",
      "file": "factory_kernel/repro.py",
      "find": "    child.update(REPRO_ENV_SYNTHETIC)\n    return child",
      "replace": "    child.update(REPRO_ENV_SYNTHETIC)\n    child.update({k: base[k] for k in (\"SUPADATA_API_KEY\", \"OPENROUTER_API_KEY\", \"DATABASE_URL\") if k in base})\n    return child",
      "why": "A validation credential would be re-added to the allowlisted child environment under the guise of a synthetic variable."
    },
    {
      "id": "repro-python-c-accepted",
      "file": "factory_kernel/repro.py",
      "find": "    shape = matched_shape(tuple(argv))\n    if shape is None:",
      "replace": "    shape = matched_shape(tuple(argv)) or (argv[0],)\n    if False:",
      "why": "Any program with any arguments, including python -c, would be executed as a repro."
    },
    {
      "id": "repro-npx-accepted",
      "file": "factory_kernel/repro.py",
      "find": "    (\"bun\", \"x\", \"vitest\", \"run\"),\n)",
      "replace": "    (\"bun\", \"x\", \"vitest\", \"run\"),\n    (\"npx\",),\n    (\"python\",),\n)",
      "why": "npx and bare python would return to the shape list, turning the allowlist back into arbitrary process capability."
    },
    {
      "id": "repro-eval-flags-unchecked",
      "file": "factory_kernel/repro.py",
      "find": "    if arg in EVAL_FLAGS or arg.split(\"=\", 1)[0] in EVAL_FLAGS:",
      "replace": "    if False:",
      "why": "python -m pytest -c <file> or bun run test -e <code> would evaluate model-supplied text."
    },
    {
      "id": "repro-may-dirty-worktree",
      "file": "factory_kernel/runtime.py",
      "find": "        if before != after:\n            raise NeedsHuman(\"bug repro refused: repro modified the worktree\")",
      "replace": "        if False:\n            raise NeedsHuman(\"bug repro refused: repro modified the worktree\")",
      "why": "A repro that edits a source file and then prints the symptom would contaminate what the contract worker reads next."
    },
    {
      "id": "worker-turn-cap-dropped-for-role",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \"conformance\": 30,\n",
      "replace": "",
      "why": "A role without a turn cap is an unbounded loop again; only the 20-minute timeout would end it."
    },
    {
      "id": "provider-max-turns-not-passed",
      "file": "factory_kernel/providers.py",
      "find": "            argv.extend([\"--max-turns\", str(request.max_turns)])",
      "replace": "            pass",
      "why": "The cap would be computed and never reach the CLI; every worker would run unbounded while the policy claimed otherwise."
    },
    {
      "id": "provider-error-envelope-accepted",
      "file": "factory_kernel/providers.py",
      "find": "    if raw.get(\"is_error\") is True or subtype.startswith(\"error\"):",
      "replace": "    if False:",
      "why": "A worker that hit its cap, ran out of budget or died on an API error would be treated as a successful stage with empty output."
    },
    {
      "id": "context-worker-brief-reduced-to-hash",
      "file": "factory_kernel/runtime.py",
      "find": "                    context=self._worker_brief(\n                        paths, contract_hash=contract_hash, issue_context=issue_context\n                    ) + planning_advice(admission),\n",
      "replace": "                    context=f\"Validated contract sha256: {contract_hash}\" + planning_advice(admission),\n",
      "why": "The context worker would again receive only the hash and have to rediscover the whole task from disk, the slowest stage of the first canary."
    },
    {
      "id": "security-ratchet-regression-unchecked",
      "file": "scripts/factory_security.py",
      "find": "    if FLOOR_FILE in changed_files:\n        findings.extend(ratchet_regressions(base_floor, head_floor))\n",
      "replace": "",
      "why": "A maintainer PR could lower a ratchet floor and pass every check; the monotonicity the floor file documents would again be unenforced."
    },
    {
      "id": "repro-deferred-accepted-unvalidated",
      "file": "factory_kernel/runtime.py",
      "find": "                record = deferred_record(load_deferred(artifacts / DEFERRED_ARTIFACT))",
      "replace": "                record = dict(json.loads((artifacts / DEFERRED_ARTIFACT).read_text(encoding=\"utf-8\")), mode=\"deferred\")",
      "why": "A deferred record would reach the contract worker without validation, so an empty symptom or an arbitrary seam could enter the build."
    },
    {
      "id": "repro-deferred-symptom-not-verified-in-red",
      "file": "factory_kernel/runtime.py",
      "find": "            self._close_deferred_repro(paths.artifacts)\n",
      "replace": "",
      "why": "A deferred red loop would never be closed: the promised symptom could be absent from every RED checkpoint and the build would continue."
    },
    {
      "id": "repro-both-records-accepted",
      "file": "factory_kernel/runtime.py",
      "find": "        if executed and deferred:\n            raise NeedsHuman(\"bug repro refused: investigate wrote both repro.json and repro-deferred.json\")",
      "replace": "        if False:\n            raise NeedsHuman(\"bug repro refused: investigate wrote both repro.json and repro-deferred.json\")",
      "why": "A worker could write a deferred record as a fallback beside an executed repro, so the weaker record would be taken when the command did not fail."
    },
    {
      "id": "repro-deferred-symptom-match-forced",
      "file": "factory_kernel/repro.py",
      "find": "        if isinstance(tail, str) and needle in tail.lower():",
      "replace": "        if isinstance(tail, str):",
      "why": "Any RED output would close a deferred repro, so the promised symptom would never actually have to appear."
    },
    {
      "id": "provider-max-budget-not-passed",
      "file": "factory_kernel/providers.py",
      "find": "        if request.max_budget_usd is not None:\n            argv.extend([\"--max-budget-usd\", f\"{request.max_budget_usd:g}\"])\n",
      "replace": "",
      "why": "The dollar backstop would be configured and never reach the CLI; cost would be bounded only by the turn cap, which does not bound the resent conversation."
    },
    {
      "id": "provider-timeout-swallowed",
      "file": "factory_kernel/providers.py",
      "find": "        if run.timed_out:\n",
      "replace": "        if False:\n",
      "why": "A timed-out worker would be reported as a successful empty result instead of a failed stage."
    },
    {
      "id": "worker-cap-exceeds-timeout",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \"implement\": 30,\n",
      "replace": "    \"implement\": 120,\n",
      "why": "A cap the subprocess timeout would cut off first is fiction; the stage would die without telemetry instead of stopping cleanly at its cap."
    },
    {
      "id": "prompt-placeholders-not-rendered",
      "file": "factory_kernel/prompt_render.py",
      "find": "    values = {\"ARTIFACTS_DIR\": str(artifacts_dir(env))}\n",
      "replace": "    return prompt\n    values = {\"ARTIFACTS_DIR\": str(artifacts_dir(env))}\n",
      "why": "Workers would again receive `$ARTIFACTS_DIR` literally and, having no shell, write into a directory of that name (canary run 33910993905)."
    },
    {
      "id": "prompt-unknown-placeholder-forwarded",
      "file": "factory_kernel/prompt_render.py",
      "find": "    if unknown:\n        raise PromptRenderError(",
      "replace": "    if False:\n        raise PromptRenderError(",
      "why": "A placeholder the kernel cannot render would reach the worker unexpanded instead of refusing the launch."
    },
    {
      "id": "prompt-literal-artifacts-dir-unnamed",
      "file": "factory_kernel/worker_runtime.py",
      "find": "        self._refuse_literal_artifacts_dir(cwd)\n        self._assert_clean(cwd)\n",
      "replace": "        self._assert_clean(cwd)\n",
      "why": "A worker writing to a literal `$ARTIFACTS_DIR` would fail as an anonymous dirty tree instead of naming the class."
    },
    {
      "id": "refusal-classifier-always-unknown",
      "file": "factory_kernel/refusal.py",
      "find": "    if is_stale_base(text):\n        return \"stale_base\"\n",
      "replace": "    if is_stale_base(text):\n        return \"unknown\"\n",
      "why": "Every refusal would again reach GitHub as an unclassified failure; nothing downstream could tell a moved base from a security veto."
    },
    {
      "id": "refusal-scrub-removed",
      "file": "factory_kernel/refusal.py",
      "find": "        detail=scrub(tail)[-2000:],\n",
      "replace": "        detail=tail[-2000:],\n",
      "why": "A tool tail holding a token would be written into the durable refusal record and uploaded."
    },
    {
      "id": "refusal-stale-base-consumes-attempt",
      "file": "factory_kernel/runtime.py",
      "find": "            if linked_issue is not None and reason_code != \"stale_base\":\n",
      "replace": "            if linked_issue is not None:\n",
      "why": "A base that moved under the PR would charge the issue's rebuild budget, exhausting it on nothing the build did."
    },
    {
      "id": "rehead-skips-green",
      "file": "factory_kernel/runtime.py",
      "find": "            self._rehead_green(paths, worktree.path, env, output=\"green-proof.json\", log=self.REHEAD_GREEN_LOG)\n",
      "replace": "",
      "why": "The re-headed tip would go to conformance and the final proof without the GREEN replay that a new head invalidates."
    },
    {
      "id": "rehead-worktree-not-blinded",
      "file": "factory_kernel/runtime.py",
      "find": "            base_dir=self.config.runtime.work_root / \"rehead-worktrees\",\n            blind=BUILDER_BLIND_PATHS,\n",
      "replace": "            base_dir=self.config.runtime.work_root / \"rehead-worktrees\",\n",
      "why": "The conformance worker would run in a worktree that contains the holdout scenarios."
    },
    {
      "id": "rehead-accepts-any-reason",
      "file": "factory_kernel/refusal.py",
      "find": "    if refusal is None or refusal.get(\"reason_code\") != \"stale_base\":\n        return False\n",
      "replace": "    if refusal is None:\n        return False\n",
      "why": "A PR refused by a certifier or the holdout would be re-headed and handed back to validation as if the base had merely moved."
    },
    {
      "id": "rehead-cap-removed",
      "file": "factory_kernel/refusal.py",
      "find": "    return str(reheads[-1].get(\"new_head\") or \"\") == head\n",
      "replace": "    return True\n",
      "why": "A pull request that changed after its re-head and failed again would be re-headed forever. The cap is no longer one per PR (D-077); what it refuses now is a second re-head of a head the last one did not produce."
    },
    {
      "id": "rehead-force-push-without-lease",
      "file": "factory_kernel/runtime.py",
      "find": "            self.github.push_branch(branch, force_with_lease=head)\n",
      "replace": "            self.github.push_branch(branch)\n",
      "why": "The rewritten branch would be pushed without naming the judged head, so the remote could not refuse overwriting a head nobody judged."
    },
    {
      "id": "contract-keyed-behaviors-refused",
      "file": "scripts/factory_protocol.py",
      "find": "    behaviors = c[\"behaviors\"] = normalise_behaviors(c[\"behaviors\"])\n",
      "replace": "    behaviors = c[\"behaviors\"]\n",
      "why": "A complete contract written with behaviors keyed by AC id would be refused again as having no behaviors, as on canary run 33912650468."
    },
    {
      "id": "worker-context-rendered-as-prompt",
      "file": "factory_kernel/worker_runtime.py",
      "find": "        if context.strip():\n            prompt = prompt.rstrip(\"\\n\") + \"\\n\\n\" + context.strip() + \"\\n\"\n",
      "replace": "        if context.strip():\n            prompt = render_prompt(prompt.rstrip(\"\\n\") + \"\\n\\n\" + context.strip() + \"\\n\", env)\n",
      "why": "Untrusted context (an issue body naming $PATH) would be rendered and refuse every stage of the run before a model is invoked."
    },
    {
      "id": "review-diff-not-supplied",
      "file": "factory_kernel/runtime.py",
      "find": "        diff_context = self._diff_context(worktree.path, env)\n        full_context = (context.strip() + \"\\n\\n\" + diff_context) if context.strip() else diff_context\n",
      "replace": "        full_context = context\n",
      "why": "Reviewers have no shell and cannot compute a diff; without the kernel supplying it they burn their cap reading the checkout blind."
    },
    {
      "id": "governor-brief-omits-applicable-policy-ids",
      "file": "factory_kernel/runtime.py",
      "find": "                    include_design=True, include_applicable_policy=True,\n",
      "replace": "                    include_design=True,\n",
      "why": "The governor would guess the policy ID sets the compiler recomputes exactly, and be refused on any semantic disagreement."
    },
    {
      "id": "stage-note-existence-unchecked",
      "file": "factory_kernel/runtime.py",
      "find": "            self._require_stage_note(paths.artifacts, role)\n",
      "replace": "",
      "why": "A plan/investigate worker that wrote nothing would pass silently and the contract would be drawn from the issue alone."
    },
    {
      "id": "red-gate-refuses-spec-files-the-commit-authority-accepts",
      "file": "scripts/factory_shapes.py",
      "find": "TEST_MARKERS: tuple[str, ...] = (\"/tests/\", \"/__tests__/\", \".test.\", \".spec.\")",
      "replace": "TEST_MARKERS: tuple[str, ...] = (\"/tests/\", \".test.\")",
      "why": "A .spec. or __tests__ acceptance file committed by git_authority would be refused by RED, leaving a stray commit on the branch. Since D-064 one predicate (scripts/factory_shapes.test_shaped) serves RED, the commit authority and the architecture guard, so the disagreement is now a mutation of that shared list."
    },
    {
      "id": "context-object-entries-refused-again",
      "file": "scripts/factory_impact.py",
      "find": "    raw = normalise_lists(load(args.input), CONTEXT_LISTS, \"context\", die)\n",
      "replace": "    raw = load(args.input)\n",
      "why": "A context whose file entries are objects would again be refused as 'no seed files' although its content is correct (canary attempt 8)."
    },
    {
      "id": "design-object-entries-refused-again",
      "file": "scripts/factory_artifacts.py",
      "find": "    raw = normalise_lists(load(args.input), DESIGN_LISTS, \"design\", die)\n",
      "replace": "    raw = load(args.input)\n",
      "why": "A design whose module/seam entries are objects would be refused although its content is correct."
    },
    {
      "id": "governor-id-objects-refused-again",
      "file": "scripts/factory_architecture.py",
      "find": "    raw = normalise_lists(raw, GOVERNOR_LISTS, \"governor\", die)\n",
      "replace": "    pass\n",
      "why": "A governor that spells policy ids as {id: ...} objects would be refused although its judgement is correct (canary attempt 7)."
    },
    {
      "id": "shape-duplicate-after-normalisation-accepted",
      "file": "scripts/factory_shapes.py",
      "find": "    if len(out) != len(set(out)):\n        die(f\"{where} contains duplicate entries after normalisation\")\n",
      "replace": "",
      "why": "Two objects naming the same file would pass as distinct entries, weakening the duplicate check the string form always had."
    },
    {
      "id": "shape-inapplicable-flag-ignored",
      "file": "scripts/factory_shapes.py",
      "find": "        if entry.get(APPLICABLE_FIELD) is False:\n            return None\n",
      "replace": "",
      "why": "A governor that lists every policy with applicable:false would be refused again for the ids it explicitly excluded (canary attempt 7)."
    },
    {
      "id": "test-author-not-told-deferred-symptom",
      "file": "factory_kernel/runtime.py",
      "find": "            ) + self._deferred_symptom_brief(paths.artifacts)\n",
      "replace": "            )\n",
      "why": "The RED gate would demand a symptom string the test author was never shown, refusing every deferred-repro bug after a full RED run."
    },
    {
      "id": "conformance-brief-omits-policy-ids",
      "file": "factory_kernel/runtime.py",
      "find": "            + \"\\n\\nAPPLICABLE ARCHITECTURE POLICY IDS FOR THE CHANGED FILES (computed by the \"",
      "replace": "            + \"\\n\\n(policy ids withheld) (computed by the \"",
      "why": "The conformance worker would guess the ID sets from the wrong basis again, as run 33914596611's governor did, and the compiler would refuse by name."
    },
    {
      "id": "architecture-guard-test-predicate-narrowed",
      "file": "scripts/factory_architecture_guard.py",
      "find": "    return path.startswith(PRODUCT_PREFIXES) and not test_shaped(path)\n",
      "replace": "    return path.startswith(PRODUCT_PREFIXES) and not any(m in ('/' + path) for m in TEST_MARKERS)\n",
      "why": "A test_*.py the commit envelope and RED accept would again be an unplanned production file to the GREEN architecture guard, two gates after a stray commit."
    },
    {
      "id": "architecture-holdout-ids-withheld",
      "file": "factory_kernel/runtime.py",
      "find": "        context[\"applicable_policy_ids\"] = self._applicable_policy_ids(paths, files=changed_files)\n",
      "replace": "",
      "why": "The architecture holdout would have to derive the exact ID sets the evidence verifier checks from a prose rule."
    },
    {
      "id": "certifier-skeleton-removed",
      "file": "factory_kernel/runtime.py",
      "find": "            \"Return ONLY this JSON object, with `certifies` exactly as shown: \"\n            + skeleton\n",
      "replace": "            \"Return ONLY JSON with version 1.0; certifies the claim; verdict pass|fail. \"\n",
      "why": "The load-bearing `certifies` field would again be stated once in prose with no skeleton; a certifier omitting it fails every validation."
    },
    {
      "id": "provider-terminal-errors-retried",
      "file": "factory_kernel/providers.py",
      "find": "    if subtype.startswith(\"error\"):\n        return False\n    lowered = detail.lower()\n    return any(pattern.lower() in lowered for pattern in TRANSIENT_ERROR_PATTERNS)",
      "replace": "    return True",
      "why": "Every error envelope, including a turn cap, a budget stop and a missing model, would be re-launched as if the network had dropped; a worker's verdict would be retried up to the cap and paid for each time."
    },
    {
      "id": "provider-retry-without-worktree-restore",
      "file": "factory_kernel/providers.py",
      "find": "                if before_retry is not None:\n                    before_retry(attempt)\n",
      "replace": "",
      "why": "A mutation role retried after a dropped stream would start from a half-written checkout; the kernel's commit envelope would judge the union of two attempts."
    },
    {
      "id": "provider-transient-retries-unbounded",
      "file": "factory_kernel/providers.py",
      "find": "                if attempt < attempts_allowed:\n                    continue\n",
      "replace": "                if True:\n                    continue\n",
      "why": "A provider that keeps dropping streams would be re-launched forever; the stage would never fail closed and the dollar backstop would be paid per attempt without end."
    },
    {
      "id": "provenance-script-imports-kernel-without-bootstrap",
      "file": "scripts/factory_provenance.py",
      "find": "HERE = Path(__file__).resolve().parent\nsys.path.insert(0, str(HERE.parent))\n",
      "replace": "HERE = Path(__file__).resolve().parent\n",
      "why": "The publish step runs from a detached PR-head worktree; without the bootstrap the kernel import fails there and a completed build never reaches review, as on canary attempt 10."
    },
    {
      "id": "resume-green-commit-binding-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "        if str(proof.get(\"green_commit\") or \"\") != head:",
      "replace": "        if False:",
      "why": "A resume would accept artifacts from a different build and bind another build's proof to this PR head."
    },
    {
      "id": "resume-red-hash-check-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "            self._verify_red_unchanged(\n                {\"artifacts\": {\"red-proof\": {\"content\": self._read_json(paths.artifacts / \"red-proof.json\")}}},\n                worktree.path,\n            )\n",
      "replace": "",
      "why": "A resume would publish provenance for a head whose acceptance tests no longer match the RED proof."
    },
    {
      "id": "resume-cap-removed",
      "file": "factory_kernel/runtime.py",
      "find": "        if resume_count(self.github.pr_comments(pr_number)) > 0:",
      "replace": "        if False:",
      "why": "A PR could be resumed indefinitely instead of escalating after one attempt."
    },
    {
      "id": "resume-accepts-human-authored-pr",
      "file": "factory_kernel/runtime.py",
      "find": "        if author.get(\"type\") != self.FACTORY_PR_AUTHOR[\"type\"] or author.get(\"login\") != factory_login:",
      "replace": "        if False:",
      "why": "A human-authored PR could be resumed as if the factory had built it; resume must only finish the factory's own PRs."
    },
    {
      "id": "resume-author-type-ignored",
      "file": "factory_kernel/runtime.py",
      "find": "        if author.get(\"type\") != self.FACTORY_PR_AUTHOR[\"type\"] or author.get(\"login\") != factory_login:",
      "replace": "        if author.get(\"login\") != factory_login:",
      "why": "Identity would be a login string alone; a User account named like the bot would pass as the factory."
    },
    {
      "id": "kernel-authority-run-from-subject-copy",
      "file": "factory_kernel/runtime.py",
      "find": "        argv = resolve_trusted_program(self._kernel_checkout, argv)\n",
      "replace": "        argv = list(argv)\n",
      "why": "The kernel would again run a trust-root program by relative path from the PR-head worktree, so the subject's copy of the authority (stale, or tampered) would be the program that judges it (D-036)."
    },
    {
      "id": "kernel-authority-inspects-kernel-checkout",
      "file": "factory_kernel/runtime.py",
      "find": "        proc = subprocess.run(\n            argv,\n            cwd=cwd,\n            env=merged,",
      "replace": "        proc = subprocess.run(\n            argv,\n            cwd=self.repo_root,\n            env=merged,",
      "why": "Every authority would inspect the kernel's own checkout instead of the PR worktree it was handed: main's code would judge main's tree and pass any PR."
    },
    {
      "id": "trusted-program-rule-disabled",
      "file": "factory_kernel/trusted_programs.py",
      "find": "    return posix.startswith(AUTHORITY_DIRS) or posix in AUTHORITY_PROGRAMS\n",
      "replace": "    return False\n",
      "why": "No repository-relative program would count as an authority, so none would be resolved to the kernel checkout."
    },
    {
      "id": "evidence-loads-validators-from-subject",
      "file": "scripts/factory_evidence.py",
      "find": "    return load_module(HERE / \"factory_protocol.py\", \"factory_protocol_authoritative\")",
      "replace": "    return load_module(ROOT / \"scripts\" / \"factory_protocol.py\", \"factory_protocol_authoritative\")",
      "why": "The evidence authority would load the PR head's contract validator as 'authoritative', letting a PR ship the validator that certifies it."
    },
    {
      "id": "protocol-sibling-from-subject",
      "file": "scripts/factory_protocol.py",
      "find": "        sys.executable, str(HERE / \"factory_impact.py\"), \"context\",",
      "replace": "        sys.executable, str(ROOT / \"scripts\" / \"factory_impact.py\"), \"context\",",
      "why": "A sibling authority would be invoked from the tree under test rather than from beside the calling program."
    },
    {
      "id": "provenance-note-written-without-identity",
      "file": "scripts/factory_provenance.py",
      "find": "[\"git\", *KERNEL_COMMIT_ARGS, \"notes\", f\"--ref={NOTE_REF}\", \"add\", \"-f\", \"-F\", str(note_file), head],",
      "replace": "[\"git\", \"notes\", f\"--ref={NOTE_REF}\", \"add\", \"-f\", \"-F\", str(note_file), head],",
      "why": "The provenance note would be written without the kernel identity and fail on a runner that configures none, so no exact-head provenance could ever be published."
    },
    {
      "id": "proof-runner-output-unsanitised",
      "file": "scripts/factory_proof.py",
      "find": "    return rc,sanitise_output(out),round(time.monotonic()-started,3),fault",
      "replace": "    return rc,out,round(time.monotonic()-started,3),fault",
      "why": "Terminal control sequences would re-enter the stored RED tail; the attached proof can then stop being JSON after the PR-body round trip."
    },
    {
      "id": "proof-attach-round-trip-unchecked",
      "file": "scripts/factory_proof.py",
      "find": "        if not round_trip_ok(back,kind,value): die(f'ATTACH_FAIL: {kind} block did not survive the PR body round trip')",
      "replace": "        pass",
      "why": "A block that did not survive the PR body would be reported attached; the validator would refuse it hours later instead of the build failing closed."
    },
    {
      "id": "attached-sanitiser-strips-nothing",
      "file": "factory_kernel/attached.py",
      "find": "    without_escapes = _ANSI.sub(\"\", text)\n    return _CONTROL.sub(REPLACEMENT, without_escapes)",
      "replace": "    return text",
      "why": "The symptom check and the stored evidence would silently disagree with what a validator can parse."
    },
    {
      "id": "provider-nonzero-exit-bypasses-transient-classification",
      "file": "factory_kernel/providers.py",
      "find": "            transient = _transient_from_stdout(\n                stdout,\n                role=request.role,\n                events_seen=run.events_seen,\n                thinking_tokens=run.thinking_tokens,\n            )\n            if transient is not None:\n                raise transient\n",
      "replace": "",
      "why": "A non-zero CLI exit would again raise the generic failure before its envelope is classified, so a dropped stream is refused instead of retried (D-040)."
    },
    {
      "id": "failed-stage-record-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "            self._record_failed_agent(\n                paths, request.role, exc, started=started, model=model,\n                effort=request.effort, record=record, stage_run=stage_run,\n            )\n            raise\n",
      "replace": "            raise\n",
      "why": "A worker stage that failed would again leave no agent-<role>.json and no timing row, so a dropped stream is evidenced only by the exception text (D-041)."
    },
    {
      "id": "failed-stage-error-unscrubbed",
      "file": "factory_kernel/runtime.py",
      "find": "            \"error\": scrub(str(exc))[-4000:],",
      "replace": "            \"error\": str(exc)[-4000:],",
      "why": "A provider error can echo the prompt and the prompt can echo an issue body; an unscrubbed failure record would write a secret shape into an uploaded artifact."
    },
    {
      "id": "rehead-guesses-base-with-merge-base",
      "file": "factory_kernel/runtime.py",
      "find": "        old_base = self._pack_base(paths, head=head, issue=linked_issue)",
      "replace": "        old_base = self._git(\"merge-base\", f\"origin/{default}\", head)",
      "why": "The re-head would recompute the base from the current branch tip instead of reading the binding the pack declares; a pack cut from any other commit could never be fetched (the first production re-head, run 33938048704)."
    },
    {
      "id": "pack-base-ancestor-check-dropped",
      "file": "factory_kernel/provenance.py",
      "find": "    if is_ancestor is not None and not is_ancestor(base, head):\n        raise ValueError(\"builder provenance base is not an ancestor of its head\")",
      "replace": "    pass",
      "why": "A pack recording a base the head was never cut from (origin/main at publish time after it moved) would be accepted at every read."
    },
    {
      "id": "publish-base-read-from-branch-tip",
      "file": "factory_kernel/runtime.py",
      "find": "                \"python\", \"scripts/factory_provenance.py\", \"publish\",\n                \"--pr\", str(pr_number),\n                \"--artifacts\", str(paths.artifacts),\n                \"--base\", base_sha,\n",
      "replace": "                \"python\", \"scripts/factory_provenance.py\", \"publish\",\n                \"--pr\", str(pr_number),\n                \"--artifacts\", str(paths.artifacts),\n                \"--base\", self._git(\"rev-parse\", f\"origin/{self.config.default_branch}\"),\n",
      "why": "publish would record the base branch tip at publish time instead of the commit the branch was cut from, which is exactly the false binding the first production pack carried."
    },
    {
      "id": "acceptance-static-gate-skipped",
      "file": "factory_kernel/worker_runtime.py",
      "find": "            if self._static_gate_or_retry(\n                \"test_author\", cwd, paths, env, static_retry=static_retry, context=context,\n            ):\n                return\n            commit_acceptance_tests(cwd, paths.artifacts / \"test-spec.json\")",
      "replace": "            commit_acceptance_tests(cwd, paths.artifacts / \"test-spec.json\")",
      "why": "A lint-failing acceptance test would be RED-hashed and frozen again, dooming the build at the quick gate with no stage able to repair it."
    },
    {
      "id": "static-gate-retry-unbounded",
      "file": "factory_kernel/worker_runtime.py",
      "find": "        if static_retry >= STATIC_RETRIES:\n            raise NeedsHuman(",
      "replace": "        if False:\n            raise NeedsHuman(",
      "why": "A worker that never produces lint-clean files would be re-run without bound, burning the stage budget on every attempt."
    },
    {
      "id": "static-gate-after-red-commit",
      "file": "factory_kernel/worker_runtime.py",
      "find": "            if self._static_gate_or_retry(\n                \"test_author\", cwd, paths, env, static_retry=static_retry, context=context,\n            ):\n                return\n            commit_acceptance_tests(cwd, paths.artifacts / \"test-spec.json\")\n            return",
      "replace": "            commit_acceptance_tests(cwd, paths.artifacts / \"test-spec.json\")\n            if self._static_gate_or_retry(\n                \"test_author\", cwd, paths, env, static_retry=static_retry, context=context,\n            ):\n                return\n            return",
      "why": "Running the gate after the RED commit is too late: the files are already frozen, and a retry would be refused by the commit authority for touching immutable tests."
    },
    {
      "id": "implement-static-gate-skipped",
      "file": "factory_kernel/worker_runtime.py",
      "find": "            if self._static_gate_or_retry(\n                role, cwd, paths, env, static_retry=static_retry, context=context,\n            ):\n                return\n            subject = (",
      "replace": "            subject = (",
      "why": "A lint failure in production files would surface only at the whole-tree quick gate, where it ends the build instead of being repaired once."
    },
    {
      "id": "protocol-attach-publishes-again-without-base",
      "file": "scripts/factory_protocol.py",
      "find": "    print(f\"CONTRACT_ATTACHED pr={args.pr} sha256={h}\")\n",
      "replace": "    subprocess.check_call([sys.executable, str(HERE / \"factory_provenance.py\"), \"publish\", \"--pr\", str(args.pr), \"--artifacts\", str(Path(args.contract).resolve().parent)], cwd=ROOT)\n    print(f\"CONTRACT_ATTACHED pr={args.pr} sha256={h}\")\n",
      "why": "A second, argument-less publisher would return to the attach path and die on publish's required --base, losing a fully built PR at its last step (run 33941987102)."
    },
    {
      "id": "rehead-keeps-old-test-commit",
      "file": "factory_kernel/runtime.py",
      "find": "            self._reissue_red(pack, paths, worktree.path, env, new_base=new_base, new_head=new_head)\n",
      "replace": "",
      "why": "After a rebase the pack's RED proof names a commit that no longer exists in the branch; skipping the re-issue hands validation a proof whose test_commit is not an ancestor of the head."
    },
    {
      "id": "rehead-red-commit-ancestor-unchecked",
      "file": "factory_kernel/runtime.py",
      "find": "            self._git(\"merge-base\", \"--is-ancestor\", test_commit, new_head, cwd=cwd)\n",
      "replace": "            pass\n",
      "why": "A re-issued RED proof bound to a commit outside the rebased history would be handed to validation instead of refused at the re-head."
    },
    {
      "id": "rehead-accepts-any-commit-shape",
      "file": "factory_kernel/runtime.py",
      "find": "        self._verify_test_commit_files(cwd, test_commit, changed, red_files, guard_files, files)\n",
      "replace": "",
      "why": "RED would be replayed at a commit that also changes production files, which the evidence bundle's reconstruction refuses; the re-head must refuse first."
    },
    {
      "id": "e2e-browser-origin-loopback-literal",
      "file": "harness/e2e.py",
      "find": "BROWSER_ORIGIN_HOST = \"localhost\"",
      "replace": "BROWSER_ORIGIN_HOST = \"127.0.0.1\"",
      "why": "The browser would open the frontend at the loopback literal, the Secure session cookie would be discarded, and every login in the E2E would bounce back to the form (D-046)."
    },
    {
      "id": "e2e-validation-account-under-reserved-name",
      "file": "harness/bootstrap_e2e.py",
      "find": "VALIDATION_EMAIL = \"dark-factory-e2e@example.com\"",
      "replace": "VALIDATION_EMAIL = \"dark-factory-e2e@localhost.invalid\"",
      "why": "The bootstrap would provision an account under a special-use name that the login route's EmailStr validation refuses with 422; the account exists but can never sign in, and the browser E2E stalls on the form (D-047)."
    },
    {
      "id": "e2e-login-probe-assumed-green",
      "file": "harness/e2e.py",
      "find": "        probe_status, probe_cookie, probe_body, cookie = _probe_login(app, email, password)\n",
      "replace": "        probe_status, probe_cookie, probe_body, cookie = 200, True, \"\", None\n",
      "why": "The harness would launch the browser without asking the route whether it accepts the validation account, so a route refusal would surface only as a silent predicate timeout with no reason in the log (D-047)."
    },
    {
      "id": "e2e-failure-dump-skipped",
      "file": "harness/e2e.py",
      "find": "    except E2EFailure as exc:\n        _dump_failure(session, artifacts, str(exc), (password, *secrets))\n        raise\n",
      "replace": "    except E2EFailure as exc:\n        raise\n",
      "why": "A broken browser journey would leave no page, console, network or cookie evidence in the uploaded artifact, which is exactly what made D-047 a two-run diagnosis."
    },
    {
      "id": "holdout-shown-only-green",
      "file": "factory_kernel/runtime.py",
      "find": "                \"proof_summary\": proof_summary,\n            }\n            self._authority_cursor = stage_context = \"code_holdout\"\n",
      "replace": "                \"proof_summary\": \"GREEN\",\n            }\n            self._authority_cursor = stage_context = \"code_holdout\"\n",
      "why": "The code holdout would again see GREEN results and no RED evidence, which is exactly the gap a careful judge refused on in run 33955178802 (D-048)."
    },
    {
      "id": "pack-verified-after-holdout",
      "file": "factory_kernel/runtime.py",
      "find": "            pack = self._builder_pack(paths, head=head, base=base, issue=linked_issue)\n\n            self._authority_cursor = stage_context = \"attached_evidence\"\n            changed_files = sorted(x for x in changed if x)\n            proof_summary = self._holdout_proof_summary(\n                proof, pack, changed_files=changed_files,\n                worktree=worktree.path, base=base, head=head,\n            )\n            holdout_context = {\n                \"contract\": contract,\n                \"changed_files\": changed_files,\n                \"diff_sha256\": hashlib.sha256(patch.encode()).hexdigest(),\n                \"diff\": patch,\n                \"proof_summary\": proof_summary,\n            }\n            self._authority_cursor = stage_context = \"code_holdout\"\n            verdict = self._run_blinded_holdout(paths, holdout_context)\n            if verdict.get(\"verdict\") != \"pass\":\n                raise NeedsHuman(\"blinded holdout rejected PR\")\n\n",
      "replace": "            pack_dir = paths.artifacts / \"spine\"\n            self._exec(\n                [\n                    \"python\", \"scripts/factory_provenance.py\", \"fetch\",\n                    \"--head\", head, \"--base\", base, \"--issue\", str(linked_issue),\n                    \"--output-dir\", str(pack_dir),\n                ],\n                cwd=self.repo_root,\n                env={\"FACTORY_REPO\": self.config.repository},\n                credential_scope=\"github\",\n                timeout=240,\n                transcript=paths.transcripts / \"provenance-fetch.log\",\n            )\n            pack = self._read_json(pack_dir / \"builder-provenance.json\")\n\n            self._authority_cursor = stage_context = \"attached_evidence\"\n            changed_files = sorted(x for x in changed if x)\n            proof_summary = self._holdout_proof_summary(\n                proof, pack, changed_files=changed_files,\n                worktree=worktree.path, base=base, head=head,\n            )\n            holdout_context = {\n                \"contract\": contract,\n                \"changed_files\": changed_files,\n                \"diff_sha256\": hashlib.sha256(patch.encode()).hexdigest(),\n                \"diff\": patch,\n                \"proof_summary\": proof_summary,\n            }\n            self._authority_cursor = stage_context = \"code_holdout\"\n            verdict = self._run_blinded_holdout(paths, holdout_context)\n            if verdict.get(\"verdict\") != \"pass\":\n                raise NeedsHuman(\"blinded holdout rejected PR\")\n\n\n            self._authority_cursor = stage_context = \"provenance\"\n            pack = self._builder_pack(paths, head=head, base=base, issue=linked_issue)\n",
      "why": "The RED evidence handed to the holdout would be cross-checked against a pack nobody had verified yet; verify_pack must precede the judge that relies on it (D-048)."
    },
    {
      "id": "e2e-ref-matches-line-text-not-role",
      "file": "harness/e2e.py",
      "find": "        if node_role == role and node_name.startswith(name):\n            return ref\n",
      "replace": "        if name in node_name:\n            return ref\n",
      "why": "Any node whose text contains the query would be a target again, and the React root container is listed first with every visible string in its name: both credential fills would land on that div, report Done, and the form would be submitted empty (D-049, regression run 33953697016)."
    },
    {
      "id": "e2e-field-check-assumed-filled",
      "file": "harness/e2e.py",
      "find": "    filled, detail = _check_fields(\n        session, {email_ref: (\"email\", email), password_ref: (\"password\", password)})\n",
      "replace": "    filled, detail = True, \"\"\n",
      "why": "The harness would submit the form without reading the fields back, so a fill that landed nowhere would again surface only as a 20-second predicate timeout with the form still showing (D-049)."
    },
    {
      "id": "e2e-proxy-probe-assumed-green",
      "file": "harness/e2e.py",
      "find": "        proxy_status, proxy_cookie, proxy_body = _probe_proxy_login(frontend_url, email, password)\n",
      "replace": "        proxy_status, proxy_cookie, proxy_body = 200, True, \"\"\n",
      "why": "The browser would start without proof that the frontend origin proxies the login to the backend and returns the session cookie; a wrong proxy target or a stripped cookie would be indistinguishable from a page bug (D-049)."
    },
    {
      "id": "e2e-server-vite-api-target-not-exported",
      "file": "harness/serve.py",
      "find": "    env[\"VITE_API_TARGET\"] = f\"http://127.0.0.1:{backend_port}\"\n",
      "replace": "    env.pop(\"VITE_API_TARGET\", None)\n",
      "why": "The Vite child would proxy every /api request to its default port 8000, where nothing listens under the harness; the backend probe would stay green while the browser's login never reached the route (D-049 boundary)."
    },
    {
      "id": "e2e-evidence-dump-outside-run-artifacts",
      "file": "harness/e2e.py",
      "find": "        candidate = Path(configured).expanduser().resolve() / \"e2e-evidence\"\n",
      "replace": "        candidate = Path(tempfile.gettempdir()) / f\"dark-factory-e2e-{session}\"\n",
      "why": "The failure dump would be written to the runner's temp directory instead of the uploaded artifacts directory and die with the runner, which is what made D-049 a local re-derivation instead of a read (run 33953697016)."
    },
    {
      "id": "e2e-stream-probe-accepts-any-status",
      "file": "harness/e2e.py",
      "find": "    ok = raw[\"status\"] == 200 and parsed[\"tokens\"] >= 1 and parsed[\"error\"] == \"-\"\n",
      "replace": "    ok = parsed[\"tokens\"] >= 1 and parsed[\"error\"] == \"-\"\n",
      "why": "A streaming route answering 5xx with a token-shaped body would pass the harness-side stream probe, and the browser would then fail on a predicate timeout with the route's refusal unnamed (D-051)."
    },
    {
      "id": "e2e-stream-window-requires-transient-stop",
      "file": "harness/e2e.py",
      "find": "    arrived_at_once = matched is not None and polls <= 1\n",
      "replace": "    arrived_at_once = False\n",
      "why": "The transient Stop button would become a hard requirement again: an answer that lands before the first poll never shows it, so a fast, correct stream would fail the journey (D-051)."
    },
    {
      "id": "e2e-app-log-tail-not-printed",
      "file": "harness/e2e.py",
      "find": "            _dump_app_log(app, _artifact_dir(f\"df-{os.getpid()}-{app.port}\"), secrets)\n",
      "replace": "            pass\n",
      "why": "A failed journey would print nothing from the app process log, so the backend's own streaming error line and uvicorn's traceback would stay in a file nobody uploads, which is what made run 33960088633 undiagnosable (D-051)."
    },
    {
      "id": "e2e-bootstrap-missing-non-fatal",
      "file": "harness/e2e.py",
      "find": "        require(\"bootstrap ingested the locked fixture\", bootstrapped, bootstrap_detail)\n",
      "replace": "        require(\"bootstrap ingested the locked fixture\", True, bootstrap_detail)\n",
      "why": "A run whose fixture ingestion never printed E2E_BOOTSTRAP_OK would still open a browser and fail at the streaming step with the missing fixture unnamed (D-051)."
    },
    {
      "id": "validation-stage-records-nothing",
      "file": "factory_kernel/runtime.py",
      "find": "            result = self._agent_stage(\n                paths,\n                AgentRequest(\n                    role=\"holdout\",\n",
      "replace": "            result = self.provider.run(\n                AgentRequest(\n                    role=\"holdout\",\n",
      "why": "The blinded code holdout would call the provider directly again and leave no agent-holdout.* record and no timing row, which is how 25 minutes of run 33960088633 went unrecorded; caught by tests/factory/test_factory_validation_stage_telemetry.py (the provider.run funnel pin and the rehearsal's per-authority records) (D-050)."
    },
    {
      "id": "stage-line-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "    print(stage_line(row), flush=True)\n",
      "replace": "",
      "why": "No FACTORY_STAGE line would be printed as a stage ends, so the Actions log would again be silent between the dispatch banner and the traceback; caught by tests/factory/test_factory_validation_stage_telemetry.py (a stage line for every validation role, the agent and exec line shapes) (D-050)."
    },
    {
      "id": "stage-line-unflushed",
      "file": "factory_kernel/runtime.py",
      "find": "    print(stage_line(row), flush=True)\n",
      "replace": "    print(stage_line(row))\n",
      "why": "Under Actions the kernel's stdout is a pipe, so an unflushed stage line reaches the job log only when the process exits, which is exactly when it stops being useful; caught by tests/factory/test_factory_validation_stage_telemetry.py (test_the_line_is_flushed) (D-050)."
    },
    {
      "id": "over-budget-never-set",
      "file": "factory_kernel/runtime.py",
      "find": "    budget = stage_budget_seconds(role)\n    return budget is not None and wall_seconds > budget\n",
      "replace": "    return False\n",
      "why": "A stage that outlived its turn cap at the per-turn ceiling (the 934-second holdout of run 33960088633 against 350 s) would carry no over_budget flag in its record, row or line, and the caps could not be tuned from the data; caught by tests/factory/test_factory_validation_stage_telemetry.py (OverBudgetTests) (D-050)."
    },
    {
      "id": "holdout-budget-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "                    max_budget_usd=max_budget_usd(\"holdout\"),\n",
      "replace": "",
      "why": "The blinded code holdout would run with no dollar bound again, as it did for 934 seconds in run 33960088633; caught by tests/factory/test_factory_authority_bounds.py (every request site names every bound by AST, the funnel refuses the unbounded request, and the provider argv carries --max-budget-usd) (D-052)."
    },
    {
      "id": "certifier-tools-widened",
      "file": "factory_kernel/runtime.py",
      "find": "                    structured_schema={\"type\": \"object\"},\n                    allowed_tools=allowed_tools(role),\n",
      "replace": "                    structured_schema={\"type\": \"object\"},\n                    allowed_tools=(*allowed_tools(role), \"Write\"),\n",
      "why": "A pre-code certifier could write files: a judge that can edit a tree is a defect, and the bound must be the policy's own value rather than a widened copy of it; caught by tests/factory/test_factory_authority_bounds.py (the AST pin that each bound is worker_policy.<bound>(role), and the rehearsal's no-authority-can-write check) (D-052)."
    },
    {
      "id": "agent-stage-accepts-unbounded-request",
      "file": "factory_kernel/runtime.py",
      "find": "        if missing:\n            raise RuntimeError(\n                f\"agent request for role {request.role!r} is unbounded: missing \"\n",
      "replace": "        if False:\n            raise RuntimeError(\n                f\"agent request for role {request.role!r} is unbounded: missing \"\n",
      "why": "The one funnel a model is run through would pass a request missing a tool policy, a turn cap or a budget straight to the provider, which renders no flag for an absent value; caught by tests/factory/test_factory_authority_bounds.py (TheFunnelRefusesAnUnboundedRequestTests) (D-052)."
    },
    {
      "id": "judge-tools-widened-in-policy",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \"holdout\": JUDGE_TOOLS,\n",
      "replace": "    \"holdout\": WRITE_TOOLS,\n",
      "why": "The policy itself would hand the blinded code holdout Write and Edit, so a judge could change the tree it judges while every request site still looked correct; caught by tests/factory/test_factory_authority_bounds.py (a judge has no tools; no authority can write or run anything) (D-052)."
    },
    {
      "id": "triage-turns-unbounded",
      "file": "factory_kernel/triage.py",
      "find": "                max_turns=max_turns(\"triage\"),\n",
      "replace": "",
      "why": "Triage, the one model call that does not pass through _agent_stage, would run with no turn cap and only the subprocess timeout behind it; caught by tests/factory/test_factory_authority_bounds.py (the AST pin over triage.py and the triage request test) (D-052)."
    },
    {
      "id": "validation-db-without-pgvector",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      postgres:\n        image: pgvector/pgvector:pg16\n",
      "replace": "      postgres:\n        image: postgres:16\n",
      "why": "The validation database would lack the vector extension, so every retrieval query would fail with `type \"vector\" does not exist` while ingestion succeeded, and the browser journey could never see a citation (D-053)."
    },
    {
      "id": "idle-detection-never-fires",
      "file": "factory_kernel/providers.py",
      "find": "        if idle_left <= 0:\n            hung = True\n            break\n",
      "replace": "        if False:\n            hung = True\n            break\n",
      "why": "A CLI process that stopped printing events would run to its wall instead of being killed at the idle timeout, so a hung worker could not be told from a slow one and would never be retried; caught by tests/factory/test_factory_stream_timeouts.py (HangTests against the fake CLI) (D-054)."
    },
    {
      "id": "hang-not-retried",
      "file": "factory_kernel/providers.py",
      "find": "                if hangs <= HANG_RETRIES and attempt < attempts_allowed:\n                    continue\n",
      "replace": "                if False:\n                    continue\n",
      "why": "A first hang would be terminal instead of relaunched once with the worktree restored, so one silent process would cost the whole build; caught by tests/factory/test_factory_stream_timeouts.py (a hang followed by a healthy process is one retry; the hang budget beside the transient budget) (D-054)."
    },
    {
      "id": "timed-out-telemetry-dropped",
      "file": "factory_kernel/providers.py",
      "find": "                telemetry=observed,\n                timed_out=True,\n                envelope=partial,\n",
      "replace": "                telemetry={},\n                timed_out=True,\n",
      "why": "A stage killed at its wall would again record no turns, tokens, events or partial output, which is exactly the empty agent-test_author.json of run 33987381035; caught by tests/factory/test_factory_stream_timeouts.py (WallTimeoutTests, TimedOutStageLeavesTelemetryTests) and tests/factory/test_factory_worker_throughput.py (the timeout carries events_seen) (D-054)."
    },
    {
      "id": "per-role-wall-ignored",
      "file": "factory_kernel/providers.py",
      "find": "            wall_seconds=wall,\n",
      "replace": "            wall_seconds=self.config.timeout_seconds,\n",
      "why": "Every process would run under the global 2700 s maximum instead of its role's own wall, so a ten-turn judge could run four times its turn budget before anything stopped it; caught by tests/factory/test_factory_stream_timeouts.py (the reader is given the request's wall; the fake CLI is killed at the request's 2 s, not the configured 60 s) (D-054)."
    },
    {
      "id": "ceiling-reverted-to-35",
      "file": "factory_kernel/worker_policy.py",
      "find": "OBSERVED_SECONDS_PER_TURN_CEILING = 45\n",
      "replace": "OBSERVED_SECONDS_PER_TURN_CEILING = 35\n",
      "why": "The per-turn ceiling would sit below the 40.4 s/turn build run 33987381035 measured, so every wall and every over_budget flag would be computed from a rate the data already refutes, while assert_caps_fit_timeout still passes; caught by tests/factory/test_factory_stream_timeouts.py (the ceiling is stated from the run's four observations) and tests/factory/test_factory_worker_throughput.py (the ceiling is at least the measured rate) (D-054)."
    },
    {
      "id": "effort-flag-dropped",
      "file": "factory_kernel/providers.py",
      "find": "        effort = self.effort_level(request)\n        if effort is not None:\n            argv.extend([\"--effort\", effort])\n",
      "replace": "        effort = self.effort_level(request)\n        if False:\n            argv.extend([\"--effort\", effort])\n",
      "why": "Every worker would again run at the CLI's default effort with nothing bounding how long a turn thinks, which is the 2025-second, 76,248-event test_author of run 33992451400; caught by tests/factory/test_factory_effort_and_stream_logs.py (a worker asks for medium and a judge for high; a configured override wins) (D-055)."
    },
    {
      "id": "judge-effort-lowered-to-worker-level",
      "file": "factory_kernel/worker_policy.py",
      "find": "JUDGE_EFFORT = \"high\"\n",
      "replace": "JUDGE_EFFORT = \"medium\"\n",
      "why": "The five validation authorities and triage, tool-less single-prompt calls where reasoning is the whole job, would judge at the workers' cost-trimmed level; caught by tests/factory/test_factory_effort_and_stream_logs.py (judges think harder than workers; a judge says high) (D-055)."
    },
    {
      "id": "stream-tee-dropped",
      "file": "factory_kernel/providers.py",
      "find": "                idle_seconds=self.config.idle_timeout_seconds,\n                tee=tee,\n",
      "replace": "                idle_seconds=self.config.idle_timeout_seconds,\n                tee=None,\n",
      "why": "A stage killed at its wall or its idle clock would again leave only the 1500-character tail in its record and no stream in agent-<role>.log, so what a worker did in 34 minutes could not be read; caught by tests/factory/test_factory_effort_and_stream_logs.py (a wall-killed stage leaves everything it printed; a hung stage leaves its stream; the kernel record keeps the streamed log) (D-055)."
    },
    {
      "id": "thinking-telemetry-always-zero",
      "file": "factory_kernel/providers.py",
      "find": "    return total + (mark or 0)\n",
      "replace": "    return 0\n",
      "why": "Every stage would report thinking=0 whatever its stream showed, so the effort levels could never be tuned from the records and the preflight probe would always print honoured=false; caught by tests/factory/test_factory_effort_and_stream_logs.py (high-water marks are summed across resets; the record, row and line carry it; the probe line when the route honours the level) (D-055)."
    },
    {
      "id": "effort-override-not-validated",
      "file": "factory_kernel/worker_policy.py",
      "find": "        if not isinstance(level, str) or level not in EFFORT_LEVELS:\n            raise ValueError(\n                f\"kernel {name}.{role} must be one of {', '.join(EFFORT_LEVELS)}; got {level!r}\"\n            )\n",
      "replace": "        if not isinstance(level, str):\n            raise ValueError(\n                f\"kernel {name}.{role} must be one of {', '.join(EFFORT_LEVELS)}; got {level!r}\"\n            )\n",
      "why": "A misspelt level in kernel.json would load, and every stage of that role would be refused at launch by the provider instead of at configuration by the loader, one dispatch per hour; caught by tests/factory/test_factory_effort_and_stream_logs.py (a level the CLI does not accept is refused at load) (D-055)."
    },
    {
      "id": "proof-refusal-without-output-tail",
      "file": "scripts/factory_proof.py",
      "find": "        f\"  output tail (last {OUTPUT_TAIL_LINES} lines, at most {OUTPUT_TAIL_CHARS} chars):\\n{tail}\")\n",
      "replace": "        f\"  output tail: (discarded)\")\n",
      "why": "A refused RED or GREEN checkpoint would again say only its verdict on stderr, which is the whole of red-gate.log for build run 33997386843 (`PROOF_FAIL: AC-1 RED failed for the wrong reason`, 0.448 s, nothing else); caught by tests/factory/test_factory_red_evidence_and_stop.py (a wrong-reason refusal carries argv, cwd, rc, seconds and the output; a refusal quotes the tail) (D-056)."
    },
    {
      "id": "proof-failure-json-not-written",
      "file": "scripts/factory_proof.py",
      "find": "        try: write(Path(root)/failure_artifact(kind),evidence)\n",
      "replace": "        try: pass\n",
      "why": "No <red|green>-proof-failure.json would reach the run's artifacts, so the kernel's needs-human comment could quote no exit code and no output tail and the uploaded bundle would hold only the gate's stderr; caught by tests/factory/test_factory_red_evidence_and_stop.py (a wrong-reason refusal writes red-proof-failure.json; a missing command's record; a GREEN refusal's record) (D-056)."
    },
    {
      "id": "stop-still-labels-needs-human",
      "file": "factory_kernel/runtime.py",
      "find": "        except FactoryStopped as exc:\n            self._release_stopped_build(\n                issue_number, exc, paths=paths, cwd=worktree.path, pr_number=pr_number\n            )\n            raise\n",
      "replace": "        except FactoryStopped as exc:\n            self._mark_issue_human(issue_number, f\"builder failed closed: {exc}\")\n            raise\n",
      "why": "An operator stop read between stages would again escalate the issue to factory:needs-human with `builder failed closed: STOPPED`, taking factory:accepted away, as run 33989911383 did to #49, so every press of the stop button would need a human to un-escalate by hand; caught by tests/factory/test_factory_red_evidence_and_stop.py (a stop returns the issue to accepted; a stop is not needs-human; the stop handler precedes the failure handlers) (D-056)."
    },
    {
      "id": "stop-comment-missing-the-stop-issue",
      "file": "factory_kernel/runtime.py",
      "find": "        detail = scrub(str(exc)).strip()\n        stop_issues = sorted({int(n) for n in STOP_ISSUE_RE.findall(detail)})\n",
      "replace": "        detail = \"\"\n        stop_issues = sorted({int(n) for n in STOP_ISSUE_RE.findall(detail)})\n",
      "why": "The stop comment would say a stop happened and not which one: no stop issue number, no stop-check output, and the stdout line would read stop=-, so an operator reading the issue could not tell whose stop released it; caught by tests/factory/test_factory_red_evidence_and_stop.py (the stop comment names the stop issue; a kill-file stop quotes the check output) (D-056)."
    },
    {
      "id": "read-scope-dropped-for-test-author",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \"test_author\": MUTATION_SCOPE,\n",
      "replace": "    \"test_author\": JUDGE_SCOPE,\n",
      "why": "The acceptance-test author would carry an empty scope: no Edit allow for app/ and no deny for the trust root, so the provider would render no path rules and the worker could read kernel source, the harness and the workflows exactly as the test_author of run 34002520477 did for 46 Read calls; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (mutation roles may write the product tree; every tool-bearing role is denied the trust root; a mutation role is told paths not bare tools) (D-057)."
    },
    {
      "id": "read-scope-lets-factory-kernel-through",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \".git/**\",\n    \"factory_kernel/**\",\n",
      "replace": "    \".git/**\",\n",
      "why": "Every worker's deny list would omit factory_kernel/**, and a file inside the working directory is readable without any rule, so the kernel's own source would be back in every builder's reach while the allow rules looked complete; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (every tool-bearing role is denied the trust root; a mutation role is told paths not bare tools; the probe argv carries the deny) (D-057)."
    },
    {
      "id": "draft-deadline-kills-again",
      "file": "factory_kernel/providers.py",
      "find": "            if watch is not None:\n                watch.observe(event, index=len(events) - 1)\n    # The deadline is read off the watch after the loop, never acted on inside it: nothing\n    # about it ends the process (D-066).\n    draft_missed = watch.deadline_missed if watch is not None else False\n    returncode: int | None = None\n    if timed_out or hung:\n",
      "replace": "            if watch is not None:\n                watch.observe(event, index=len(events) - 1)\n                if watch.deadline_missed:\n                    break\n    draft_missed = watch.deadline_missed if watch is not None else False\n    returncode: int | None = None\n    if timed_out or hung or draft_missed:\n",
      "why": "D-057's kill restored: the stream reader would break out and the process be killed the moment a turn past the draft deadline began with nothing written, so a worker reading its way to a draft would be truncated mid-read exactly as run 34042566216's test_author was at turn 25 of 30 after 22 in-scope reads, on a model that wrote a complete test file in other runs; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (a worker that only reads runs to its cap and the cap rule refuses it; a worker that drafts past the deadline completes with it recorded) (D-066)."
    },
    {
      "id": "draft-deadline-fires-on-a-run-that-wrote",
      "file": "factory_kernel/providers.py",
      "find": "            if name in WRITE_TOOL_NAMES:\n                if self.wrote_at_turn is None:\n                    self.wrote_at_turn = self.turns\n",
      "replace": "            if name in WRITE_TOOL_NAMES:\n                pass\n",
      "why": "A Write or Edit would not disarm the deadline, so every mutation worker that drafted early and then read on, which is what issue #49's healthy author did (Edits at turn ~5, 15 turns), would be recorded as having passed its deadline: the one stage shape the signal is meant to distinguish from a reading-only run would read the same in the telemetry; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (a worker that wrote at turn two is untouched; a write in time disarms the watch) (D-057, D-066)."
    },
    {
      "id": "draft-deadline-refuses-the-stage",
      "file": "factory_kernel/providers.py",
      "find": "            observed.update(draft_deadline_telemetry(run))\n        if run.hung:\n",
      "replace": "            raise ProviderStageError(\n                f\"agent worker role={request.role!r} wrote nothing by turn {run.deadline_turn} \"\n                f\"of {request.max_turns}\",\n                telemetry=draft_deadline_telemetry(run),\n                envelope=partial,\n            )\n        if run.hung:\n",
      "why": "The observation would become a verdict again: a process that had written nothing when the turn past its deadline began would be refused when it ended, so a worker that read to turn 25 and then drafted at turn 26 inside its own budget would lose the draft it had on disk and the stage would fail rather than reach the gates; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (a worker that drafts past the deadline completes with it recorded, outcome ok; a worker that only reads returns marked cap_reached for D-065's rule to refuse) (D-066)."
    },
    {
      "id": "guard-failure-at-green-ignored",
      "file": "scripts/factory_proof.py",
      "find": "    if rc!=0:\n        reason=f\"{ac} guard failed on the unchanged tree\" if stage=='red' else f\"{ac} guard broken by the implementation\"\n        refuse(kind,cp,reason,rc=rc,out=out,seconds=seconds,stage=stage)\n",
      "replace": "    if rc!=0 and stage=='red':\n        reason=f\"{ac} guard failed on the unchanged tree\"\n        refuse(kind,cp,reason,rc=rc,out=out,seconds=seconds,stage=stage)\n",
      "why": "A guard checkpoint that fails after the implementation would be recorded as a GREEN result with a non-zero exit and the build would go on, so the kept behaviour the contract pinned (issue #103's AC-3: switching conversations still aborts the old stream) could be broken by the very change that was supposed to keep it; caught by tests/factory/test_factory_guard_checkpoints.py (a guard broken by the implementation is refused at green with `green-proof-failure.json`; the evidence bundle accepts only exit-0 guard results) (D-058)."
    },
    {
      "id": "guard-only-spec-accepted",
      "file": "scripts/factory_proof.py",
      "find": "    if guards_in(cps)==len(cps): die('test spec must declare at least one red checkpoint; a spec of only guards proves no change')\n",
      "replace": "    if guards_in(cps)>len(cps): die('test spec must declare at least one red checkpoint; a spec of only guards proves no change')\n",
      "why": "A test author could declare every checkpoint a guard, RED would prove that nothing fails on the unchanged tree, and GREEN would prove the same after the change: a build with no red loop at all, wearing a RED_PROVED line; caught by tests/factory/test_factory_guard_checkpoints.py (a spec of only guards is refused) (D-058)."
    },
    {
      "id": "guard-treated-as-red",
      "file": "scripts/factory_proof.py",
      "find": "    if checkpoint_kind(cp)=='guard':\n        rc,out,seconds=prove_guard(cp,'red')\n",
      "replace": "    if checkpoint_kind(cp)=='guard' and False:\n        rc,out,seconds=prove_guard(cp,'red')\n",
      "why": "A guard would run through the red path at RED and be refused as `unexpectedly passed`, which is exactly build run 34008561672's refusal: the protocol would once again have no way to express kept behaviour while the prompt and the contract said it did; caught by tests/factory/test_factory_guard_checkpoints.py (a guard that passes on the unchanged tree is proved at RED; `red` proves the red checkpoint red and the guard green with guards=1) (D-058)."
    },
    {
      "id": "second-attempt-record-overwrites-the-first",
      "file": "factory_kernel/runtime.py",
      "find": "    while (transcripts / f\"{stem}.json\").exists() or (transcripts / f\"{stem}.log\").exists():\n        run += 1\n        stem = f\"{base}.{run}\"\n    return stem, run\n",
      "replace": "    return stem, run\n",
      "why": "Every run of a stage would write `agent-<role>.json` and stream into `agent-<role>.log`, so the static-gate hand-back's second `test_author` would again overwrite the first run's record (the 2687 s, 41-turn, two-process first run of 34008561672 survived only as headers in the appended log) and every row would say stage_run=1; caught by tests/factory/test_factory_stage_runs.py (the second run writes its own json and log and the first survives; the hand-back re-run keeps the first run's record; each run leaves its own row and line) (D-058)."
    },
    {
      "id": "thinking-cap-set-but-not-exported",
      "file": "factory_kernel/providers.py",
      "find": "        cap = self.thinking_cap(request)\n        if cap is not None:\n            env[THINKING_CAP_ENV] = str(cap)\n",
      "replace": "        cap = self.thinking_cap(request)\n        if False:\n            env[THINKING_CAP_ENV] = str(cap)\n",
      "why": "A cap set in worker_policy.ROLE_THINKING_CAP or provider.thinking_cap_overrides would be read and exported to no CLI process: the operator would believe test_author's thinking was bounded while the worker still thought as the sixth build of issue #103 did (121,065 tokens in 15 turns, run 34013852733); caught by tests/factory/test_factory_thinking_cap.py (a configured override is exported as the variable; the launched process gets exactly that environment) (D-059)."
    },
    {
      "id": "thinking-cap-honoured-rule-inverted",
      "file": "scripts/factory_thinking_cap_probe.py",
      "find": "    return not within_cap(uncapped, cap) and within_cap(thinking, cap) and clearly_below(thinking, uncapped)\n",
      "replace": "    return not (not within_cap(uncapped, cap) and within_cap(thinking, cap) and clearly_below(thinking, uncapped))\n",
      "why": "The preflight line would print honoured=true exactly when the route ignored MAX_THINKING_TOKENS, and the table every row of which waits on that line would be filled in on the strength of the opposite of the evidence; caught by tests/factory/test_factory_thinking_cap.py (a cap is honoured only when both hold; the line when the route ignores the variable) (D-059)."
    },
    {
      "id": "contract-guard-id-scheme-accepted",
      "file": "scripts/factory_protocol.py",
      "find": "AC = re.compile(r\"^AC-[1-9][0-9]*$\")\n",
      "replace": "AC = re.compile(r\"^AC-[A-Z]?[1-9][0-9]*$\")\n",
      "why": "A guard behaviour written as `AC-G1` (run 34015187797, the first build after D-058) would compile, the certifiers would hash an id scheme the prompt never defined, and the test author and the design's ac_mapping would be handed ids outside the one scheme every downstream authority matches with `^AC-[1-9][0-9]*$`; caught by tests/factory/test_factory_guard_behavior_ids.py (`AC-G1` is refused with the rule named; the run's contract is refused and compiles once its guards are `AC-5..AC-7`) (D-060)."
    },
    {
      "id": "model-override-ignored-by-run",
      "file": "factory_kernel/providers.py",
      "find": "        override = self.config.model_overrides.get(request.role)\n        if override:\n            return override\n",
      "replace": "        override = self.config.model_overrides.get(request.role)\n        if False:\n            return override\n",
      "why": "A provider.model_overrides row would be parsed, validated and listed by the preflight's route probe and then never used: every stage would still launch on provider.model, so the operator who routed test_author off z-ai/glm-5.3-flash after six dead builds of issue #103 would watch a seventh die on it, with a stage line that said model=z-ai/glm-5.3-flash; caught by tests/factory/test_factory_model_overrides.py (an override wins over the default and over the architecture rule; the launched argv names it; the record and the stage line say it) (D-061)."
    },
    {
      "id": "model-override-unknown-role-accepted",
      "file": "factory_kernel/worker_policy.py",
      "find": "        if not isinstance(role, str) or role not in ROLE_MAX_TURNS:\n            raise ValueError(\n                f\"kernel {name} names a role the worker policy does not know: {role!r}\"\n            )\n        if not isinstance(model, str) or not model.strip():\n",
      "replace": "        if not isinstance(role, str):\n            raise ValueError(\n                f\"kernel {name} names a role the worker policy does not know: {role!r}\"\n            )\n        if not isinstance(model, str) or not model.strip():\n",
      "why": "A row for `tester` or `test-author` would load cleanly and route nothing, so the operator would believe the role had been moved to another model while the builds kept dying on the old one, and the preflight would probe a model no stage uses; caught by tests/factory/test_factory_model_overrides.py (a role the policy does not know is refused at load, by the validator and by the script) (D-061)."
    },
    {
      "id": "model-route-probe-skips-override-models",
      "file": "scripts/factory_models.py",
      "find": "    for model in overrides.values():\n        ordered.append(model)\n",
      "replace": "    for model in ():\n        ordered.append(model)\n",
      "why": "The preflight would prove the worker and architecture models and never the model a role was routed to, so a build could reach test_author before discovering that its route was closed, which is exactly the mid-build failure the route probe exists to move to preflight; caught by tests/factory/test_factory_model_overrides.py (the list carries every override value, once, after the two; the workflow's loop reads the list) (D-061)."
    },
    {
      "id": "e2e-stream-probe-passes-without-sources",
      "file": "harness/e2e.py",
      "find": "    ok = ok and cited_fixture\n",
      "replace": "    ok = ok and True\n",
      "why": "A stream that answered 200 with tokens and [DONE] and no sources event would pass the harness-side probe, and the browser would then fail on a citation predicate timeout with the missing retrieval tool call unnamed, which is exactly run 34020965800 (stream_chat round=1 finish_reason=stop tool_calls_made=0); caught by tests/factory/test_e2e_stream_evidence.py (the probe requires a sources event naming the fixture; the journey refuses before the browser when the stream had no citation) (D-062)."
    },
    {
      "id": "e2e-fixture-title-missing-non-fatal",
      "file": "harness/e2e.py",
      "find": "        require(\"journey question is grounded in the fixture title\", question_ok,\n                question_detail)\n",
      "replace": "        require(\"journey question is grounded in the fixture title\", True,\n                question_detail)\n",
      "why": "A catalog row without a title would render the question as `the video titled \"\"`, the ungrounded question this decision removes, and the run would spend a message and open a browser on it with E2E_FIXTURE_TITLE_MISSING printed and ignored; caught by tests/factory/test_e2e_stream_evidence.py (the journey refuses before the stream probe when the title is missing) (D-062)."
    },
    {
      "id": "e2e-question-template-ignored",
      "file": "harness/e2e.py",
      "find": "    question = str(template).replace(\"{title}\", title).replace(\"{description}\", description)\n",
      "replace": "    question = str(template)\n",
      "why": "The probe and the browser would ask the template's literal text, `the video titled \"{title}\"`, so the question would name no video the model can search for and whether a citation appeared would be luck again; caught by tests/factory/test_e2e_stream_evidence.py (the template is rendered with the fixture title and printed; the browser asks the same rendered question as the probe) (D-062)."
    },
    {
      "id": "acceptance-guard-file-required-to-change",
      "file": "factory_kernel/git_authority.py",
      "find": "    unchanged_red = sorted(red_files - changed)\n",
      "replace": "    unchanged_red = sorted(set(declared) - changed)\n",
      "why": "The old rule: every declared file, a guard's included, must be in the dirty checkout, so a guard on the existing kept test the author correctly left untouched is refused, which is exactly run 34027157595 (`test-author changed [ChatArea.test.tsx]; declared acceptance files are [ChatArea.test.tsx, useStreamingResponse.test.ts]`); caught by tests/factory/test_factory_guard_files_untouched.py (a guard on an untouched existing test passes and the commit holds only the red file) (D-064)."
    },
    {
      "id": "acceptance-guard-on-missing-file-accepted",
      "file": "factory_kernel/git_authority.py",
      "find": "        if not (cwd / path).is_file():\n",
      "replace": "        if False:\n",
      "why": "A guard on a file that does not exist at the head would be committed as if it pinned something, and the RED gate would refuse `unsafe/missing test file` one stage later with the test-author commit already on the branch; caught by tests/factory/test_factory_guard_files_untouched.py (a guard on a missing file is refused before the commit, naming the file) (D-064)."
    },
    {
      "id": "red-gate-guard-file-required-to-change",
      "file": "scripts/factory_proof.py",
      "find": "    unchanged_red=sorted(red_files-changed_set)\n",
      "replace": "    unchanged_red=sorted(set(declared)-changed_set)\n",
      "why": "The old equality in the RED gate: a test commit that leaves a guard's file untouched, which is what the commit authority now accepts, would be refused as `unchanged` at RED, so the first correct guard would die one stage later than in run 34027157595; caught by tests/factory/test_factory_guard_files_untouched.py (red proves a guard whose file the test commit left alone; a guard on an untouched file passes the rule) (D-064)."
    },
    {
      "id": "red-gate-stray-file-accepted",
      "file": "scripts/factory_proof.py",
      "find": "    if stray: die(",
      "replace": "    if False and stray: die(",
      "why": "A test commit carrying a file no checkpoint declares would be proved RED and its undeclared change would ride into the implementation as part of the immutable test commit; caught by tests/factory/test_factory_guard_files_untouched.py (a stray file in the test commit is refused and named) (D-064)."
    },
    {
      "id": "cap-treated-as-failure-again",
      "file": "factory_kernel/providers.py",
      "find": "            if envelope.subtype == CAP_SUBTYPE and request.role in REPO_MUTATION_ROLES:\n",
      "replace": "            if False:\n",
      "why": "The old behaviour: the CLI exits 1 at --max-turns and the provider refused the stage on that exit code, so run 34033360798 lost a written and edited test file without the static gate, the commit authority or RED ever seeing it; caught by tests/factory/test_factory_cap_ends_the_loop.py (through the fake CLI a mutation role stopped at its cap with rc=1 returns marked cap_reached) (D-065)."
    },
    {
      "id": "cap-unwrap-refuses-a-mutation-role",
      "file": "factory_kernel/providers.py",
      "find": "    if subtype == CAP_SUBTYPE and role in REPO_MUTATION_ROLES:\n",
      "replace": "    if False:\n",
      "why": "The unwrap is the second place a cap envelope is classified (a CLI that exits 0 at its cap reaches it directly); refusing it there fails the build exactly as before D-065; caught by tests/factory/test_factory_cap_ends_the_loop.py (unwrap returns a mutation role's error_max_turns envelope marked cap_reached; through the fake CLI with rc=0) (D-065)."
    },
    {
      "id": "cap-with-clean-worktree-proceeds",
      "file": "factory_kernel/worker_runtime.py",
      "find": "        if not files:\n            raise NeedsHuman(\n                f\"{role} reached its turn cap",
      "replace": "        if False:\n            raise NeedsHuman(\n                f\"{role} reached its turn cap",
      "why": "A capped worker that wrote nothing would go on to the static gate and the commit authority, which refuses a clean checkout with a message about the spec or the envelope rather than about the cap, so the human is told the wrong thing; caught by tests/factory/test_factory_cap_ends_the_loop.py (a clean checkout at the cap is refused as no_draft_at_cap before any gate, for test_author, implement and repair) (D-065)."
    },
    {
      "id": "no-spec-at-cap-accepted",
      "file": "factory_kernel/worker_runtime.py",
      "find": "        problem = self._spec_problem(cwd, paths.artifacts / \"test-spec.json\")\n        if problem is not None:\n",
      "replace": "        problem = self._spec_problem(cwd, paths.artifacts / \"test-spec.json\")\n        if False:\n",
      "why": "Run 34033360798's test author had written ChatArea.test.tsx and no test-spec.json; without this check the capped draft goes to the static gate (a wasted hand-back process) and then dies in the commit authority's spec loader without naming the files that were written; caught by tests/factory/test_factory_cap_ends_the_loop.py (a dirty checkout without a spec, with an unparsable spec, or with a spec declaring a file not on disk is refused as no_spec_at_cap naming the written files, before the static gate) (D-065)."
    },
    {
      "id": "glob-dropped-from-mutation-roles",
      "file": "factory_kernel/worker_policy.py",
      "find": "READ_TOOLS = (\"Read\", \"Glob\", \"Grep\")\n",
      "replace": "READ_TOOLS = (\"Read\", \"Grep\")\n",
      "why": "A mutation worker without Glob navigates by guessing file names and burns turns on a tool it does not have, which is half of what run 34033360798 spent its 31 turns on; caught by tests/factory/test_factory_cap_ends_the_loop.py (every tool-bearing role has Read, Glob and Grep; a mutation role asks the CLI for all five tools) (D-065)."
    },
    {
      "id": "bare-mode-collapses-the-tool-surface",
      "file": "factory_kernel/providers.py",
      "find": "ISOLATION_FLAGS: tuple[str, ...] = (\"--safe-mode\", \"--setting-sources\", \"\")\n",
      "replace": "ISOLATION_FLAGS: tuple[str, ...] = (\"--bare\",)\n",
      "why": "--bare sets CLAUDE_CODE_SIMPLE=1, under which the pinned CLI registers only Read and Edit whatever --tools names (init tools=['Edit','Read'] on 2.1.245), so the policy's Glob, Grep and Write never reach the model however the table reads; caught by tests/factory/test_factory_cap_ends_the_loop.py (the provider launches with --safe-mode --setting-sources '' and never --bare) (D-065)."
    },
    {
      "id": "cap-reached-not-recorded",
      "file": "factory_kernel/runtime.py",
      "find": "            \"cap_reached\": bool(getattr(result, \"cap_reached\", False)),\n",
      "replace": "            \"cap_reached\": False,\n",
      "why": "A capped stage's record, timing row and FACTORY_STAGE line would read as an ordinary returned stage, and the needs-human comment (which reads the records) would not say the cap was reached when a gate refuses the draft; caught by tests/factory/test_factory_cap_ends_the_loop.py (the record and the row say cap_reached and the line prints cap_reached=true; a capped test_author through the gates leaves the flag in its record) (D-065)."
    },
    {
      "id": "draft-deadline-not-recorded",
      "file": "factory_kernel/runtime.py",
      "find": "    if not getattr(result, \"draft_deadline_missed\", False):\n        return {}\n",
      "replace": "    if True:\n        return {}\n",
      "why": "A stage that returned after passing its draft deadline would record none of it: no draft_deadline_missed, no draft_deadline_turn, no reads and no files_read in agent-<role>.json, nothing on its timing row or its FACTORY_STAGE line, and nothing in the needs-human comment when a gate refuses the draft - so the one signal the deadline still exists to give, the cost of a reading-heavy stage, would be invisible; caught by tests/factory/test_factory_read_scope_and_draft_deadline.py (the stage record, row and line say so; a worker that drafts past the deadline completes with it recorded; the paths read are capped in the record and the count is not) (D-066)."
    },
    {
      "id": "cap-comment-silent",
      "file": "factory_kernel/runtime.py",
      "find": "        if not capped:\n            return \"\"\n",
      "replace": "        if True:\n            return \"\"\n",
      "why": "A gate refusing a capped draft would post its verdict with no word that the worker's loop had been ended by the cap, which is the one fact the human needs to read the verdict correctly; caught by tests/factory/test_factory_cap_ends_the_loop.py (the evidence names the capped stage, and build_issue's comment carries it when the commit authority refuses) (D-065)."
    },
    {
      "id": "cap-envelope-refused-as-malformed",
      "file": "factory_kernel/providers.py",
      "find": "    if not isinstance(raw, Mapping) or \"is_error\" not in raw:\n        raise RuntimeError(f\"agent worker role={role!r} did not return a JSON result envelope\")\n    subtype = str(raw.get(\"subtype\") or \"\")\n",
      "replace": "    if not isinstance(raw, Mapping) or \"is_error\" not in raw or \"result\" not in raw:\n        raise RuntimeError(f\"agent worker role={role!r} did not return a JSON result envelope\")\n    subtype = str(raw.get(\"subtype\") or \"\")\n",
      "why": "The shape guard would require a key the real error envelope does not have: measured on run 34047586142, an error_max_turns result event carries duration_api_ms duration_ms errors fast_mode_* is_error modelUsage num_turns permission_denials queued_turn_count session_id stop_reason subagent_stats subtype terminal_reason total_cost_usd type usage uuid and no result at all, so D-065's cap branch would be unreachable for the only payload it exists for and the build would die on 'did not return a JSON result envelope' with the draft on disk, exactly as that run did; caught by tests/factory/test_factory_cap_ends_the_loop.py (the cap fixture is the real payload key for key, and a cap envelope with no result key reaches the cap branch for every mutation role) (D-068)."
    },
    {
      "id": "result-required-before-the-cap-is-classified",
      "file": "factory_kernel/providers.py",
      "find": "    subtype = str(raw.get(\"subtype\") or \"\")\n    if subtype == CAP_SUBTYPE and role in REPO_MUTATION_ROLES:\n",
      "replace": "    subtype = str(raw.get(\"subtype\") or \"\")\n    if \"result\" not in raw:\n        raise RuntimeError(f\"agent worker role={role!r} did not return a JSON result envelope\")\n    if subtype == CAP_SUBTYPE and role in REPO_MUTATION_ROLES:\n",
      "why": "The order is the fix: result is required only of a NON-error envelope, and requiring it of an error envelope before the cap is classified refuses the real error_max_turns payload as malformed again, whatever the shape guard says - the same lost build as run 34047586142, reached by a different line; caught by tests/factory/test_factory_cap_ends_the_loop.py (the cap branch is reached before result is required; a capped envelope's content is the empty string, not a refusal) (D-068)."
    },
    {
      "id": "static-gate-formatter-not-applied",
      "file": "factory_kernel/static_gate.py",
      "find": "    formatted = _apply_formatter(worktree, files, env, runner=runner, timeout=timeout)\n",
      "replace": "    formatted: tuple[str, ...] = ()\n",
      "why": "A whitespace difference would cost a model stage again: build run 34047586142's only static-gate finding was biome's 'Formatter would have printed the following content:' on a multi-line call, and the kernel spent a second test_author process, thirty turns and the whole budget on it and produced nothing, while the formatter that removes it deterministically runs in the same tree; caught by tests/factory/test_factory_static_gate.py (a formatting-only finding is fixed and never becomes a finding; through the real _agent it never reaches a hand-back and the reformatted file is what is committed) (D-068)."
    },
    {
      "id": "static-gate-applies-a-lint-fix",
      "file": "factory_kernel/static_gate.py",
      "find": "             [\"bun\", \"x\", \"biome\", \"format\", \"--write\", *frontend])\n",
      "replace": "             [\"bun\", \"x\", \"biome\", \"check\", \"--write\", \"--unsafe\", *frontend])\n",
      "why": "The gate would rewrite the worker's code, not its whitespace: 'biome check --write' applies lint fixes, which can change behaviour, to files the worker is about to have committed and RED-hashed, so a machine edit no worker and no reviewer ever saw would enter the acceptance tests; only formatting is safe to apply unasked; caught by tests/factory/test_factory_static_gate.py (the formatter commands are the repository's own formatters, and no lint-fix flag appears anywhere in the module) (D-068)."
    },
    {
      "id": "route-probe-made-conditional",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      - name: Prove the worker's model route with the pinned CLI\n        shell: bash\n",
      "replace": "      - name: Prove the worker's model route with the pinned CLI\n        if: ${{ inputs.run_answered_probes == true || inputs.run_answered_probes == 'true' }}\n        shell: bash\n",
      "why": "Reachability would stop being proved on the dispatches that matter: D-070 moved the three ANSWERED probes (effort, thinking cap, read scope) behind the run_answered_probes input precisely because the model ROUTE probe stays on every run - a slug that has gone unreachable, or a new provider.model_overrides value nobody proved, must refuse in the preflight rather than close mid-build after the earlier stages have spent their budgets (D-061); putting the route probe on the same switch means the hourly schedule, which passes no inputs, would dispatch a build against an unproven route; caught by tests/factory/test_factory_workflow_hygiene.py (the route probe carries no `if:`, and the gated steps are exactly the three answered probes) (D-070)."
    },
    {
      "id": "answered-probes-still-unconditional-on-dispatch",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      - name: Probe whether the route honours an effort level\n        # Answered, so measured daily rather than per dispatch (D-070): six readings on this\n        # route flipped false/true/false/true/false/false with no stable signal, so the level is\n        # not a bound here and a seventh reading of the same question costs a run its minutes.\n        if: ${{ inputs.run_answered_probes == true || inputs.run_answered_probes == 'true' }}\n",
      "replace": "      - name: Probe whether the route honours an effort level\n        # Answered, so measured daily rather than per dispatch (D-070): six readings on this\n        # route flipped false/true/false/true/false/false with no stable signal, so the level is\n        # not a bound here and a seventh reading of the same question costs a run its minutes.\n",
      "why": "The saving would be silently given back: the effort probe costs 71-215 s of a worker run's wall clock and two model calls, measured over nine runs, to re-answer a question six readings already settled (honoured flips run to run on this route), and D-070 moved it to the daily regression on main; an ungated step runs on every hourly schedule again, where no input is passed, so the factory pays for it forever while the decision claims it does not; caught by tests/factory/test_factory_workflow_hygiene.py (every answered probe's head carries the run_answered_probes gate) (D-070)."
    },
    {
      "id": "regression-workflow-missing-the-probes",
      "file": ".github/workflows/dark-factory-main-regression.yml",
      "find": "      - name: Probe whether the route honours an effort level\n        # The worker's probe, body for body (tests/factory/test_factory_workflow_hygiene.py\n        # pins them equal), so one marker corpus spans both workflows. `continue-on-error`\n        # because this job's verdict is the harness on main, never a probe (D-070).\n        continue-on-error: true\n        shell: bash\n        env:\n          ANTHROPIC_AUTH_TOKEN: ${{ secrets.OPENROUTER_API_KEY }}\n        run: |\n          # Whether the route honours the level is measured, not assumed: the worker model\n          # twice on one fixed reasoning prompt, one turn each, at the lowest and the highest\n          # level the policy uses (scripts/factory_effort_probe.py), counting the thinking each\n          # stream showed. The line is data for tuning `worker_policy.ROLE_EFFORT`; a\n          # `honoured=false` is printed, never a refusal (D-055).\n          worker_model=\"$(python3 -c 'import json;print(json.load(open(\".factory/kernel.json\"))[\"provider\"][\"model\"])')\"\n          timeout 400 python3 scripts/factory_effort_probe.py --model \"$worker_model\" \\\n            || echo \"FACTORY_PREFLIGHT_EFFORT_PROBE model=$worker_model low_thinking=0 high_thinking=0 honoured=false error=probe-did-not-run\"\n\n",
      "replace": "",
      "why": "The measurement series would end rather than move: D-070's whole trade is that the three answered probes leave the per-dispatch path BECAUSE the daily regression on main keeps measuring them, so their marker lines keep landing in an archived run and a change to the model, the route or the pinned CLI still shows up within a day; a regression workflow with a probe step deleted keeps the saving and drops the evidence, which is the one outcome the decision refuses; caught by tests/factory/test_factory_workflow_hygiene.py (all four probes are present in the daily regression, ahead of the gate, unconditional, and every marker format appears in both workflows) (D-070)."
    },
    {
      "id": "red-refusal-never-handed-back",
      "file": "factory_kernel/runtime.py",
      "find": "                if record is None or attempt > RED_HANDBACK_ATTEMPTS:\n",
      "replace": "                if record is None or attempt > 0:\n",
      "why": "The RED gate would end the build on the first refusal again, which is the old behaviour that cost build run 34054922788 about $15 and forty-five minutes for one mis-declared expected_failure with the correct test already on disk: the author declared the message getByRole prints while its own query was a label query, and nothing ever told it so; caught by tests/factory/test_factory_red_handback.py (a wrong-reason refusal triggers exactly one hand-back and a second RED run, and the re-run author is given the refusal's argv, rc, seconds, declared string and output tail) (D-069)."
    },
    {
      "id": "red-hand-back-is-unbounded",
      "file": "factory_kernel/runtime.py",
      "find": "RED_HANDBACK_ATTEMPTS = 1\n",
      "replace": "RED_HANDBACK_ATTEMPTS = 99\n",
      "why": "A worker that cannot answer the gate would be re-run until the run's wall or budget ended it, paying a full test_author process per attempt with nothing converging - the same unbounded shape the static gate refused to take (D-043, STATIC_RETRIES = 1). One hand-back is a correction; an unbounded one is a loop; caught by tests/factory/test_factory_red_handback.py (the bound is one, and three consecutive refusals still run the author exactly twice and the gate exactly twice) (D-069)."
    },
    {
      "id": "red-hand-back-weakening-check-dropped",
      "file": "factory_kernel/runtime.py",
      "find": "        self._refuse_weakened_spec(before, self._spec_shape(spec_path))\n",
      "replace": "        _ = (before, self._spec_shape(spec_path))\n",
      "why": "The hand-back would become a licence to weaken the test: the cheapest way to make a wrong-reason refusal disappear is to drop the checkpoint or re-declare it kind guard, which has no expected_failure and only has to exit 0, so the gate would go green while proving strictly less than the first attempt did and the build would ship an acceptance suite the refusal itself had talked the author out of; caught by tests/factory/test_factory_red_handback.py (a re-draft that drops a checkpoint or downgrades a red one to a guard is refused as red_handback_weakened_spec, naming the change, before the gate is re-run) (D-069)."
    },
    {
      "id": "red-hand-back-second-record-overwrites-the-first",
      "file": "factory_kernel/runtime.py",
      "find": "        second = paths.artifacts / RED_FAILURE_ARTIFACT\n        if second.is_file():\n            second.replace(paths.artifacts / RED_FAILURE_ARTIFACT_2)\n        KernelRuntime._write_json(paths.artifacts / RED_FAILURE_ARTIFACT, dict(first))\n",
      "replace": "        second = paths.artifacts / RED_FAILURE_ARTIFACT\n        if not second.is_file():\n            KernelRuntime._write_json(second, dict(first))\n",
      "why": "The hand-back would destroy the evidence it was given: the gate writes red-proof-failure.json on every attempt, so without the rename only the second attempt's record survives and the needs-human comment could not show what the author was actually told the first time - the same class of loss as the overwritten agent-test_author.json of run 34008561672, which left a 2687-second stage with no record (D-058); caught by tests/factory/test_factory_red_handback.py (both attempts survive as red-proof-failure.json and red-proof-failure.2.json with their own reasons and exit codes, and the comment names both) (D-069)."
    },
    {
      "id": "carry-ignores-a-moved-base",
      "file": "factory_kernel/carry.py",
      "find": "    if identity[\"base_sha\"] != expected_base_sha:\n",
      "replace": "    if False:\n",
      "why": "A carry whose upstream was certified against another commit of main would be restored onto this build's tree, so the compiled context's file hashes, the design's planned files and the governor's decision would all describe a tree that no longer exists; caught by tests/factory/test_factory_carry.py (a moved base is refused by verify_carry, and _carry_reuse misses with reason=base_moved and restores nothing) (D-071)."
    },
    {
      "id": "carry-ignores-an-edited-issue",
      "file": "factory_kernel/carry.py",
      "find": "                \"body\": issue.get(\"body\"),\n",
      "replace": "                \"body\": \"\",\n",
      "why": "The issue digest is what notices an edit; hashing the title alone means a rewritten body reuses a contract drawn from the old one, and the build implements the issue as it used to read; caught by tests/factory/test_factory_carry.py (the digest moves with the body, and an edited issue misses with reason=issue_changed) (D-071)."
    },
    {
      "id": "carry-skips-the-architecture-gate",
      "file": "factory_kernel/runtime.py",
      "find": "                self._gate_architecture(paths, cwd, env, prefix=\"carry-\")\n",
      "replace": "                pass\n",
      "why": "A reused build would take the restored architecture-governor.json on trust instead of recompiling it against .factory/architecture.json, the contract, the context and the design, which is the whole reason a restored artifact can be believed; caught by tests/factory/test_factory_carry.py (a hit runs carry-read, contract-gate, context-gate, architecture-gate and architecture-scope in that order) (D-071)."
    },
    {
      "id": "carry-artifact-hashes-unverified",
      "file": "factory_kernel/carry.py",
      "find": "        if sha256_bytes(text.encode(\"utf-8\")) != expected:\n",
      "replace": "        if False:\n",
      "why": "Anything that could reach the notes ref could then widen the design's planned_files or rewrite the contract, and the build would carry it as certified upstream; caught by tests/factory/test_factory_carry.py (a tampered artifact and a tampered hash are both refused as artifact_hash_mismatch, before anything is restored) (D-071)."
    },
    {
      "id": "carry-readable-by-a-worker",
      "file": "factory_kernel/worker_policy.py",
      "find": "    \".git\",\n    \".git/**\",\n",
      "replace": "",
      "why": "The read boundary is the deny list and .git sits inside the worktree, so without these patterns a worker's Read, Grep and Glob reach the object database and with it both notes refs -- the carry it must never see or shape, and the provenance pack; caught by tests/factory/test_factory_carry.py (every tool-bearing role denies .git and .git/**, and the CLI receives Read(./.git/**)) (D-071)."
    },
    {
      "id": "rehead-compares-the-whole-file-map",
      "file": "factory_kernel/runtime.py",
      "find": "        self._verify_test_commit_files(cwd, test_commit, changed, red_files, guard_files, files)\n",
      "replace": "        if changed != sorted(files):\n            raise NeedsHuman(\"rebased test-author commit does not change exactly the RED-hashed files; re-head refused\")\n",
      "why": "The RED proof's `files` map is the union of red and guard files, hashed for immutability, so comparing it to the test-author commit's diff refuses every certified build that declares a guard and leaves it with no way back onto a moved main -- exactly what happened to PR #134; caught by tests/factory/test_factory_rehead_guard_files.py (the measured PR #134 shape re-heads, and the kernel never compares the diff to the whole map) (D-072)."
    },
    {
      "id": "rehead-guard-hashes-unverified",
      "file": "factory_kernel/runtime.py",
      "find": "            if actual != expected:\n                raise NeedsHuman(\n                    f\"guard checkpoint file {rel!r} differs at the rebased test-author commit: \"\n",
      "replace": "            if False:\n                raise NeedsHuman(\n                    f\"guard checkpoint file {rel!r} differs at the rebased test-author commit: \"\n",
      "why": "A guard file's immutability was a side effect of the equality check the re-head used to do; once the diff is read as the red half alone, nothing but this hash keeps a rebase from carrying a rewritten kept test into the replayed RED, which is the property D-058 intended; caught by tests/factory/test_factory_rehead_guard_files.py (a guard whose hash moved is refused with both hashes) (D-072)."
    },
    {
      "id": "rehead-changed-guard-file-accepted",
      "file": "factory_kernel/runtime.py",
      "find": "        rewritten = sorted((set(guard_files) & changed_set) - set(red_files))\n",
      "replace": "        rewritten: list[str] = []\n",
      "why": "A guard pins an existing test the author must not rewrite (D-064); without this rule a test-author commit that quietly edits the kept test it guards is accepted at re-head, because the file is declared and so is never stray; caught by tests/factory/test_factory_rehead_guard_files.py (a commit that also changed the guard file is refused by name) (D-072)."
    },
    {
      "id": "evidence-replay-compares-the-whole-file-map",
      "file": "scripts/factory_evidence.py",
      "find": "    red_files, guard_files = checkpoint_file_sets(checkpoints)\n",
      "replace": "    red_files, guard_files = checkpoint_file_sets(checkpoints)\n    red_files = red_files | guard_files\n",
      "why": "The evidence bundle held the same conflation as the re-head, one stage later: replaying RED against the whole `files` map refuses any proof whose guard checkpoints pin a file the commit correctly left alone, so a re-headed guarded build would die at validation instead; caught by tests/factory/test_factory_rehead_guard_files.py (a commit changing only the red file is accepted, and replay_red asks the checkpoints) (D-072)."
    },
    {
      "id": "mutation-budget-warning-removed",
      "file": "harness/mutations/run.py",
      "find": "    if warning:\n        print(warning, flush=True)\n    print(f\"MUTATIONS_SECONDS={seconds:.1f}\", flush=True)\n",
      "replace": "    print(f\"MUTATIONS_SECONDS={seconds:.1f}\", flush=True)\n",
      "why": "The rung's only warning that it is running out of clock is the line it prints while it still passes; without it the next person meets the drift as `TIMEOUT after Ns` on the one run that mattered, which is how run 34066724127 ended; caught by tests/factory/test_factory_mutation_budget.py (a run near the budget warns while it still passes) (D-073)."
    },
    {
      "id": "mutation-budget-is-a-bare-literal-again",
      "file": "harness/ci.py",
      "find": "timeout=MUTATIONS_TIMEOUT)",
      "replace": "timeout=900)",
      "why": "The literal is the defect: 900 was never measured against the catalogue or the suites it re-runs, and nothing recorded where it came from; caught by tests/factory/test_factory_mutation_budget.py (the rung is not given a literal) (D-073)."
    },
    {
      "id": "mutation-budget-ignores-its-headroom",
      "file": "harness/budget.py",
      "find": "    generous = measured_seconds(record, scope) * float(record[\"headroom\"])\n",
      "replace": "    generous = measured_seconds(record, scope)\n",
      "why": "A budget equal to the worst observation is that observation with a rounding error: the first run one second slower than the slowest recorded one fails; caught by tests/factory/test_factory_mutation_budget.py (the budget covers the measurement with headroom) (D-073)."
    },
    {
      "id": "mutation-budget-takes-the-mean-not-the-worst",
      "file": "harness/budget.py",
      "find": "    return max(float(entry[\"total_seconds\"]) for entry in measurements(record, scope))\n",
      "replace": "    values = [float(entry[\"total_seconds\"]) for entry in measurements(record, scope)]\n    return sum(values) / len(values)\n",
      "why": "A rung has to finish on its worst run, not its average one; a mean silently sets the budget below half the recorded observations; caught by tests/factory/test_factory_mutation_budget.py (p100 and not the mean) (D-073)."
    },
    {
      "id": "factory-mutations-skip-defects-when-short-of-time",
      "file": "harness/factory_mutations/run.py",
      "find": "    if workers <= 1:\n        return [evaluate(defect, order) for defect in defects]\n",
      "replace": "    affordable = int(FAMILY_BUDGET_SECONDS // 12)\n    if len(defects) > affordable:\n        defects = defects[:affordable]\n    if workers <= 1:\n        return [evaluate(defect, order) for defect in defects]\n",
      "why": "The clock is a budget, not a filter: a catalogue trimmed to what the runner thinks it can afford reports FACTORY_MUTATIONS_TOTAL for a smaller catalogue, and every dropped defect counts as neither caught nor escaped; caught by tests/factory/test_factory_mutation_budget.py (the whole catalogue is evaluated however long it takes) (D-073)."
    },
    {
      "id": "factory-mutations-lose-an-escape-under-concurrency",
      "file": "harness/factory_mutations/run.py",
      "find": "        return list(pool.map(lambda defect: evaluate(defect, order), defects))\n",
      "replace": "        results = list(pool.map(lambda defect: evaluate(defect, order), defects))\n        return [result for result in results if result[\"outcome\"] != \"escaped\"]\n",
      "why": "Running the copies concurrently must not change the verdict: a pool that drops an escaped result reports caught == total and passes a run in which a trust-root bypass survived; caught by tests/factory/test_factory_mutation_budget.py (every defect is evaluated and reported in manifest order) (D-073)."
    },
    {
      "id": "factory-mutations-order-narrows-the-suite",
      "file": "harness/factory_mutations/run.py",
      "find": "    return tuple(sorted(TEST_FILES, key=lambda rel: (durations.get(rel, float(\"inf\")), rel)))\n",
      "replace": "    ordered = sorted(TEST_FILES, key=lambda rel: (durations.get(rel, float(\"inf\")), rel))\n    return tuple(ordered[:8])\n",
      "why": "Cheapest-first ordering is sound only because the order is a permutation of the whole suite; a prefix turns it into running the tests most likely to be quick instead of the suite, and a defect whose only detector is expensive escapes unnoticed; caught by tests/factory/test_factory_mutation_budget.py (the order is every file cheapest first) (D-073)."
    },
    {
      "id": "factory-mutations-run-fixtures-as-tests",
      "file": "harness/factory_mutations/run.py",
      "find": "TEST_FILES = tuple(\n    rel for rel in COPY_FILES\n    if rel.startswith(\"tests/\") and rel.endswith(\".py\") and Path(rel).name.startswith(\"test_\")\n)\n",
      "replace": "TEST_FILES = tuple(rel for rel in COPY_FILES if rel.startswith(\"tests/\"))\n",
      "why": "Selecting every copied path under tests/ hands the recorded JSON fixtures to `python <path>`; three of them are not valid Python, so the baseline is red and the whole family refuses with `focused baseline is red` on every platform; caught by tests/factory/test_factory_mutation_budget.py (only test modules are run as tests) (D-073)."
    },
    {
      "id": "factory-mutations-stop-before-a-green-suite-finishes",
      "file": "harness/factory_mutations/run.py",
      "find": "        if proc.returncode != 0:\n            return subprocess.CompletedProcess([], 1, \"\\n\".join(outputs), \"\"), durations\n",
      "replace": "        return subprocess.CompletedProcess(\n            [], proc.returncode, \"\\n\".join(outputs), \"\"), durations\n",
      "why": "Stopping at the first red is verdict-identical only because a green file does not stop the suite; returning after the first file whatever it said makes a green baseline out of a red one, and an escape out of every defect whose detector is not the first file; caught by tests/factory/test_factory_mutation_budget.py (a green suite still runs every file) (D-073)."
    },
    {
      "id": "mutation-timing-line-loses-its-outcome",
      "file": "harness/mutations/run.py",
      "find": "    return f\"MUTATION_TIMING id={defect_id} seconds={seconds:.1f} outcome={outcome}\"\n",
      "replace": "    return f\"MUTATION_TIMING id={defect_id} seconds={seconds:.1f}\"\n",
      "why": "A per-defect clock without its outcome cannot distinguish a defect that was expensive because it was caught late from one that was expensive because it escaped and paid for every channel; caught by tests/factory/test_factory_mutation_budget.py (the timing line format) (D-073)."
    },
    {
      "id": "mutations-ok-line-drops-its-clock",
      "file": "harness/mutations/run.py",
      "find": "            f\"MUTATIONS_OK defects={total} seconds={seconds:.1f} budget={RUNG_BUDGET_SECONDS}\",\n",
      "replace": "            \"MUTATIONS_OK\",\n",
      "why": "The total against the budget is the number that makes drift visible in the evidence log; without it a rung at 40% of its budget and one at 99% are the same green line; caught by tests/factory/test_factory_mutation_budget.py (the closing line reports the clock against the budget) (D-073)."
    },
    {
      "id": "ci-timeout-discards-what-the-rung-said",
      "file": "harness/ci.py",
      "find": "        detail = f\"TIMEOUT after {timeout}s\"\n        return 124, f\"{detail}\\n{partial[-4000:]}\" if partial else detail\n",
      "replace": "        return 124, f\"TIMEOUT after {timeout}s\"\n",
      "why": "A timeout that throws away the partial output is the failure nobody can diagnose: run 34066724127 reported `TIMEOUT after 900s` and nothing about which defect, or even which family, the clock had reached; caught by tests/factory/test_factory_mutation_budget.py (a timeout keeps what the rung managed to say) (D-073)."
    },
    {
      "id": "ci-slow-rung-warning-removed",
      "file": "harness/ci.py",
      "find": "    if seconds > timeout * SLOW_RUNG_FRACTION:\n",
      "replace": "    if False:\n",
      "why": "A rung that finishes just inside its timeout is the run before the one that fails, and looks identical to a fast one; the warning is the only notice anyone gets while the ladder is still green; caught by tests/factory/test_factory_mutation_budget.py (a rung close to its deadline says so) (D-073)."
    },
    {
      "id": "spine-factory-authority-keeps-its-literal",
      "file": "scripts/factory_evidence_spine.py",
      "find": "        timeout=ladder_budget.budget_seconds(ladder_budget.load(), scope=\"factory-family\"),\n",
      "replace": "        timeout=1200,\n",
      "why": "The spine re-observes the factory family independently, so it carried the same unmeasured deadline one authority further out: 1200s was never measured against a catalogue of 391 defects; caught by tests/factory/test_factory_mutation_budget.py (the spine bounds the factory family by the derived budget) (D-073)."
    },
    {
      "id": "worker-throughput-stage-borrows-the-ambient-repository",
      "file": "tests/factory/test_factory_worker_throughput.py",
      "find": "            repo = git_repo(Path(tmp))\n",
      "replace": "            repo = Path(tmp)\n",
      "why": "The timing/telemetry stage ends in `_refuse_literal_artifacts_dir`, which runs `git status` in the cwd it is handed; a directory that is not a repository is exactly the state the mutation family's copy is in, and that one error refused the whole family with `focused baseline is red` before any defect was evaluated; caught by tests/factory/test_factory_worker_throughput.py (the stage is given a repository the test created) (D-074)."
    },
    {
      "id": "suite-hermeticity-scan-stops-reading-agent-call-sites",
      "file": "tests/factory/test_factory_suite_hermetic.py",
      "find": "        if called in GIT_CWD_POSITION:\n",
      "replace": "        if False:\n",
      "why": "A hermeticity scan that no longer looks at the `_agent`/`_git` call sites reports nothing on a suite that has gone back to running git in its own checkout, which is the defect that made the factory mutation family unusable in the first place; caught by tests/factory/test_factory_suite_hermetic.py (the scan is exercised against sources it must flag) (D-074)."
    },
    {
      "id": "ladder-budget-inner-raised-past-its-wrapper",
      "file": "harness/budgets.json",
      "find": "      \"total_seconds\": 2962,\n",
      "replace": "      \"total_seconds\": 29620,\n",
      "why": "The exact shape of the defect that ended run 34081507222: an inner budget grows and the wrapper that contains it does not. The factory family's budget goes from 4500 s to 44460 s while the mutation rung that runs it keeps 8580 s, and the spine that re-observes it keeps 15540 s. harness/budget.py:validate() refuses the record, so every runner that loads it stops; caught by tests/factory/test_factory_ladder_budget.py (every wrapper bounds what it contains) (D-075)."
    },
    {
      "id": "ladder-budget-containment-check-made-vacuous",
      "file": "tests/factory/test_factory_ladder_budget.py",
      "find": "        for scope in budget.scope_names(record)\n    ]\n",
      "replace": "        for scope in budget.scope_names(record)[:1]\n    ]\n",
      "why": "A containment rule that stops examining scopes passes for the same reason an empty suite passes: narrowed to one scope it would still report green while the mutation rung outgrew the ladder. The walk is a function whose coverage is asserted separately, so narrowing it is caught by tests/factory/test_factory_ladder_budget.py (the containment walk covers every declared scope) (D-075)."
    },
    {
      "id": "spine-literal-timeout-reintroduced-for-the-evidence-bundle",
      "file": "scripts/factory_evidence_spine.py",
      "find": "        timeout=ladder_budget.budget_seconds(ladder_budget.load(), scope=\"evidence-bundle\"),\n",
      "replace": "        timeout=3000,\n",
      "why": "The wrapper that contains the whole ladder, back to a number nobody measured: 3000 s bounds a program that runs a 10020 s ladder, which is the defect one authority out from the 1800 s that killed run 34081507222 after every judge had passed. The detector reads the syntax tree, so writing 3001 instead is caught identically; caught by tests/factory/test_factory_ladder_budget.py (the ladder files carry no literal timeout) (D-075)."
    },
    {
      "id": "mutation-anchor-check-dropped-from-the-static-rung",
      "file": "harness/static.py",
      "find": "    (\"mutation-anchors\", ROOT, [sys.executable, str(HERE / \"mutation_anchors.py\")]),\n",
      "replace": "",
      "why": "The anchor check would exist and never run, so drift would again be invisible until the mutation rung fifty minutes into a validation."
    },
    {
      "id": "mutation-anchor-uniqueness-made-vacuous",
      "file": "harness/mutation_anchors.py",
      "find": "        if found != 1:\n",
      "replace": "        if False:\n",
      "why": "A factory defect whose anchor is missing or ambiguous would be reported as injectable, which is exactly the silence this check exists to end."
    },
    {
      "id": "mutation-anchor-copy-set-check-skipped",
      "file": "harness/mutation_anchors.py",
      "find": "    if rel in copy_files:\n        return True\n    return any(rel == d or rel.startswith(d + \"/\") for d in copy_dirs)\n",
      "replace": "    return True\n",
      "why": "A defect naming a file the runner never copies would pass the check while being impossible to inject."
    },
    {
      "id": "mutation-anchor-failures-reported-as-a-count",
      "file": "harness/mutation_anchors.py",
      "find": "        for line in failures:\n            print(line, flush=True)\n",
      "replace": "",
      "why": "A count with no names is what let twenty-three drifted defects accumulate across ten merges (D-076)."
    },
    {
      "id": "factory-mutation-failures-leave-their-members-unnamed",
      "file": "harness/factory_mutations/run.py",
      "find": "    for outcome, marker in ((\"escaped\", \"FACTORY_MUTATIONS_ESCAPED\"),\n                            (\"not_injected\", \"FACTORY_MUTATIONS_UNINJECTED\")):\n        ids = [result[\"id\"] for result in results if result[\"outcome\"] == outcome]\n        if ids:\n            print(f\"{marker}={','.join(ids)}\", flush=True)\n",
      "replace": "",
      "why": "A failing family would again reach the refusal artifact as a count, because every consumer of this output keeps only its tail."
    },
    {
      "id": "rehead-budget-assumes-a-head-it-was-not-given",
      "file": "factory_kernel/refusal.py",
      "find": "    if not head:\n        return False\n",
      "replace": "    if not head:\n        return True\n",
      "why": "A caller that cannot say what the head is would get a re-head anyway, so the budget's own condition would go unchecked rather than fail closed."
    },
    {
      "id": "currency-check-runs-after-the-judges",
      "file": "factory_kernel/runtime.py",
      "find": "            self._authority_cursor = stage_context = \"trust_root_currency\"\n            self._exec(\n",
      "replace": "            self._authority_cursor = stage_context = \"trust_root_currency\"\n            _ = lambda: self._exec(\n",
      "why": "A base move would again cost five blinded judges and $1.64 before a 0.4 s git diff refused it (run 34112301646)."
    },
    {
      "id": "currency-only-falls-through-to-the-bundle",
      "file": "scripts/factory_evidence.py",
      "find": "    if args.currency_only:\n        print(f\"EVIDENCE_CURRENCY_OK head={head} base={base} main={current_main}\")\n        return\n\n",
      "replace": "",
      "why": "The early check would go on to parse the attached blocks and run the whole ladder, so the cheap pre-check would cost what it was added to save."
    },
    {
      "id": "currency-refusal-recorded-as-the-evidence-bundle",
      "file": "factory_kernel/refusal.py",
      "find": "            return \"trust_root_currency\" if stage == \"trust_root_currency\" else \"evidence_spine\"\n",
      "replace": "            return \"evidence_spine\"\n",
      "why": "A non-stale failure of the pre-check would be recorded as a refusal by the Evidence Bundle, which never ran."
    },
    {
      "id": "stale-base-escalation-ignores-the-budget",
      "file": "factory_kernel/runtime.py",
      "find": "                if not rehead_budget_allows(self.github.pr_comments(pr_number), head=head):\n",
      "replace": "                if False:\n",
      "why": "A pull request that changed after its re-head and hit a stale base again would never be escalated, and the comment would promise a re-head the dispatch refuses."
    },
    {
      "id": "rehearsal-conflates-the-two-evidence-calls",
      "file": "harness/rehearsal.py",
      "find": "        if tool == \"factory_evidence.py\" and \"--currency-only\" in argv:\n            phase = \"currency\"\n",
      "replace": "",
      "why": "One program is called twice in a validate with different failure modes. Conflating them aims every evidence scenario at whichever call runs first, so the Evidence Bundle's own scenarios would silently stop exercising the bundle."
    },
    {
      "id": "mutation-detector-reference-check-made-vacuous",
      "file": "harness/mutation_anchors.py",
      "find": "            if named in tests:\n                continue\n",
      "replace": "            continue\n",
      "why": "A defect could again name a detector that never runs, which is how four re-head defects escaped from D-072 onward; caught by tests/factory/test_factory_mutation_anchors.py (a named detector outside the suite fails)."
    },
    {
      "id": "rehead-guard-detector-dropped-from-the-copy-set",
      "file": "harness/factory_mutations/run.py",
      "find": "    \"tests/factory/test_factory_rehead_guard_files.py\",\n",
      "replace": "",
      "why": "The detector four re-head defects name would stop running in the copies, and those defects would silently escape again; caught by tests/factory/test_factory_mutation_anchors.py (every detector the catalogue names is in the suite)."
    },
    {
      "id": "spine-refuses-the-currency-call",
      "file": "scripts/factory_evidence_spine.py",
      "find": "    parser.add_argument(\"--verdict\")\n",
      "replace": "    parser.add_argument(\"--verdict\", required=True)\n",
      "why": "The wrapper would refuse the early trust-root currency check the kernel routes to it, refusing a real pull request for a reason that has nothing to do with the pull request; caught by tests/factory/test_factory_spine_routing.py (the wrapper accepts every call the kernel makes)."
    },
    {
      "id": "spine-currency-forward-swallows-the-refusal",
      "file": "scripts/factory_evidence_spine.py",
      "find": "    if proc.returncode != 0:\n        raise SystemExit(proc.returncode)\n",
      "replace": "    if False:\n        raise SystemExit(proc.returncode)\n",
      "why": "A stale trust root would be waved through by the very check added to catch it, because the wrapper would exit zero on the forwarded program's refusal; caught by tests/factory/test_factory_spine_routing.py (the wrapper adds nothing)."
    },
    {
      "id": "spine-currency-forward-takes-validation-credentials",
      "file": "scripts/factory_evidence_spine.py",
      "find": "        env=scoped_environment(scope=\"github\"),\n        capture_output=True,\n        text=True,\n        encoding=\"utf-8\",\n        errors=\"replace\",\n        timeout=ladder_budget.budget_seconds(ladder_budget.load(), scope=\"trust-root-currency\"),\n",
      "replace": "        env=scoped_environment(scope=\"github+validation\"),\n        capture_output=True,\n        text=True,\n        encoding=\"utf-8\",\n        errors=\"replace\",\n        timeout=ladder_budget.budget_seconds(ladder_budget.load(), scope=\"trust-root-currency\"),\n",
      "why": "Three git diffs and one GitHub read would run holding the validation database credentials they have no use for; caught by tests/factory/test_factory_spine_routing.py (the forwarded call runs with no more credentials than it needs)."
    },
    {
      "id": "rehead-guard-asks-the-budget-without-a-head",
      "file": "factory_kernel/runtime.py",
      "find": "        if not rehead_eligible(comments, head=head):\n",
      "replace": "        if not rehead_eligible(comments):\n",
      "why": "The re-head's own guard would apply the pre-D-077 rule while the dispatcher applies the new one, so the dispatcher chooses a re-head this refuses and the pull request has no way forward; caught by tests/factory/test_factory_base_move.py (every asker passes a head)."
    },
    {
      "id": "dispatcher-asks-the-budget-without-a-head",
      "file": "factory_kernel/dispatch_plan.py",
      "find": "            if rehead_eligible(self.github.pr_comments(number), head=str(pr.get(\"headRefOid\") or \"\")):",
      "replace": "            if rehead_eligible(self.github.pr_comments(number)):",
      "why": "The dispatcher would never choose a second re-head, so a base that moved again under unchanged certified work would sit waiting for a human; caught by tests/factory/test_factory_base_move.py (every asker passes a head)."
    },
    {
      "id": "copies-cannot-read-the-catalogue",
      "file": "harness/factory_mutations/run.py",
      "find": "    \"harness/factory_mutations/defects.json\",\n",
      "replace": "",
      "why": "load_defects() requires all eight manifests, so a copy without them cannot load this runner at all: every test that asks it a question dies with `required factory mutation manifest missing`, the baseline goes red, and the whole family is REFUSED rather than run; caught by tests/factory/test_factory_mutation_anchors.py (the copy set carries every manifest the runner requires)."
    },
    {
      "id": "merge-job-persists-checkout-token",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "          ref: ${{ needs.dispatch.outputs.kernel_sha }}\n          fetch-depth: 0\n          persist-credentials: false",
      "replace": "          ref: ${{ needs.dispatch.outputs.kernel_sha }}\n          fetch-depth: 0\n          persist-credentials: true",
      "why": "A write-capable GitHub token could be stored in Git config where PR-controlled subprocesses can read it."
    },
    {
      "id": "merge-job-claude-version-floats",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      - name: Merge job - Install pinned worker and browser CLIs\n        run: |\n          npm install -g @anthropic-ai/claude-code@2.1.245",
      "replace": "      - name: Merge job - Install pinned worker and browser CLIs\n        run: |\n          npm install -g @anthropic-ai/claude-code",
      "why": "The reasoning-worker executable could drift between unattended runs without review or revalidation."
    },
    {
      "id": "merge-job-db-without-pgvector",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      postgres:\n        # Fresh post-merge validation database.\n        image: pgvector/pgvector:pg16\n",
      "replace": "      postgres:\n        # Fresh post-merge validation database.\n        image: postgres:16\n",
      "why": "The validation database would lack the vector extension, so every retrieval query would fail with `type \"vector\" does not exist` while ingestion succeeded, and the browser journey could never see a citation (D-053)."
    },
    {
      "id": "programme-confirmation-does-not-retry-missing-read",
      "file": "factory_kernel/programme_runtime.py",
      "find": "for delay in (0, 2, 5):",
      "replace": "for delay in (0,):",
      "why": "An acknowledged candidate that appears on a later inventory read must be confirmed without another POST."
    },
    {
      "id": "programme-confirmation-ignores-stop",
      "file": "factory_kernel/programme_runtime.py",
      "find": "                time.sleep(delay)\n            check_stop()\n",
      "replace": "                time.sleep(delay)\n",
      "why": "A stop arriving during confirmation wait must refuse the remaining reads."
    },
    {
      "id": "programme-confirmation-ignores-scope-replacement",
      "file": "factory_kernel/programme_runtime.py",
      "find": "            if latest is None or latest.sha256 != programme.sha256:\n                raise ProgrammeRefused(\"programme changed during candidate confirmation\")",
      "replace": "            if False:\n                raise ProgrammeRefused(\"programme changed during candidate confirmation\")",
      "why": "A retired or replaced scope cannot be confirmed while waiting for candidate visibility."
    },
    {
      "id": "programme-confirmation-ignores-post-identity",
      "file": "factory_kernel/programme_runtime.py",
      "find": "if confirmed[\"number\"] != number:",
      "replace": "if False:",
      "why": "Confirmation must bind the acknowledged POST number, not accept a different matching issue."
    },
    {
      "id": "continuation-counter-not-decremented",
      "file": "factory_kernel/continuation.py",
      "find": "f\"continuation_remaining={count - 1}\"",
      "replace": "f\"continuation_remaining={count}\"",
      "why": "A successful pulse must spend one of its remaining continuation slots."
    },
    {
      "id": "continuation-ignores-failed-merge",
      "file": "factory_kernel/continuation.py",
      "find": "if dispatch_result != \"success\" or merge_result not in {\"success\", \"skipped\"}:",
      "replace": "if dispatch_result != \"success\":",
      "why": "A failed or cancelled merge/post-merge authority cannot schedule another paid attempt."
    },
    {
      "id": "continuation-ignores-final-stop",
      "file": "factory_kernel/continuation.py",
      "find": "    check_stop()\n    queue.github.run([",
      "replace": "    queue.github.run([",
      "why": "Stop must be rechecked immediately before the continuation request."
    },
    {
      "id": "continuation-admits-replaced-programme",
      "file": "factory_kernel/programme_runtime.py",
      "find": "if expected_programme and (programme is None or programme.sha256 != expected_programme):",
      "replace": "if False and expected_programme and (programme is None or programme.sha256 != expected_programme):",
      "why": "A continued pulse cannot materialize scope from a different approved programme."
    },
    {
      "id": "publication-base-omitted",
      "file": "factory_kernel/runtime.py",
      "find": "env = self._run_env(paths, base_ref=value[\"base\"], base_sha=value[\"base\"])",
      "replace": "env = self._run_env(paths, base_ref=value[\"base\"])",
      "why": "A fresh publication process must preserve the captured branch base for the sole provenance publisher."
    },
    {
      "id": "publication-base-substituted",
      "file": "factory_kernel/runtime.py",
      "find": "env = self._run_env(paths, base_ref=value[\"base\"], base_sha=value[\"base\"])",
      "replace": "env = self._run_env(paths, base_ref=value[\"base\"], base_sha=value[\"kernel\"])",
      "why": "The kernel checkout identity is not the certified branch base."
    },
    {
      "id": "publication-malformed-base-accepted",
      "file": "factory_kernel/runtime.py",
      "find": "                or not isinstance(value.get(\"base\"), str)\n                or not re.fullmatch(r\"(?:[0-9a-f]{40}|[0-9a-f]{64})\", value[\"base\"])\n",
      "replace": "",
      "why": "An invalid prepared base must refuse before spending App authority on push or PR creation."
    },
    {
      "id": "resume-programme-any-bot",
      "file": "factory_kernel/runtime.py",
      "find": "or author.get(\"login\") != factory_login:",
      "replace": "or (not admission and author.get(\"login\") != factory_login):",
      "why": "Admission of the linked issue cannot authorize a PR opened by a different Bot."
    },
    {
      "id": "resume-programme-readmission-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "            if admission:\n                fresh = ProgrammeQueue",
      "replace": "            if False and admission:\n                fresh = ProgrammeQueue",
      "why": "Programme retirement during preparation must prevent publication."
    },
    {
      "id": "trajectory-source-main-ignored",
      "file": "factory_kernel/trajectory.py",
      "find": "run.get(\"head_branch\") != \"main\" or ",
      "replace": "",
      "why": "Candidate-branch observations cannot be misattributed to canonical main execution."
    },
    {
      "id": "trajectory-source-completion-ignored",
      "file": "factory_kernel/trajectory.py",
      "find": "or run.get(\"status\") != \"completed\"",
      "replace": "or False",
      "why": "An in-progress run does not have a final outcome to archive."
    },
    {
      "id": "trajectory-model-privacy-bypassed",
      "file": "factory_kernel/trajectory.py",
      "find": "\"model\": model if model in models else None",
      "replace": "\"model\": model",
      "why": "Arbitrary artifact strings must not escape through model metadata."
    },
    {
      "id": "trajectory-missing-cost-invented-as-zero",
      "file": "factory_kernel/trajectory.py",
      "find": "_number(row.get(\"cost_usd\"))",
      "replace": "_number(row.get(\"cost_usd\") or 0)",
      "why": "Unknown cost is missing evidence, not evidence of a free attempt."
    },
    {
      "id": "trajectory-existing-record-replaced",
      "file": "factory_kernel/trajectory_archive.py",
      "find": "if entries[name] != blob:",
      "replace": "if False:",
      "why": "An altered observation must not replace the immutable record for an existing attempt."
    },
    {
      "id": "trajectory-archive-force-update",
      "file": "factory_kernel/trajectory_archive.py",
      "find": "{\"sha\": new_commit, \"force\": False}",
      "replace": "{\"sha\": new_commit, \"force\": True}",
      "why": "A racing archive writer must not erase another completed append."
    },
    {
      "id": "trajectory-existing-tree-dropped",
      "file": "factory_kernel/trajectory_archive.py",
      "find": "payload[\"base_tree\"] = tree",
      "replace": "pass",
      "why": "New archive commits must preserve previously recorded trajectories."
    },
    {
      "id": "trajectory-app-age-exempted",
      "file": "factory_kernel/github_cli.py",
      "find": "\"archive_trajectory\",",
      "replace": "",
      "why": "Even a non-authoritative archive publication requires a fresh App capability."
    },
    {
      "id": "continuation-exempts-proof-job-errors",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "    timeout-minutes: 360\n    outputs:",
      "replace": "    timeout-minutes: 360\n    continue-on-error: true\n    outputs:",
      "why": "Only the optional scheduling job may ignore errors; a proof failure must fail its run."
    },
    {
      "id": "frontdoor-synthesis-owner-bypassed",
      "find": "if principal.role != \"owner\":",
      "file": "factory_kernel/frontdoor_synthesis.py",
      "replace": "if False:",
      "why": "A proposal principal could request paid programme synthesis."
    },
    {
      "id": "frontdoor-synthesis-approval-bypassed",
      "find": "if (type(command[\"approval_version\"]) is not int",
      "file": "factory_kernel/frontdoor_synthesis.py",
      "replace": "if False and (type(command[\"approval_version\"]) is not int",
      "why": "A wrong approval identity could spend API calls before eventual compilation refusal."
    },
    {
      "id": "frontdoor-synthesis-reservation-skipped",
      "find": "            record = {\"identity\": identity, \"state\": \"pending\", \"stages\": []}\n            self._save(path, record)",
      "file": "factory_kernel/frontdoor_synthesis.py",
      "replace": "            record = {\"identity\": identity, \"state\": \"pending\", \"stages\": []}",
      "why": "An API call could start before a durable reservation, allowing repeated uncertain spend."
    },
    {
      "id": "frontdoor-synthesis-freshness-bypassed",
      "find": "\"expected_project_version\": version, \"approval_version\": command[\"approval_version\"],",
      "file": "factory_kernel/frontdoor_synthesis.py",
      "replace": "\"expected_project_version\": self.store.snapshot(project, principal=principal)[\"project_version\"], \"approval_version\": command[\"approval_version\"],",
      "why": "A new owner intent during synthesis could be hidden by silently rebinding the old review to a newer project version."
    },
    {
      "id": "frontdoor-synthesis-tools-granted",
      "find": "\"programme-proposer\": JUDGE_TOOLS,",
      "file": "factory_kernel/worker_policy.py",
      "replace": "\"programme-proposer\": READ_TOOLS,",
      "why": "A programme proposer could inspect private host files beyond the committed facts provided."
    },
    {
      "id": "frontdoor-synthesis-budget-raised",
      "find": "\"programme-proposer\": 1.0,",
      "file": "factory_kernel/worker_policy.py",
      "replace": "\"programme-proposer\": 10.0,",
      "why": "Programme synthesis could exceed its approved per-call limit."
    },
    {
      "id": "frontdoor-http-stop-blocked-by-observation",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "class FrontDoorServer(ThreadingMixIn, WSGIServer):",
      "replace": "class FrontDoorServer(WSGIServer):",
      "why": "A slow observation must not prevent an authenticated stop request from being served; caught by tests/factory/test_frontdoor_http.py."
    },
    {
      "id": "frontdoor-http-owner-auth-bypassed",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "return hmac.compare_digest(supplied, self.token_hash)",
      "replace": "return True",
      "why": "A forged bearer cannot read intent, approve scope or request stop; caught by tests/factory/test_frontdoor_http.py."
    },
    {
      "id": "frontdoor-http-missing-origin-accepted",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "if method == \"POST\" and environ.get(\"HTTP_ORIGIN\") != self.origin:",
      "replace": "if False:",
      "why": "Owner commands require an explicit same-origin request; caught by tests/factory/test_frontdoor_http.py."
    },
    {
      "id": "frontdoor-http-stop-dispatches-worker",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "\"workflow\", \"run\", \"dark-factory-owner-stop.yml\"",
      "replace": "\"workflow\", \"run\", \"dark-factory-worker.yml\"",
      "why": "A stop command must never dispatch product execution; caught by tests/factory/test_frontdoor_http.py."
    },
    {
      "id": "frontdoor-http-read-failure-claims-clear",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "result.update(stop=None, stop_observed_at=None)",
      "replace": "result.update(stop={\"state\": \"clear\", \"issues\": []}, stop_observed_at=None)",
      "why": "Missing remote evidence must remain unknown rather than claiming the stop is clear; caught by tests/factory/test_frontdoor_http.py."
    },
    {
      "id": "frontdoor-stop-created-without-stop-label",
      "file": "factory_kernel/frontdoor_control.py",
      "find": "\"labels\": [\"factory:stop\"]",
      "replace": "\"labels\": []",
      "why": "A stop issue must carry the existing remote stop label in its initial atomic POST; caught by tests/factory/test_frontdoor_control.py."
    },
    {
      "id": "frontdoor-stop-ignores-owner",
      "file": "factory_kernel/frontdoor_control.py",
      "find": "os.environ.get(\"GITHUB_ACTOR\") != owner",
      "replace": "False",
      "why": "Only the owner's protected-main workflow invocation may request this control effect; caught by tests/factory/test_frontdoor_control.py."
    },
    {
      "id": "frontdoor-stop-ignores-changed-replay",
      "file": "factory_kernel/frontdoor_control.py",
      "find": "row.get(\"body\") != body",
      "replace": "False",
      "why": "A repeated stop request ID cannot silently bind different wording; caught by tests/factory/test_frontdoor_control.py."
    },
    {
      "id": "frontdoor-stop-workflow-queues-behind-worker",
      "file": ".github/workflows/dark-factory-owner-stop.yml",
      "find": "group: dark-factory-owner-stop",
      "replace": "group: dark-factory-worker",
      "why": "Emergency stop must be able to reach the existing authority while a long worker proof is running; caught by tests/factory/test_frontdoor_control.py."
    },
    {
      "id": "frontdoor-review-worker-prepares-activation",
      "file": "factory_kernel/frontdoor_programme.py",
      "find": "if principal.role != \"owner\":",
      "replace": "if False:",
      "why": "An untrusted proposal worker cannot prepare an owner-authorized activation review; caught by tests/factory/test_frontdoor_programme.py."
    },
    {
      "id": "frontdoor-review-stale-project-accepted",
      "file": "factory_kernel/frontdoor_programme.py",
      "find": "request[\"expected_project_version\"] != state[\"project_version\"]",
      "replace": "False",
      "why": "A review based on stale intent state must refuse; caught by tests/factory/test_frontdoor_programme.py."
    },
    {
      "id": "frontdoor-review-wrong-approval-hash-accepted",
      "file": "factory_kernel/frontdoor_programme.py",
      "find": "request[\"spec_sha256\"] != approval[\"spec_sha256\"]",
      "replace": "False",
      "why": "An owner review must name the exact approved scope hash; caught by tests/factory/test_frontdoor_programme.py."
    },
    {
      "id": "frontdoor-review-uses-unapproved-draft",
      "file": "factory_kernel/frontdoor_programme.py",
      "find": "\"spec\": deepcopy(approval[\"spec\"])",
      "replace": "\"spec\": deepcopy(state[\"draft\"][\"spec\"])",
      "why": "Using an unapproved draft blocks a valid review against the approved hash even though the compiler still prevents scope escalation; caught by tests/factory/test_frontdoor_programme.py."
    },
    {
      "id": "frontdoor-worker-approves-scope",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "if principal.role != \"owner\" and operation != \"propose-spec\":",
      "replace": "if False:",
      "why": "Proposal workers must not approve their own scope; caught by tests/factory/test_frontdoor_intent.py."
    },
    {
      "id": "frontdoor-stale-command-overwrites-intent",
      "file": "factory_kernel/project_events.py",
      "find": "            if expected_version != len(events):",
      "replace": "            if False:",
      "why": "A stale browser or worker cannot overwrite newer intent; the check lives in the canonical append primitive since WP01. Detector: tests/factory/test_frontdoor_intent.py"
    },
    {
      "id": "frontdoor-approval-ignores-spec-hash",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "or payload[\"spec_sha256\"] != draft[\"spec_sha256\"]",
      "replace": "",
      "why": "Explicit approval must bind the reviewed specification bytes; caught by tests/factory/test_frontdoor_intent.py."
    },
    {
      "id": "frontdoor-approves-unresolved-product-question",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "if draft[\"open_questions\"]:",
      "replace": "if False:",
      "why": "Blocking product questions prevent approval while technical uncertainty may remain deferred; caught by tests/factory/test_frontdoor_intent.py."
    },
    {
      "id": "frontdoor-reuses-command-key-for-different-content",
      "file": "factory_kernel/project_events.py",
      "find": "                if (old[\"operation\"] != operation or event[\"actor\"] != actor\n                        or not spec.replay_matches(old[\"payload\"], detached)):",
      "replace": "                if False:",
      "why": "Retry identity binds the original command and authenticated actor; the check lives in the canonical append primitive since WP01. Detector: tests/factory/test_frontdoor_intent.py"
    },
    {
      "id": "frontdoor-ignores-history-chain",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "event[\"previous\"] != previous",
      "replace": "False",
      "why": "A changed historical event must refuse replay instead of silently changing original wording; caught by tests/factory/test_frontdoor_intent.py."
    },
    {
      "id": "frontdoor-new-intent-keeps-obsolete-draft",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "                if operation == \"record-intent\":\n                    draft = None",
      "replace": "",
      "why": "New user intent requires a fresh draft before approval; caught by tests/factory/test_frontdoor_intent.py."
    },
    {
      "id": "frontdoor-prepare-owner-bypassed",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "if principal.role != \"owner\":",
      "replace": "if False:",
      "why": "A proposal principal must not spend the owner's intake API budget."
    },
    {
      "id": "frontdoor-prepare-audit-binding-ignored",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "if value[\"draft_sha256\"] != sha256_value(draft) or value[\"decision\"] not in",
      "replace": "if value[\"decision\"] not in",
      "why": "An audit of a different draft cannot mark this scope ready for owner review."
    },
    {
      "id": "frontdoor-prepare-readiness-ignored",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "if value[\"decision\"] == \"ready\" and check[\"status\"] != \"met\":",
      "replace": "if False:",
      "why": "Unresolved product readiness cannot be hidden behind a ready decision."
    },
    {
      "id": "frontdoor-prepare-tools-granted",
      "file": "factory_kernel/worker_policy.py",
      "find": "\"intent-proposer\": JUDGE_TOOLS,",
      "replace": "\"intent-proposer\": READ_TOOLS,",
      "why": "Intake model processes receive only supplied facts, never tools for private host state."
    },
    {
      "id": "frontdoor-prepare-reservation-skipped",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "self._save(path, record)  # Durable before the first API call; crashes do not repeat it.",
      "replace": "pass  # Missing durable reservation.",
      "why": "An interrupted request must be recorded before any API spend, preventing silent repeats."
    },
    {
      "id": "frontdoor-prepare-budget-raised",
      "file": "factory_kernel/worker_policy.py",
      "find": "\"intent-proposer\": 1.0,",
      "replace": "\"intent-proposer\": 10.0,",
      "why": "One preparation stage may not silently exceed its bounded API budget."
    },
    {
      "id": "frontdoor-hosted-reservation-skipped",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "self.records._save(path, record)  # No POST before this durable reservation.",
      "replace": "pass  # Missing durable dispatch reservation.",
      "why": "A remote paid call must be durably identified before its one-shot POST."
    },
    {
      "id": "frontdoor-hosted-owner-ignored",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "or run.get(\"actor\", {}).get(\"login\") != owner",
      "replace": "or False",
      "why": "A successful run from another actor cannot supply this owner's result."
    },
    {
      "id": "frontdoor-hosted-result-hash-ignored",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "result[\"request_sha256\"] != sha256_value(payload)",
      "replace": "False",
      "why": "Encrypted results still have to name the exact private request."
    },
    {
      "id": "frontdoor-hosted-rerun-accepted",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "or run.get(\"run_attempt\") != 1",
      "replace": "or False",
      "why": "Rerunning a paid preparation is not an authorized retry of its recorded attempt."
    },
    {
      "id": "frontdoor-hosted-duplicate-accepted",
      "file": "factory_kernel/frontdoor_hosted_worker.py",
      "find": "if len(matches) != 1 or matches[0].get(\"id\") != run_id or matches[0].get(\"run_attempt\") != 1:",
      "replace": "if not matches:",
      "why": "Concurrent or repeated dispatches with the same identity must not repeat API spending."
    },
    {
      "id": "frontdoor-hosted-expiry-ignored",
      "file": "factory_kernel/frontdoor_hosted_worker.py",
      "find": "if not 0 <= now - payload[\"issued_at\"] <= 900:",
      "replace": "if False:",
      "why": "An old encrypted request cannot be replayed outside its bounded observation window."
    },
    {
      "id": "frontdoor-hosted-payload-role-widened",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "or payload[\"role\"] not in (PREFLIGHT_ROLES if exploration else INTAKE_ROLES)",
      "replace": "or False",
      "why": "The encrypted proposal bridge must never grant implementation or qualification capabilities."
    },
    {
      "id": "frontdoor-hosted-secret-step-main-guard-removed",
      "file": ".github/workflows/dark-factory-frontdoor-prepare.yml",
      "find": "github.ref == 'refs/heads/main' && github.run_attempt == 1",
      "replace": "github.run_attempt == 1",
      "why": "The workflow must reject candidate branches before any secret-bearing step starts."
    },
    {
      "id": "programme-early-validation-admission-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "        # Admission is an early refusal, never a substitute for the fresh merge check.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))\n        self.check_stop()",
      "replace": "        # Admission is an early refusal, never a substitute for the fresh merge check.\n        self.check_stop()",
      "why": "Retired or edited programme work would start repository and proof work before admission."
    },
    {
      "id": "programme-early-validation-stop-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "        # Admission is an early refusal, never a substitute for the fresh merge check.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))\n        self.check_stop()",
      "replace": "        # Admission is an early refusal, never a substitute for the fresh merge check.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))",
      "why": "A stop arriving during remote admission would be missed before repository and proof work."
    },
    {
      "id": "programme-early-rehead-admission-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "        # Reheading also spends proof/conformance work; retired scope must refuse first.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))\n        self.check_stop()",
      "replace": "        # Reheading also spends proof/conformance work; retired scope must refuse first.\n        self.check_stop()",
      "why": "Retired or edited programme work would start repository and proof work before admission."
    },
    {
      "id": "programme-early-rehead-stop-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "        # Reheading also spends proof/conformance work; retired scope must refuse first.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))\n        self.check_stop()",
      "replace": "        # Reheading also spends proof/conformance work; retired scope must refuse first.\n        if linked_issue is not None:\n            ProgrammeQueue(self.github, self.config.default_branch).admit(self.github.issue(linked_issue))",
      "why": "A stop arriving during remote admission would be missed before repository and proof work."
    },
    {
      "id": "frontdoor-recovery-parent-binding-ignored",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "sha256_value(parent) != command[\"failed_preparation_sha256\"]",
      "replace": "False",
      "why": "A recovery must bind the exact retained failed proposal."
    },
    {
      "id": "frontdoor-recovery-uncertain-call-accepted",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "record.get(\"failure\") != \"ProgrammeRefused\"",
      "replace": "False",
      "why": "An uncertain timeout cannot authorize another paid attempt."
    },
    {
      "id": "frontdoor-recovery-pending-call-accepted",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "record.get(\"state\") != \"failed\"",
      "replace": "False",
      "why": "A running or interrupted preparation cannot be retried."
    },
    {
      "id": "frontdoor-recovery-audited-call-accepted",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "or \"audit_output\" in record",
      "replace": "or False",
      "why": "Recovery is limited to a completed proposal compiler refusal, before any audit."
    },
    {
      "id": "frontdoor-recovery-result-hidden",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "return self._record(path) if path.exists() else original",
      "replace": "return original",
      "why": "The owner must see the durable replacement result after recovery."
    },
    {
      "id": "control-issue-edited-binding-forgiven",
      "file": "factory_kernel/control_issue.py",
      "find": "and issue[\"lastEditedAt\"] is None",
      "replace": "and True",
      "why": "An edited programme issue must not be disguised as an original stop record."
    },
    {
      "id": "control-issue-missing-edit-provenance-forgiven",
      "file": "factory_kernel/control_issue.py",
      "find": "\"lastEditedAt\" in issue and issue[\"lastEditedAt\"] is None",
      "replace": "issue.get(\"lastEditedAt\") is None",
      "why": "Missing platform edit metadata is not evidence of an original issue body."
    },
    {
      "id": "control-issue-body-race-forgiven",
      "file": "factory_kernel/control_issue.py",
      "find": "and issue.get(\"body\") == body",
      "replace": "and True",
      "why": "The original-body observation must bind the same issue snapshot."
    },
    {
      "id": "control-issue-human-author-forgiven",
      "file": "factory_kernel/control_issue.py",
      "find": "and issue.get(\"author\") == {\"login\": app_login.removesuffix(\"[bot]\"), \"__typename\": \"Bot\"}",
      "replace": "and True",
      "why": "A human copy of a visible stop marker is not App control provenance."
    },
    {
      "id": "control-issue-marker-only-bypass",
      "file": "factory_kernel/programme_runtime.py",
      "find": "if original_owner_stop(self.github, row, programme.app_login):",
      "replace": "if True:",
      "why": "Removing a work item's programme marker must still stop the queue."
    },
    {
      "id": "failure-activity-first-write-lost",
      "file": "factory_kernel/providers.py",
      "find": "\"first_write_turn\": run.wrote_at_turn,",
      "replace": "\"first_write_turn\": None,",
      "why": "A failed worker that attempted a draft must be distinguishable from one with no observed write."
    },
    {
      "id": "failure-activity-read-paths-unbounded",
      "file": "factory_kernel/providers.py",
      "find": "\"files_read\": list(run.files_read[:FILES_READ_CAP]),\n                \"first_write_turn\"",
      "replace": "\"files_read\": list(run.files_read),\n                \"first_write_turn\"",
      "why": "Failure diagnostics must remain bounded even when a worker repeatedly reads files."
    },
    {
      "id": "publication-request-owner-bypassed",
      "file": "factory_kernel/publication_request.py",
      "find": "if principal.role != \"owner\":",
      "replace": "if False:",
      "why": "A proposal worker cannot reserve or query an owner publication request."
    },
    {
      "id": "publication-request-destination-bypassed",
      "file": "factory_kernel/publication_request.py",
      "find": "if destination != {",
      "replace": "if False and destination != {",
      "why": "Approval must name the exact public payload and destination visibility."
    },
    {
      "id": "publication-request-duplicate-version",
      "file": "factory_kernel/publication_request.py",
      "find": "previous[\"project_version\"] == review[\"project_version\"]",
      "replace": "False",
      "why": "A new request ID must not repeat a possibly uncertain prior effect for the same version."
    },
    {
      "id": "publication-request-stale-owner-decisions",
      "file": "factory_kernel/publication_request.py",
      "find": "if (len(events) != record[\"project_version\"] or not events\n                    or sha256_value(events[-1]) != record[\"intent_head_sha256\"]):",
      "replace": "if False:",
      "why": "New owner decisions must invalidate an exported publication request before an effect."
    },
    {
      "id": "publication-request-expiry-bypassed",
      "file": "factory_kernel/publication_request.py",
      "find": "if not datetime.fromisoformat(record[\"created_at\"]) <= now < datetime.fromisoformat(record[\"expires_at\"]):",
      "replace": "if False:",
      "why": "An expired request cannot become a lasting publication capability."
    },
    {
      "id": "publication-request-existing-programme-restarted",
      "file": "factory_kernel/publication_request.py",
      "find": "if record[\"state\"] != \"reserved\":",
      "replace": "if False:",
      "why": "Already active scope and governed replacements cannot enter the initial publication path."
    },
    {
      "id": "publication-request-changed-main-forgiven",
      "file": "factory_kernel/publication_request.py",
      "find": "if observation != record[\"observation\"]:",
      "replace": "if False:",
      "why": "Fresh main, active programme and visibility must match the owner's reserved source."
    },
    {
      "id": "publication-source-main-race-forgiven",
      "file": "factory_kernel/publication_source.py",
      "find": "or latest_branch[\"commit\"][\"sha\"] != branch[\"commit\"][\"sha\"]",
      "replace": "or False",
      "why": "A programme read spanning different main revisions cannot establish publication prerequisites."
    },
    {
      "id": "publication-currency-mac-bypassed",
      "file": "factory_kernel/publication_currency.py",
      "find": "if not hmac.compare_digest(mac, expected):",
      "replace": "if False:",
      "why": "An altered or foreign message cannot become authenticated owner-request currency."
    },
    {
      "id": "publication-currency-old-challenge-accepted",
      "file": "factory_kernel/publication_currency.py",
      "find": "or not -5 <= self.clock() - challenge[\"issued_at\"] <= MAX_AGE_SECONDS",
      "replace": "or False",
      "why": "An old authenticated request must not generate a fresh publication response."
    },
    {
      "id": "publication-currency-wrong-nonce-accepted",
      "file": "factory_kernel/publication_currency.py",
      "find": "result[\"challenge\"] != challenge",
      "replace": "False",
      "why": "A valid response to another nonce or effect phase cannot be replayed."
    },
    {
      "id": "publication-currency-wrong-source-accepted",
      "file": "factory_kernel/publication_currency.py",
      "find": "if (current[\"request_sha256\"] != challenge[\"request_sha256\"]\n                or current[\"main_sha\"] != challenge[\"main_sha\"]):",
      "replace": "if False:",
      "why": "The host must authenticate the exact reserved request and workflow source revision."
    },
    {
      "id": "publication-currency-expired-consumption",
      "file": "factory_kernel/publication_currency.py",
      "find": "if expires.tzinfo is None or expires <= datetime.fromtimestamp(self.clock(), timezone.utc):",
      "replace": "if False:",
      "why": "A request expiring in transit must be refused before the publisher consumes the response."
    },
    {
      "id": "publication-currency-wrong-owner-decision",
      "file": "factory_kernel/publication_currency.py",
      "find": "or current[\"decision\"] != \"current-owner-request\"",
      "replace": "or False",
      "why": "Authentication of a message is not a replacement for its required owner-decision semantics."
    },
    {
      "id": "thinking-cap-unexercised-demand-accepted",
      "file": "scripts/factory_thinking_cap_probe.py",
      "find": "not within_cap(uncapped, cap) and within_cap(thinking, cap)",
      "replace": "within_cap(thinking, cap)",
      "why": "The actual 352/200-token measurement cannot establish a 1024-token cap: the uncapped sample must exercise its boundary."
    },
    {
      "id": "publication-currency-slow-observation-accepted",
      "file": "factory_kernel/publication_currency.py",
      "find": "        self._challenge(challenge)  # remote reads must not silently outlive the challenge\n",
      "replace": "",
      "why": "Slow remote reads must not stretch the freshness window after authentication."
    },
    {
      "id": "publication-admission-base-replacement",
      "file": "factory_kernel/publication_admission.py",
      "find": "or base_input is not None or changed_files != [ACTIVE_PATH]",
      "replace": "or False or changed_files != [ACTIVE_PATH]",
      "why": "Existing active scope cannot enter the initial publication lane."
    },
    {
      "id": "publication-admission-payload-substitution",
      "file": "factory_kernel/publication_admission.py",
      "find": "or head_input != manifest[\"input\"]",
      "replace": "or False",
      "why": "The PR must contain exactly the validated owner payload."
    },
    {
      "id": "publication-admission-commit-parent",
      "file": "factory_kernel/publication_admission.py",
      "find": "or commit.get(\"parents\") != [{\"sha\": base_sha}]",
      "replace": "or False",
      "why": "A publication cannot inherit an unapproved parent."
    },
    {
      "id": "publication-admission-source-attempt",
      "file": "factory_kernel/publication_admission.py",
      "find": "or type(run.get(\"run_attempt\")) is not int or run[\"run_attempt\"] != 1",
      "replace": "or False",
      "why": "A repeated workflow attempt cannot replay the publication authority."
    },
    {
      "id": "publication-admission-validation-job",
      "file": "factory_kernel/publication_admission.py",
      "find": "or matches[0].get(\"conclusion\") != \"success\"",
      "replace": "or False",
      "why": "A failed validation job cannot supply publication provenance."
    },
    {
      "id": "publication-admission-artifact-digest",
      "file": "factory_kernel/publication_observation.py",
      "find": "or artifact.get(\"digest\") != \"sha256:\" + hashlib.sha256(raw).hexdigest()",
      "replace": "or False",
      "why": "Downloaded bytes must match the independently observed platform digest."
    },
    {
      "id": "publication-admission-inventory-complete",
      "file": "factory_kernel/publication_observation.py",
      "find": "or len(payload[key]) != payload[\"total_count\"]",
      "replace": "or False",
      "why": "A truncated provenance inventory must not hide extra artifacts or jobs."
    },
    {
      "id": "publication-admission-currency-payload",
      "file": "factory_kernel/publication_client.py",
      "find": "if (current[\"input_sha256\"] != manifest[\"input_sha256\"]\n            or current[\"programme_sha256\"] != manifest[\"programme_sha256\"]):",
      "replace": "if False:",
      "why": "An authenticated response for another public payload is not current consent."
    },
    {
      "id": "publication-admission-guard-current",
      "file": "scripts/factory_security.py",
      "find": "if mode == \"trusted-base\" and observation.get(\"owner_currency\") != \"observed-current\":",
      "replace": "if False:",
      "why": "The old-base guard must reject head-only provenance without current owner consent."
    },
    {
      "id": "publication-admission-guard-secret-preserved",
      "file": "scripts/factory_security.py",
      "find": "if not (row[\"kind\"] == \"protected_path\" and row[\"path\"] == ACTIVE_PATH)",
      "replace": "if False",
      "why": "Publication never erases unrelated secret, path or ratchet findings."
    },
    {
      "id": "publication-effect-stop-bypassed",
      "file": "factory_kernel/publication_effects.py",
      "find": "or observation[\"stop\"] != {\"state\": \"clear\", \"issues\": []}",
      "replace": "or False",
      "why": "A stop between branch and commit must prevent the next spend."
    },
    {
      "id": "publication-effect-currency-bypassed",
      "file": "factory_kernel/publication_effects.py",
      "find": "if any(result.get(key) != value for key, value in expected.items()):",
      "replace": "if False:",
      "why": "Fresh authentication for another payload cannot authorize a write."
    },
    {
      "id": "publication-effect-journal-too-late",
      "file": "factory_kernel/publication_effects.py",
      "find": "        self._save()  # A failed durable write means no POST.\n",
      "replace": "",
      "why": "Every effect must have durable pending evidence before POST."
    },
    {
      "id": "publication-effect-uncertain-replayed",
      "file": "factory_kernel/publication_effects.py",
      "find": "if any(row[\"phase\"] == phase or row[\"state\"] != \"observed\" for row in self.record[\"effects\"]):",
      "replace": "if False:",
      "why": "An ambiguous POST cannot be repeated after a lost response."
    },
    {
      "id": "publication-effect-required-authority-omitted",
      "file": "factory_kernel/publication_effects.py",
      "find": "or not {\"quick-authority\", \"trust-root-authority\"}.issubset({row.get(\"name\") for row in checks})",
      "replace": "or False",
      "why": "Passing unrelated checks cannot stand in for both required authorities."
    },
    {
      "id": "publication-effect-merge-tree-unchecked",
      "file": "factory_kernel/publication_effects.py",
      "find": "or actual.get(\"tree\", {}).get(\"sha\") != expected.get(\"tree\", {}).get(\"sha\")",
      "replace": "or False",
      "why": "A different merged tree cannot receive a publication receipt."
    },
    {
      "id": "publication-effect-merge-parent-unchecked",
      "file": "factory_kernel/publication_effects.py",
      "find": "or [row.get(\"sha\") for row in actual.get(\"parents\", [])] != [self.manifest[\"source_sha\"]]",
      "replace": "or False",
      "why": "The merged parent must be the exact approved protected source."
    },
    {
      "id": "publication-effect-replay-allowed",
      "file": "factory_kernel/publication_worker.py",
      "find": "if len(matches) != 1 or matches[0].get(\"id\") != run_id or matches[0].get(\"run_attempt\") != 1:",
      "replace": "if False:",
      "why": "A second dispatch or rerun cannot reacquire authority."
    },
    {
      "id": "publication-effect-public-secret-unchecked",
      "file": "factory_kernel/publication_worker.py",
      "find": "if secret_findings([(\"approved-programme\", canonical_bytes(manifest).decode(\"utf-8\"))]):",
      "replace": "if False:",
      "why": "Approved scope containing a high-confidence secret must not become a public artifact."
    },
    {
      "id": "publication-effect-uncertain-merge-uncontained",
      "file": "factory_kernel/publication_worker.py",
      "find": "if phase == \"merge\" and publisher is not None and publisher.journal.record[\"effects\"]:",
      "replace": "if False:",
      "why": "An uncertain or unverified merge must request containment once."
    },
    {
      "id": "publication-dispatch-dispatch-replayed",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "path = self.directory / f\"{project}-{record['project_version']}.json\"",
      "replace": "path = self.directory / f\"{project}-{record['project_version'] + len(list(self.directory.glob('*.json')))}.json\"",
      "why": "Reloading a dispatch must preserve the single POST reservation."
    },
    {
      "id": "publication-dispatch-pending-not-durable",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "self._save(path, dispatch)  # Durable reservation before POST, separate from immutable consent.",
      "replace": "self._save(path, {**dispatch, \"state\": \"dispatch-submitted\"})  # Incorrectly claims success before POST.",
      "why": "The durable journal must report pending while the POST outcome is unknown."
    },
    {
      "id": "publication-dispatch-owner-change-ignored",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "if len(events) != record[\"project_version\"] or sha256_value(events[-1]) != record[\"intent_head_sha256\"]:",
      "replace": "if False:",
      "why": "Owner decisions changed during encryption must prevent dispatch."
    },
    {
      "id": "publication-dispatch-source-unchecked",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "or run.get(\"head_sha\") != record[\"source_sha\"]",
      "replace": "or False",
      "why": "Workflow success from another source cannot become current publication evidence."
    },
    {
      "id": "publication-dispatch-app-on-host",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "self.github.run([\"workflow\", \"run\", policy.WORKFLOW",
      "replace": "self.github.run_as_app([\"workflow\", \"run\", policy.WORKFLOW",
      "why": "The host cannot use App authority for an owner dispatch."
    },
    {
      "id": "publication-dispatch-foreign-ref",
      "file": "factory_kernel/publication_dispatch.py",
      "find": "\"-R\", policy.REPOSITORY, \"--ref\", \"main\",",
      "replace": "\"-R\", policy.REPOSITORY, \"--ref\", \"untrusted\",",
      "why": "Only protected main can receive the owner publication dispatch."
    },
    {
      "id": "programme-replan-scope-laundered",
      "file": "factory_kernel/programme_replan.py",
      "find": "if current.spec != proposed.spec:",
      "replace": "if False:",
      "why": "A valid new scope hash cannot turn changed owner intent into same-scope replanning."
    },
    {
      "id": "programme-replan-identity-changed",
      "file": "factory_kernel/programme_replan.py",
      "find": "if current.app_login != proposed.app_login:",
      "replace": "if False:",
      "why": "A replan cannot replace the execution identity."
    },
    {
      "id": "programme-replan-unbound-source",
      "file": "factory_kernel/programme_replan.py",
      "find": "if not isinstance(source_sha, str) or not re.fullmatch(r\"[a-f0-9]{40}\", source_sha):",
      "replace": "if False:",
      "why": "A mutable branch name cannot bind a review to its observed protected source."
    },
    {
      "id": "programme-replan-dependency-change-hidden",
      "file": "factory_kernel/programme_replan.py",
      "find": "if old[key] != new[key])",
      "replace": "if old[key][\"acceptance\"] != new[key][\"acceptance\"])",
      "why": "A stable item name and acceptance list must not conceal changed dependencies."
    },
    {
      "id": "programme-replan-unsupported-version",
      "file": "factory_kernel/programme_replan.py",
      "find": "if any(not isinstance(value, dict) or value.get(\"version\") not in (\"1.0\", \"1.1\")\n           for value in (current_input, proposed_input)):",
      "replace": "if False:",
      "why": "Future programme versions require explicit comparison support independently of compiler support."
    },
    {
      "id": "programme-replan-strategy-hidden",
      "file": "factory_kernel/programme_replan.py",
      "find": "strategy_changed = current.strategy != proposed.strategy",
      "replace": "strategy_changed = False",
      "why": "Advice changes must remain visible with unchanged decomposition."
    },
    {
      "id": "programme-replan-strategy-identity-incomplete",
      "file": "factory_kernel/programme_replan.py",
      "find": "\"sha256\": sha256_value(strategy),",
      "replace": "\"sha256\": sha256_value(strategy[\"candidate\"]),",
      "why": "Assumptions, uncertainty and source references are part of strategy review identity."
    },
    {
      "id": "preparation-context-after-proposer",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "            draft = validate_draft(draft, self.store.repository)\n            self._check_context(source[\"repository_context\"])",
      "replace": "            draft = validate_draft(draft, self.store.repository)",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "preparation-context-after-auditor",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "            audit = validate_audit(audit, draft)\n            self._check_context(source[\"repository_context\"])",
      "replace": "            audit = validate_audit(audit, draft)",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "preparation-context-after-synthesis",
      "file": "factory_kernel/frontdoor_synthesis.py",
      "find": "            self._check_context(source[\"repository_context\"])",
      "replace": "            pass",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "preparation-context-content-binding",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "if sha256_value(self.context()) != sha256_value(original):",
      "replace": "if self.context()[\"commit\"] != original[\"commit\"]:",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "preparation-context-total-bound",
      "file": "factory_kernel/frontdoor_prepare.py",
      "find": "if sum(len(value.encode(\"utf-8\")) for value in files.values()) > 100000:",
      "replace": "if False:",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "preparation-context-hosted-wiring",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "context = lambda: protected_repository_context(github)",
      "replace": "context = lambda: {\"commit\": \"0\" * 40, \"files\": {}}",
      "why": "Current protected facts must bind hosted preparation without stale output or repeated spend. Detector: tests/factory/test_frontdoor_repository.py"
    },
    {
      "id": "reconsideration-historical-handoff",
      "file": "factory_kernel/reconsideration.py",
      "find": "if candidate.sha256 == programme.sha256:",
      "replace": "if True:",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-preview-digest",
      "file": "factory_kernel/reconsideration.py",
      "find": "if request[\"observation_sha256\"] != preview[\"observation_sha256\"]:",
      "replace": "if False:",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-causal-ancestry",
      "file": "factory_kernel/reconsideration.py",
      "find": "if not set(claims) <= set(observation[\"causal_claim_ids\"]):",
      "replace": "if False:",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-refusal-head",
      "file": "factory_kernel/reconsideration.py",
      "find": "or raw[\"head\"] != source.head_sha or raw[\"base\"] != source.base_sha",
      "replace": "or False",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-refusal-authority",
      "file": "factory_kernel/reconsideration.py",
      "find": "or raw[\"authority\"] != AUTHORITY[raw[\"reason_code\"]]",
      "replace": "or False",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-attempt-replay",
      "file": "factory_kernel/reconsideration.py",
      "find": "if previous:",
      "replace": "if False:",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-affected-frontier",
      "file": "factory_kernel/reconsideration.py",
      "find": "and affected.intersection(session[\"recommendations\"][-1][\"claim_ids\"])]",
      "replace": "and True]",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-round-budget",
      "file": "factory_kernel/exploration.py",
      "find": "if session[\"round\"] >= session[\"policy\"][\"max_rounds\"]:",
      "replace": "if False:",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-pending-effects",
      "file": "factory_kernel/exploration.py",
      "find": "if any(row[\"status\"] == \"pending\" for row in session[\"reservations\"].values()):\n            raise IntentRefused(\"pending effects require reconciliation before reconsideration\")",
      "replace": "if False:\n            raise IntentRefused(\"pending effects require reconciliation before reconsideration\")",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-shared-budget-reset",
      "file": "factory_kernel/exploration_records.py",
      "find": "session[\"reopenings\"].append(deepcopy(data))",
      "replace": "session[\"reopenings\"].append(deepcopy(data))\n            result[\"budgets\"][session[\"binding\"][\"spec_sha256\"]][\"probe_units\"] = 0",
      "why": "Owner feedback must remain subject-bound, causal, bounded and distinct from qualification. Detector: tests/factory/test_reconsideration.py"
    },
    {
      "id": "reconsideration-refusal-retention",
      "file": "factory_kernel/evidence_retention.py",
      "find": "\"holdout.json\", \"architecture-holdout.json\", \"validation-refusal.json\",",
      "replace": "\"holdout.json\", \"architecture-holdout.json\",",
      "why": "Failed validation must retain its existing structured refusal instead of only proof gaps. Detector: tests/factory/test_evidence_retention.py"
    },
    {
      "id": "hosted-exploration-job-replay",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "if created:\n                thread =",
      "replace": "if True:\n                thread =",
      "why": "A replay must never spawn a second job. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-concurrent-job",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "if any(row[\"status\"] == \"running\" for row in state[\"adaptive_runs\"].values()):",
      "replace": "if False:",
      "why": "Recorded unfinished jobs block fresh starts, including after restart. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-live-recovery",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "if run_id in self._threads:",
      "replace": "if False:",
      "why": "A live provider must not be abandoned while it can still return. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-unknown-spend",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "if (budget[\"uncertain\"] or budget[\"calls\"] >= budget[\"limits\"][\"calls\"]",
      "replace": "if (False or budget[\"calls\"] >= budget[\"limits\"][\"calls\"]",
      "why": "Unknown cost blocks even a fresh operational job reservation. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-owner-boundary",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "records._owner(principal)\n        with records.store._locked(project)",
      "replace": "pass\n        with records.store._locked(project)",
      "why": "An untrusted principal must not read private exploration state. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-worker-cap",
      "file": "factory_kernel/hosted_exploration_call.py",
      "find": "max_turns=limits[\"max_turns\"], max_budget_usd=limits[\"max_usd\"],",
      "replace": "max_turns=limits[\"max_turns\"], max_budget_usd=1,",
      "why": "The worker must preserve the owner-reserved cap rather than widening it. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-worker-stop",
      "file": "factory_kernel/hosted_exploration_call.py",
      "find": "    check_stop()\n    with tempfile.",
      "replace": "    pass\n    with tempfile.",
      "why": "A stopped worker must not spend. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-pre-dispatch-stop",
      "file": "factory_kernel/frontdoor_hosted.py",
      "find": "if limits is not None:\n            self.check_stop()\n        deadline",
      "replace": "if False:\n            self.check_stop()\n        deadline",
      "why": "Stop observed after reservation prevents the dispatch effect. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-concurrent-observation",
      "file": "factory_kernel/frontdoor_intent.py",
      "find": "with self._file_locked(project) as path:\n                yield path",
      "replace": "self._thread_lock.release()\n            try:\n                with self._file_locked(project) as path:\n                    yield path\n            finally:\n                self._thread_lock.acquire()",
      "why": "Local observation must not contend with a reserved effect and lose its usable completion. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-reader-adapter",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "inspect_protected_repository(github, list(selected), check_stop=stop)",
      "replace": "inspect_protected_repository(github, selected, check_stop=stop)",
      "why": "Actual hosted source observation must accept fixed configured paths and retain scope after caller mutation. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "hosted-exploration-frozen-paths",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "selected = tuple(paths)",
      "replace": "selected = paths",
      "why": "Actual hosted source observation must accept fixed configured paths and retain scope after caller mutation. Detector: tests/factory/test_frontdoor_exploration.py"
    },
    {
      "id": "authenticated-feedback-archive-digest",
      "file": "factory_kernel/feedback_observation.py",
      "find": "or artifact[\"digest\"] != \"sha256:\" + sha256_bytes(raw)",
      "replace": "or False",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-receipt-binding",
      "file": "factory_kernel/feedback_observation.py",
      "find": "if sha256_value(receipt) != sha256_value(expected):",
      "replace": "if False:",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-receipt-issuer",
      "file": "factory_kernel/feedback_observation.py",
      "find": "row.get(\"user\", {}).get(\"login\") == \"github-actions[bot]\"",
      "replace": "True",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-handoff-binding",
      "file": "factory_kernel/factory_feedback.py",
      "find": "if candidate.sha256 == programme.sha256:",
      "replace": "if True:",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-duplicate-attempt",
      "file": "factory_kernel/factory_feedback.py",
      "find": "if any(row[\"run_identity\"] == identity for row in state[\"factory_outcomes\"].values()):",
      "replace": "if False:",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-outcome-invalidates-strategy",
      "file": "factory_kernel/exploration_records.py",
      "find": "result[\"factory_outcomes\"][data[\"id\"]] = deepcopy(data)",
      "replace": "result[\"factory_outcomes\"][data[\"id\"]] = deepcopy(data)\n            result[\"sessions\"][key][\"status\"] = \"reconsideration-required\"",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-runtime-receipt",
      "file": "factory_kernel/runtime.py",
      "find": "feedback_marker = marker(receipt) + \"\\n\"",
      "replace": "feedback_marker = \"\"",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "authenticated-feedback-stop-before-append",
      "file": "factory_kernel/factory_feedback.py",
      "find": "engine.check_stop()\n        # The shared writer",
      "replace": "pass\n        # The shared writer",
      "why": "Authenticated outcome import must preserve exact provenance without granting causal or qualification authority. Detector: tests/factory/test_factory_feedback.py"
    },
    {
      "id": "strategy-rejection-preregistration",
      "file": "factory_kernel/strategy_rules.py",
      "find": "if (session[\"status\"] != \"exploring\" or session[\"recommendations\"]\n                or session[\"handoffs\"] or session[\"rejection_rules\"]):",
      "replace": "if False:",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-authority-source",
      "file": "factory_kernel/strategy_findings.py",
      "find": "if raw.replace(b\"\\r\\n\", b\"\\n\") != actual.replace(b\"\\r\\n\", b\"\\n\"):",
      "replace": "if False:",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-finding-currency",
      "file": "factory_kernel/strategy_findings.py",
      "find": "if revision != expected_revision:",
      "replace": "if False:",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-unknown-layer",
      "file": "factory_kernel/strategy_findings.py",
      "find": "\"status\": \"unresolved\" if permitted is None else \"supported\" if permitted else \"contradicted\",",
      "replace": "\"status\": \"supported\" if permitted else \"contradicted\",",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-positive-is-contradiction",
      "file": "factory_kernel/strategy_rejection.py",
      "find": "if row[\"status\"] == \"contradicted\"",
      "replace": "if row[\"status\"] in {\"contradicted\", \"supported\"}",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-missing-means-rejection",
      "file": "factory_kernel/strategy_rejection.py",
      "find": "if report else []",
      "replace": "if report else [rules[0][\"claim_id\"]]",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-no-automatic-assessment",
      "file": "factory_kernel/factory_feedback.py",
      "find": "return after_import(engine, self.github, project, result, principal=principal)",
      "replace": "return result",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "strategy-rejection-frontier-dropped",
      "file": "factory_kernel/exploration_records.py",
      "find": "for claim_id in data[\"invalidated_claim_ids\"]:",
      "replace": "for claim_id in []:",
      "why": "Only current independent counterexamples to preregistered operational assumptions may reopen the causal frontier. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "programme-turnover-completed-changed",
      "file": "factory_kernel/programme_turnover.py",
      "find": "if new_items.get(key) != row[\"item\"]:",
      "replace": "if False:",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-receipt-ignored",
      "file": "factory_kernel/programme_turnover.py",
      "find": "receipt = queue.completion_receipt(programme, item, row) if row else None",
      "replace": "receipt = {\"forged\": True} if row and row[\"state\"] == \"closed\" else None",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-worker-hidden",
      "file": "factory_kernel/programme_turnover.py",
      "find": "blockers.extend({\"kind\": \"active-worker\", \"run_id\": row[\"id\"]} for row in execution[\"active_runs\"])",
      "replace": "blockers.extend([])",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-changed-observation",
      "file": "factory_kernel/programme_turnover.py",
      "find": "if execution != _execution(github, current) or observation != source(github):",
      "replace": "if False:",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-budget-reset",
      "file": "factory_kernel/programme_turnover.py",
      "find": "budget = deepcopy(state[\"budgets\"].get(approval[\"spec_sha256\"]))",
      "replace": "budget = None",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-new-intent-ignored",
      "file": "factory_kernel/programme_turnover.py",
      "find": "approval = approved_scope(store, events)",
      "replace": "approval = store._snapshot(events)[\"approvals\"][-1]",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "programme-turnover-unknown-budget-is-zero",
      "file": "factory_kernel/programme_turnover.py",
      "find": "\"status\": \"requires-cumulative-ledger\"",
      "replace": "\"status\": \"zero-spend\"",
      "why": "Replacement review must expose verified completion, conflicting execution and cumulative uncertainty without granting activation. Detector: tests/factory/test_programme_turnover.py"
    },
    {
      "id": "execution-fence-presence",
      "file": "factory_kernel/execution_fence.py",
      "find": "if result[\"state\"] != \"clear\":",
      "replace": "if False:",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-protection",
      "file": "factory_kernel/execution_fence.py",
      "find": "branch.get(\"protected\") is not True or",
      "replace": "False or",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-incomplete-tree",
      "file": "factory_kernel/execution_fence.py",
      "find": "tree.get(\"truncated\") is not False or",
      "replace": "False or",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-changed-source",
      "file": "factory_kernel/execution_fence.py",
      "find": "latest.get(\"protected\") is not True or latest[\"commit\"][\"sha\"] != revision",
      "replace": "False",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-kernel-stop",
      "file": "factory_kernel/runtime.py",
      "find": "            require_execution_open(self.github, self.config.default_branch)",
      "replace": "            pass",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-paid-funnel",
      "file": "factory_kernel/runtime.py",
      "find": "        self.check_stop()  # Includes judges and certifiers that do not enter through _agent.",
      "replace": "        pass  # Missing paid-stage control check.",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-late-publication",
      "file": "factory_kernel/runtime.py",
      "find": "        self.github.cwd = str(cwd)\n        self.check_stop()\n\n        body = paths.root / \"pr-body.md\"\n",
      "replace": "        self.github.cwd = str(cwd)\n\n        body = paths.root / \"pr-body.md\"\n",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. The publication step observes the fence before it renders or pushes anything, and the broker observes it again immediately before the push. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-retry",
      "file": "factory_kernel/runtime.py",
      "find": "        def guarded_retry(attempt):\n            self.check_stop()",
      "replace": "        def guarded_retry(attempt):\n            pass",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-hosted-call",
      "file": "factory_kernel/frontdoor_hosted_worker.py",
      "find": "    check_stop()\n    if payload[\"schema\"] == EXPLORATION_SCHEMA:",
      "replace": "    if payload[\"schema\"] == EXPLORATION_SCHEMA:",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-hosted-service",
      "file": "factory_kernel/frontdoor_exploration.py",
      "find": "    require_execution_open(github)",
      "replace": "    pass",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-publisher",
      "file": "factory_kernel/publication_source.py",
      "find": "if require_execution_open(github) != branch[\"commit\"][\"sha\"]:",
      "replace": "if False:",
      "why": "A protected persistent fence must stop canonical paid work, retries and publication; missing or stale observations cannot open it. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-separate-owner",
      "file": ".github/workflows/dark-factory-programme-publish.yml",
      "find": "  group: dark-factory-worker\n",
      "replace": "  group: dark-factory-programme-publication\n",
      "why": "Publication and canonical execution must share one non-cancelling workflow owner. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-fence-cancelling-owner",
      "file": ".github/workflows/dark-factory-programme-publish.yml",
      "find": "  cancel-in-progress: false",
      "replace": "  cancel-in-progress: true",
      "why": "Publication and canonical execution must share one non-cancelling workflow owner. Detector: tests/factory/test_execution_fence.py"
    },
    {
      "id": "execution-budget-empty-opening",
      "file": "factory_kernel/execution_budget.py",
      "find": "source[\"active_input\"] is None and not seen",
      "replace": "True",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-nonrefundable",
      "file": "factory_kernel/execution_budget.py",
      "find": "result[\"reserved_microusd\"] += data[\"microusd\"]",
      "replace": "result[\"reserved_microusd\"] += 0",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-unresolved",
      "file": "factory_kernel/execution_budget.py",
      "find": "\"unresolved-attempt\" if result[\"uncertain\"] else",
      "replace": "\"unresolved-attempt\" if False else",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-call-limit",
      "file": "factory_kernel/execution_budget.py",
      "find": "result[\"calls\"] >= result[\"allowance\"][\"max_calls\"] or",
      "replace": "False or",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-dollar-limit",
      "file": "factory_kernel/execution_budget.py",
      "find": "state[\"reserved_microusd\"] + request[\"microusd\"] > state[\"allowance\"][\"limit_microusd\"]",
      "replace": "False",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-overrun",
      "file": "factory_kernel/execution_budget.py",
      "find": "observation[\"reported_microusd\"] > row[\"microusd\"]",
      "replace": "False",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-replay",
      "file": "factory_kernel/execution_budget.py",
      "find": "        result = self._append(project, principal, command, \"reserved\", validate)\n        return {\"state\": projection(self._history(project)), \"execute\": not result.replayed}",
      "replace": "        result = self._append(project, principal, command, \"reserved\", validate)\n        return {\"state\": projection(self._history(project)), \"execute\": True}",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-duplicate-attempt",
      "file": "factory_kernel/execution_budget.py",
      "find": "if any(row[\"execution_id\"] == request[\"execution_id\"] and row[\"attempt\"] == request[\"attempt\"]",
      "replace": "if any(False",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-retry",
      "file": "factory_kernel/execution_budget.py",
      "find": "            raise IntentRefused(\"previous execution attempt has unresolved spend; retry refused\")",
      "replace": "            pass",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-late-source",
      "file": "factory_kernel/execution_budget.py",
      "find": "or source[\"main_sha\"] != reserved[\"state\"][\"reservations\"][reservation_id][\"source_sha\"]",
      "replace": "or False",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-budget-late-owner",
      "file": "factory_kernel/execution_budget.py",
      "find": "if (len(events) != reserved[\"state\"][\"project_version\"]",
      "replace": "if (False",
      "why": "Spending is charged before an effect and retained across restarts and replacement; stale, repeated or uncertain attempts cannot gain capacity. Detector: tests/factory/test_execution_budget.py"
    },
    {
      "id": "execution-exchange-signature",
      "file": "factory_kernel/execution_exchange.py",
      "find": "if not hmac.compare_digest(envelope[\"mac\"], self._seal(envelope[\"payload\"], purpose)[\"mac\"]):",
      "replace": "if False:",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-freshness",
      "file": "factory_kernel/execution_exchange.py",
      "find": "or not -5 <= self.clock() - payload[\"issued_at\"] <= MAX_AGE_SECONDS",
      "replace": "or False",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-nonce",
      "file": "factory_kernel/execution_exchange.py",
      "find": "response[\"challenge\"] != challenge or",
      "replace": "False or",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-role-bound",
      "file": "factory_kernel/execution_exchange.py",
      "find": "if call[\"microusd\"] > int(ROLE_BOUNDS[call[\"role\"]] * 1_000_000):",
      "replace": "if False:",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-start-replay",
      "file": "factory_kernel/execution_exchange.py",
      "find": "return result(\"already-started\", state)",
      "replace": "return result(\"start-once\", state)",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-reservation-version",
      "file": "factory_kernel/execution_budget.py",
      "find": "or row[\"reserved_project_version\"] != command[\"expected_project_version\"]",
      "replace": "or False",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-platform-rerun",
      "file": "factory_kernel/execution_authority.py",
      "find": "or run.get(\"run_attempt\") != 1",
      "replace": "or False",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-platform-actor",
      "file": "factory_kernel/execution_authority.py",
      "find": "or run.get(\"actor\", {}).get(\"login\") not in {policy.OWNER, policy.APP_LOGIN}",
      "replace": "or False",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-host-source",
      "file": "factory_kernel/execution_authority.py",
      "find": "if raw.replace(b\"\\r\\n\", b\"\\n\") != local.replace(b\"\\r\\n\", b\"\\n\"):",
      "replace": "if False:",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-secret-boundary",
      "file": "factory_kernel/credential_env.py",
      "find": "or name in HOST_AUTH_CREDENTIALS",
      "replace": "or False",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-remove-secret",
      "file": "factory_kernel/execution_client.py",
      "find": "os.environ.pop(\"FRONTDOOR_AGE_IDENTITY\", \"\")",
      "replace": "os.environ.get(\"FRONTDOOR_AGE_IDENTITY\", \"\")",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-response-origin",
      "file": "factory_kernel/execution_client.py",
      "find": "response.status != 200 or response.geturl() != request.full_url",
      "replace": "response.status != 200",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-exchange-no-retry",
      "file": "factory_kernel/execution_client.py",
      "find": "raise IntentRefused(\"unobserved worker retry refused\")",
      "replace": "return",
      "why": "Only a fresh authenticated canonical worker can consume an exact reservation once; lost responses, reruns and altered owner decisions cannot grant another call. Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "execution-worker-runtime-wrapper",
      "file": "factory_kernel/runtime.py",
      "find": "self.provider = ExecutionWorker(ClaudeCliProvider(config.provider), self.github)",
      "replace": "self.provider = ClaudeCliProvider(config.provider)",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-provider-bypass",
      "file": "factory_kernel/execution_worker.py",
      "find": "return self.client.run(self.provider, request, transcript=transcript, before_retry=before_retry)",
      "replace": "return self.provider.run(request, transcript=transcript, before_retry=before_retry)",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-bypass",
      "file": "factory_kernel/execution_probe.py",
      "find": "            if self.client is None and self.scope is None:\n                state[\"phase\"] = \"launch\"",
      "replace": "            if True:\n                state[\"phase\"] = \"launch\"",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-bound",
      "file": "factory_kernel/execution_probe.py",
      "find": "or argv[argv.index(\"--max-budget-usd\") + 1] != \"1\"",
      "replace": "or False",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-secret",
      "file": "factory_kernel/execution_probe.py",
      "find": "kwargs = {**kwargs, \"env\": env}",
      "replace": "kwargs = {**kwargs, \"env\": supplied}",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-unknown",
      "file": "factory_kernel/execution_probe.py",
      "find": "return SimpleNamespace(cost_usd=cost)",
      "replace": "return SimpleNamespace(cost_usd=cost or 0)",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-failed",
      "file": "factory_kernel/execution_probe.py",
      "find": "                cost = receipts[0].get(\"total_cost_usd\") if len(receipts) == 1 and result.returncode == 0 else None\n                state[\"phase\"] = \"observe\"",
      "replace": "                cost = receipts[0].get(\"total_cost_usd\") if len(receipts) == 1 else None\n                state[\"phase\"] = \"observe\"",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-probe-workflow",
      "file": "factory_kernel/execution_probe.py",
      "find": "        if not os.environ.get(\"GITHUB_ACTIONS\") and not ref:",
      "replace": "        if not ref:",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-workflow-route",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "python -m factory_kernel.execution_probe -- timeout 180 claude",
      "replace": "timeout 180 claude",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "execution-worker-workflow-ping",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "          # The authenticated, reserved CLI probe below establishes routing. No separate",
      "replace": "          routing_code=\"$(curl https://example.invalid)\"\n          # The authenticated, reserved CLI probe below establishes routing. No separate",
      "why": "Canonical workers and every diagnostic leg consume durable project permission before a model call, without leaking authority or assigning unknown spend zero. Detector: tests/factory/test_execution_worker.py"
    },
    {
      "id": "replacement-intent-review-digest",
      "file": "factory_kernel/replacement_intent.py",
      "find": "review[\"review_sha256\"] != command[\"review_sha256\"]",
      "replace": "False",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "replacement-intent-completed-work",
      "file": "factory_kernel/replacement_intent.py",
      "find": "if any(row[\"kind\"] == \"completed-work-changed\" for row in review[\"blockers\"]):",
      "replace": "if False:",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "replacement-intent-owner-drift",
      "file": "factory_kernel/replacement_intent.py",
      "find": "if (len(events) != review[\"project_version\"]\n                    or sha256_value(events[-1]) != review[\"intent_head_sha256\"]):",
      "replace": "if False:",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "replacement-intent-plan-digest",
      "file": "factory_kernel/replacement_intent.py",
      "find": "if sha256_value(plan) != digest:",
      "replace": "if False:",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "replacement-intent-replay-content",
      "file": "factory_kernel/replacement_intent.py",
      "find": "or event[\"command\"][\"payload\"][\"request_sha256\"] != request_sha",
      "replace": "or False",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "replacement-intent-currency",
      "file": "factory_kernel/replacement_intent.py",
      "find": "event[\"project_version\"] == len(events) else \"owner-history-changed\"",
      "replace": "True else \"owner-history-changed\"",
      "why": "The owner can freeze only exact reviewed replacement facts with unchanged completed obligations and current owner history; replays remain immutable historical evidence, never activation. Detector: tests/factory/test_replacement_intent.py"
    },
    {
      "id": "dispatch-plan-unobserved-control-treated-as-clear",
      "file": "factory_kernel/dispatch_plan.py",
      "find": "    if not o.control_observed:\n        blocked = \"control_unobserved\"\n    elif o.stopped:",
      "replace": "    if o.stopped:",
      "why": "A control plane that could not be read is not a clear one: an unreadable stop state or fence must block the plan, never fall through to work selection. Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "dispatch-plan-stale-lease-treated-as-free",
      "file": "factory_kernel/dispatch_plan.py",
      "find": "    elif o.reconciliation_required:\n        blocked = \"reconciliation_required\"\n",
      "replace": "",
      "why": "A lease the reaper would release must be reported as reconciliation_required by the read-only planner, not treated as free work it can select past (C04). Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "dispatch-plan-budget-refusal-dropped",
      "file": "factory_kernel/dispatch_plan.py",
      "find": "    if action in PAID_ACTIONS and o.budget is False:",
      "replace": "    if False:",
      "why": "Paid work with an observed-missing allowance must be visibly budget_required before any launch, never a ready plan (WP00 acceptance). Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "dispatch-plan-runtime-plans-by-reaping",
      "file": "factory_kernel/runtime.py",
      "find": "        return self._dispatch_planner(observe_programme=True).plan(\n            resume=resume, continuation=continuation\n        )",
      "replace": "        self.reap_stale_claims()\n        return self._dispatch_planner(observe_programme=True).plan(\n            resume=resume, continuation=continuation\n        )",
      "why": "Read-only planning performs no lease reaping; the reap is the dispatcher's own authorised effect after planning (R00, C04). Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "dispatch-plan-cli-constructs-execution-runtime",
      "file": "factory_kernel/cli.py",
      "find": "    planner = DispatchPlanner(GitHubClient(cfg.repository, cwd=ROOT), cfg, repo_root=ROOT, observe_programme=True)",
      "replace": "    runtime(args.config)\n    planner = DispatchPlanner(GitHubClient(cfg.repository, cwd=ROOT), cfg, repo_root=ROOT, observe_programme=True)",
      "why": "plan-dispatch must construct no execution runtime and no provider: planning needs the standard library and read modules only (R00). Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "worker-dispatch-not-gated-on-plan",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "    needs: plan\n    if: github.repository == 'ShaishiBear/dark-factory-2.0' && needs.plan.outputs.ready == 'true'",
      "replace": "    if: github.repository == 'ShaishiBear/dark-factory-2.0'",
      "why": "The service-bearing dispatch job may start only after the read-only plan says a dispatch is worth it; job-level services start before any step, so an ungated job pays the idle cost every hour (R00 step 3). Detector: tests/factory/test_dispatch_plan.py"
    },
    {
      "id": "probe-timeout-claims-provider-not-started",
      "file": "factory_kernel/execution_probe.py",
      "find": "            state[\"provider_started\"] = \"unknown\"",
      "replace": "            state[\"provider_started\"] = \"not_started\"",
      "why": "A timeout or lost handle after a launch cannot establish that the provider was never reached; provider_started is three-valued and this case is unknown (C06). Detector: tests/factory/test_execution_probe.py"
    },
    {
      "id": "probe-failure-record-dropped",
      "file": "factory_kernel/execution_probe.py",
      "find": "            self._record(invocation_id, state, exception=exc)\n            raise",
      "replace": "            raise",
      "why": "Every failure phase must leave a typed diagnostic record; a launch that fails without one is the four-run defect WP00 exists to end. Detector: tests/factory/test_execution_probe.py"
    },
    {
      "id": "probe-reason-inferred-from-exception-text",
      "file": "factory_kernel/refusal.py",
      "find": "    if isinstance(exc, subprocess.TimeoutExpired):\n        return \"failed\", \"timeout\"",
      "replace": "    if isinstance(exc, subprocess.TimeoutExpired) or \"unreachable\" in str(exc):\n        return \"failed\", \"timeout\"",
      "why": "Diagnostic classification reads exception classes and the phase, never text: an unexpected exception is retained as such, not recast into a provider conclusion its message suggests. Detector: tests/factory/test_execution_probe.py"
    },
    {
      "id": "test-author-probe-unmetered-runner",
      "file": "scripts/factory_test_author_probe.py",
      "find": "    runner = ProbeRunner.from_environment(DIAGNOSTIC_ROLE)\n    scope = None",
      "replace": "    runner = subprocess.run\n    scope = None",
      "why": "The three nested test-author calls are paid calls and must go through the metered ProbeRunner; a raw subprocess here is the unmetered paid path the critic review named (WP00, C06). Detector: tests/factory/test_factory_test_author_probe.py"
    },
    {
      "id": "project-events-role-allowlist-ignored",
      "file": "factory_kernel/project_events.py",
      "find": "        if principal.role not in spec.roles:",
      "replace": "        if False:",
      "why": "Each registered operation names the roles that may append it; a proposal principal must not approve scope through the primitive (C02). Detector: tests/factory/test_project_events.py"
    },
    {
      "id": "project-events-nonfinite-payload-accepted",
      "file": "factory_kernel/project_events.py",
      "find": "            json.dumps(payload, allow_nan=False)",
      "replace": "            json.dumps(payload)",
      "why": "New records reject non-finite numbers before anything is compared or stored (C01). Detector: tests/factory/test_project_events.py"
    },
    {
      "id": "project-events-cursor-ahead-restarts-chain",
      "file": "factory_kernel/project_events.py",
      "find": "        if after_version > version:\n            return EventPage(version, after_version, (), None, \"cursor_ahead\")",
      "replace": "        if False:\n            return EventPage(version, after_version, (), None, \"cursor_ahead\")",
      "why": "A cursor beyond the verified version is an explicit cursor_ahead result, never a silent empty page that looks like the end of history (C02). Detector: tests/factory/test_project_events.py"
    },
    {
      "id": "project-profile-numeric-identity-ignored",
      "file": "factory_kernel/project_profile.py",
      "find": "    if type(identity) is not int or identity != profile.repository_id:",
      "replace": "    if False:",
      "why": "A destination is verified by its exact numeric repository identity; a name match alone is not a destination match (WP01). Detector: tests/factory/test_project_profile.py"
    },
    {
      "id": "project-profile-visibility-ignored",
      "file": "factory_kernel/project_profile.py",
      "find": "    if type(private) is not bool or ((\"private\" if private else \"public\") != profile.visibility):",
      "replace": "    if False:",
      "why": "Visibility consent is part of the destination check; a private profile must not publish to a public repository or vice versa (WP01). Detector: tests/factory/test_project_profile.py"
    },
    {
      "id": "execution-authority-policy-file-unverified",
      "file": "factory_kernel/execution_authority.py",
      "find": "            for path in POLICY_FILES:\n                raw = read(path, 100000)",
      "replace": "            for path in ():\n                raw = read(path, 100000)",
      "why": "The project profile is part of the execution authority's verified closure; a host whose profile drifted from protected main must refuse exactly as drifted code does (pitfall 15.1). Detector: tests/factory/test_execution_exchange.py"
    },
    {
      "id": "code-subject-stale-blob-accepted",
      "file": "factory_kernel/code_subjects.py",
      "find": "    if not isinstance(expected, str) or expected != source.sha256:",
      "replace": "    if False:",
      "why": "A requested span is bound to the exact blob it believes it addresses; a changed blob is source_mismatch, never a silent re-target (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "code-subject-split-utf8-accepted",
      "file": "factory_kernel/code_subjects.py",
      "find": "        text = source.raw[start:end].decode(\"utf-8\")",
      "replace": "        text = source.raw[start:end].decode(\"utf-8\", \"replace\")",
      "why": "A span splitting a multi-byte UTF-8 sequence is refused as invalid_utf8_boundary, never decoded leniently into replacement characters (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "code-subject-empty-interval-without-anchor",
      "file": "factory_kernel/code_subjects.py",
      "find": "    if start == end and not anchor:",
      "replace": "    if False:",
      "why": "An empty interval is permitted only for an explicitly typed insertion anchor, never as evidence that existing code implements a claim (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "code-subject-symlink-followed",
      "file": "factory_kernel/code_subjects.py",
      "find": "        if mode == \"120000\":\n            gaps.append({\"path\": path, \"reason\": \"symlink_not_followed\"})\n            continue",
      "replace": "        if False:\n            gaps.append({\"path\": path, \"reason\": \"symlink_not_followed\"})\n            continue",
      "why": "The indexer never follows symlinks; a link is a recorded gap, not a blob (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "code-subject-changed-blob-treated-as-current",
      "file": "factory_kernel/code_subjects.py",
      "find": "        if old.blob_oid == new.blob_oid:\n            files[path] = FileChange(path, \"unchanged\", old.blob_oid, new.blob_oid, (), (), ((0, 0, len(old.raw)),))\n            continue",
      "replace": "        if True:\n            files[path] = FileChange(path, \"unchanged\", old.blob_oid, new.blob_oid, (), (), ((0, 0, len(old.raw)),))\n            continue",
      "why": "A changed blob cannot be silently treated as current because line numbers look unchanged; only an exact blob match carries spans over (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "trace-coverage-double-links-inflate",
      "file": "factory_kernel/code_subjects.py",
      "find": "                out.append((s, e))\n    return _union(out)",
      "replace": "                out.append((s, e))\n    return sorted(out)",
      "why": "Overlapping bindings are unioned so double links do not inflate trace coverage (C03). Detector: tests/factory/test_code_subjects.py"
    },
    {
      "id": "claims-cycle-undetected",
      "file": "factory_kernel/claims.py",
      "find": "                if colour[child] == GREY:\n                    cycle = stack[stack.index(child):]\n                    raise ClaimRefused(f\"{what} dependency cycle: {sorted(cycle)}\")",
      "replace": "                if False:\n                    cycle = stack[stack.index(child):]\n                    raise ClaimRefused(f\"{what} dependency cycle: {sorted(cycle)}\")",
      "why": "Dependency edges must be acyclic; cycle members are reported deterministically (C03). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "claims-uncovered-acceptance-bound",
      "file": "factory_kernel/claims.py",
      "find": "    unowned = sorted(set(keys) - set(owners))\n    if unowned:",
      "replace": "    unowned = sorted(set(keys) - set(owners))\n    if False:",
      "why": "Programme binding requires every accepted criterion to have assigned work (C03). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "claims-foreign-authority-profile-accepted",
      "file": "factory_kernel/claims.py",
      "find": "        if claim.kind == \"proof-obligation\" and claim.authority_profile not in policy_ids:",
      "replace": "        if False:",
      "why": "Proof obligations derive from the protected policy; a model cannot invent a weaker authority profile (3.3). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "claims-model-self-certification",
      "file": "factory_kernel/claims.py",
      "find": "        binding = check(row, (\"proposed_implementation\", \"depends_on\"), str(row.get(\"source\", \"model\")))",
      "replace": "        binding = check(row, BINDING_RELATIONS, str(row.get(\"source\", \"model\")))",
      "why": "A model-proposed binding may only propose; an observed relation needs an observer that verified it (LINE_LEVEL_CLAIMS 2). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "claims-forged-approval-accepted",
      "file": "factory_kernel/claims.py",
      "find": "        if (not isinstance(actor, Mapping) or actor.get(\"role\") != \"owner\" or not actor.get(\"identity\")\n                or approval.get(\"spec_sha256\") != spec_sha256 or approval.get(\"spec\") != approved_spec):",
      "replace": "        if False:",
      "why": "Approval is the owner's recorded decision for exactly this spec, never a boolean or a proposal-role record (3.3). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "claims-stale-predecessor-ignored",
      "file": "factory_kernel/claims.py",
      "find": "            if by_key[dep].kind == \"proof-obligation\":\n                dep_proof, dep_currency, _ = proof(dep, seen + (key,))",
      "replace": "            if False:\n                dep_proof, dep_currency, _ = proof(dep, seen + (key,))",
      "why": "A stale or missing predecessor obligation propagates as insufficient to dependents (3.4). Detector: tests/factory/test_claims.py"
    },
    {
      "id": "experiment-frozen-test-edit-accepted",
      "file": "factory_kernel/code_experiments.py",
      "find": "            if path in plan.unchanged_contract_refs:\n                raise InvestigationRefused(f\"candidate patch edits frozen acceptance test {path!r}\")",
      "replace": "            if False:\n                raise InvestigationRefused(f\"candidate patch edits frozen acceptance test {path!r}\")",
      "why": "Experiments may not edit frozen acceptance tests; a candidate that does is refused before any measurement (LINE_LEVEL 4). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "experiment-envelope-escape-accepted",
      "file": "factory_kernel/code_experiments.py",
      "find": "            if path not in plan.editable_subjects:\n                raise InvestigationRefused(f\"candidate patch touches {path!r} outside the design envelope\")",
      "replace": "            if False:\n                raise InvestigationRefused(f\"candidate patch touches {path!r} outside the design envelope\")",
      "why": "A candidate patch stays inside the compiled design envelope; widening it is not the experiment's to do (LINE_LEVEL 4). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "experiment-baseline-not-measured",
      "file": "factory_kernel/code_experiments.py",
      "find": "    order = [BASELINE_ID] + sorted(frozen)",
      "replace": "    order = sorted(frozen)",
      "why": "The unchanged baseline is always included in the comparison; without it a candidate cannot lose to the implementer's own version (C08). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "experiment-stage-not-reverted",
      "file": "factory_kernel/code_experiments.py",
      "find": "        finally:\n            stage.revert(baseline_commit, plan.exact_baseline_tree)\n        observations.append(validate_observation(plan.protocol, candidate_id, raw))",
      "replace": "        finally:\n            pass\n        observations.append(validate_observation(plan.protocol, candidate_id, raw))",
      "why": "Every candidate is measured on the exact baseline plus its own patch and the stage is reset after each run; leakage between candidates is a defect (C08). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "screening-hard-failure-selected",
      "file": "factory_kernel/evaluation_protocol.py",
      "find": "    eligible = [r for r in rows if r[\"hard_pass\"]]",
      "replace": "    eligible = list(rows)",
      "why": "Hard constraints are admission gates: a faster candidate that fails the contract cannot be selected (C09). Detector: tests/factory/test_evaluation_protocol.py"
    },
    {
      "id": "screening-tie-exaggerated",
      "file": "factory_kernel/evaluation_protocol.py",
      "find": "    selected = baseline_id if baseline_id in best else None\n    return ComparisonResult(\"finite-contract-v1\", \"tie\",",
      "replace": "    selected = best[0]\n    return ComparisonResult(\"finite-contract-v1\", \"provisional\",",
      "why": "A tie is a tie: it retains an eligible baseline and never becomes a provisional selection of an arbitrary candidate (C09). Detector: tests/factory/test_evaluation_protocol.py"
    },
    {
      "id": "sign-test-ties-counted",
      "file": "factory_kernel/evaluation_protocol.py",
      "find": "    nonzero = [v for v in values if v != 0]",
      "replace": "    nonzero = list(values)",
      "why": "Zero differences are ties, reported and excluded from n in the exact sign test (C09, NIST). Detector: tests/factory/test_evaluation_protocol.py"
    },
    {
      "id": "protocol-evaluator-from-proposal",
      "file": "factory_kernel/evaluation_protocol.py",
      "find": "        if digest not in trusted:\n            raise ProtocolRefused(\"protocol names an evaluator outside the trusted registry\")\n        evaluators.append(trusted[digest])",
      "replace": "        if digest not in trusted:\n            trusted[digest] = Evaluator(\"proposed\", \"0\", digest, \"unreviewed\", ())\n        evaluators.append(trusted[digest])",
      "why": "Evaluators are resolved by digest from the trusted registry; a proposal cannot supply one (LINE_LEVEL 3). Detector: tests/factory/test_evaluation_protocol.py"
    },
    {
      "id": "paired-inconclusive-called-supported",
      "file": "factory_kernel/evaluation_protocol.py",
      "find": "    if significant and practical:",
      "replace": "    if significant or practical:",
      "why": "A supported directional claim needs both the registered alpha and the preregistered practical threshold (C09). Detector: tests/factory/test_evaluation_protocol.py"
    },
    {
      "id": "search-confirmation-results-steer-search",
      "file": "factory_kernel/search_policy.py",
      "find": "    rows = [o for o in public_observations if o.get(\"partition\") == \"public\" and o.get(\"complete\") is True and o.get(\"hard_pass\") is True]",
      "replace": "    rows = [o for o in public_observations if o.get(\"complete\") is True and o.get(\"hard_pass\") is True]",
      "why": "Confirmation results are never a candidate-generation score stream; only public search observations choose the next parent (LINE_LEVEL 5). Detector: tests/factory/test_search_policy.py"
    },
    {
      "id": "runtime-investigation-request-ignored",
      "file": "factory_kernel/runtime.py",
      "find": "            self._host_investigation(worktree.path, paths, env, stage=\"implement\")",
      "replace": "            pass  # investigation skipped",
      "why": "The inner experiment loop is wired between the implementer and the GREEN gate; an implementer's bounded request must be honoured there, not dropped (LINE_LEVEL 4, R02). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "runtime-investigation-recovery-anchors-to-head",
      "file": "factory_kernel/runtime.py",
      "find": "            if (stage_tree.head_commit() != baseline_commit or stage_tree.head_tree() != baseline_tree\n                    or not stage_tree.is_clean()):\n                stage_tree.revert(baseline_commit, baseline_tree)",
      "replace": "            if not stage_tree.is_clean():\n                stage_tree.revert(stage_tree.head_commit(), stage_tree.head_tree())",
      "why": "Refusal recovery must restore the recorded baseline commit; anchoring to whatever HEAD is after a failed reset silently adopts a disposable candidate commit as the worker's baseline (C08 stage identity; evidence review 68ea493b F1). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "attestation-object-hash-not-checked",
      "file": "factory_kernel/attestations.py",
      "find": "        elif not isinstance(data, (bytes, bytearray)) or sha256_bytes(bytes(data)) != row[\"sha256\"]:\n            tampered.append(row[\"retained_object_id\"])",
      "replace": "        elif not isinstance(data, (bytes, bytearray)):\n            tampered.append(row[\"retained_object_id\"])",
      "why": "verify_attestation must re-hash every retained object against its attested digest; a present but altered artifact is forged evidence (SPEC 3.4). Detector: tests/factory/test_attestations.py"
    },
    {
      "id": "attestation-local-issuer-authenticated",
      "file": "factory_kernel/attestations.py",
      "find": "AUTHENTICATED_PLATFORMS = (\"github-actions\",)",
      "replace": "AUTHENTICATED_PLATFORMS = (\"github-actions\", \"local\")",
      "why": "Only the platform issuer can be authenticated independently; a local execution is recorded, never trusted (SPEC 3.4 'signature or workflow-success bit alone is insufficient'). Detector: tests/factory/test_attestations.py"
    },
    {
      "id": "attestation-issuer-observation-ignored",
      "file": "factory_kernel/attestations.py",
      "find": "    if observed != issuer:\n        differing = sorted(name for name in ISSUER_FIELDS if observed[name] != issuer[name])",
      "replace": "    if observed[\"platform\"] != issuer[\"platform\"]:\n        differing = sorted(name for name in ISSUER_FIELDS if observed[name] != issuer[name])",
      "why": "The issuer must match the independent observation on every field (run, attempt, job, source revision), not merely the platform name. Detector: tests/factory/test_attestations.py"
    },
    {
      "id": "currency-missing-dependency-is-current",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "        if then is None or not isinstance(now, str) or not now:\n            codes.append(\"dependency_missing\"); affected.append(identity)",
      "replace": "        if then is None or not isinstance(now, str) or not now:\n            continue",
      "why": "C04: missing or unknown dependencies are insufficient, never a cache hit. Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "currency-unknown-coverage-is-current",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "    if any(row[\"coverage\"] != \"complete\" for row in required.values()) or observations.get(\"coverage\", \"complete\") != \"complete\":\n        codes.append(\"dependency_coverage_unknown\")",
      "replace": "    if False:\n        codes.append(\"dependency_coverage_unknown\")",
      "why": "Unknown dependency-analysis coverage forbids narrowing and is insufficient (C04). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "currency-forged-issuer-continues",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "    if observations.get(\"issuer_valid\") is not True:\n        codes.append(\"issuer_invalid\"); details.append(\"issuer could not be authenticated independently\")\n        return finish()",
      "replace": "    if observations.get(\"issuer_valid\") is not True:\n        codes.append(\"dependency_changed\"); details.append(\"issuer could not be authenticated independently\")\n        return finish()",
      "why": "Forged evidence is rejected, not stale; an unauthenticated issuer cannot become a merely stale proof (C04). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "frontier-selected-edge-propagates",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "        if edge[\"kind\"] == \"depends_on\":\n            dependents.setdefault(edge[\"target\"], []).append(edge[\"source\"])",
      "replace": "        dependents.setdefault(edge[\"target\"], []).append(edge[\"source\"])",
      "why": "Only explicit depends_on edges carry proof dependence; a selected or layout relation must not invalidate proof (C04). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "frontier-incomplete-index-all-clear",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "    if not isinstance(dependency_index, Mapping) or dependency_index.get(\"version\") != \"1.0\" or dependency_index.get(\"complete\") is not True:\n        raise DependencyIndexInvalid(\"dependency index is missing, unversioned or incomplete\")",
      "replace": "    if not isinstance(dependency_index, Mapping) or dependency_index.get(\"version\") != \"1.0\" or dependency_index.get(\"complete\") is not True:\n        return ()",
      "why": "An invalid or incomplete dependency index cannot yield an empty all-clear frontier (C04). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "profile-narrowing-accepted",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "        if entry[\"narrowing_allowed\"] is not False:\n            raise ProfileRefused(",
      "replace": "        if entry[\"narrowing_allowed\"] not in (True, False):\n            raise ProfileRefused(",
      "why": "Only a protected migration can narrow a dependency profile; the policy file cannot grant itself narrowing (SPEC 3.4). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "profile-spine-equivalence-skipped",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "    if spine_claim_ids is not None and set(obligations) != set(spine_claim_ids):\n        raise ProfileRefused(",
      "replace": "    if spine_claim_ids is not None and not set(obligations) <= set(spine_claim_ids):\n        raise ProfileRefused(",
      "why": "The obligation map must name exactly the protected spine claims; dropping one silently removes a required obligation from currency (WP04 existing-policy equivalence). Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "proof-store-index-before-objects",
      "file": "factory_kernel/proof_store.py",
      "find": "        if role is None:\n            raise ProofStoreRefused(f\"evidence object {digest} is not retained; an index cannot precede its objects\")\n        objects.append",
      "replace": "        if role is None:\n            role = \"public\"\n        objects.append",
      "why": "Partial writes never establish retention: an index must not reference an object the store does not hold (SPEC 3.5). Detector: tests/factory/test_proof_store.py"
    },
    {
      "id": "proof-store-conflict-overwrites",
      "file": "factory_kernel/proof_store.py",
      "find": "        if target.read_bytes() != data:\n            raise ProofStoreRefused(f\"object conflict: {digest} already holds different bytes\")\n        return digest",
      "replace": "        if target.read_bytes() != data:\n            _write_atomic(target, data)\n        return digest",
      "why": "Object writes are immutable; a same-key/different-bytes conflict refuses instead of overwriting (SPEC 3.5). Detector: tests/factory/test_proof_store.py"
    },
    {
      "id": "companion-judge-material-public",
      "file": "factory_kernel/evidence_closure.py",
      "find": "            proof_store.put_verified_object(store, data, role=\"judge\" if requirement.claim_id in JUDGE_CLAIMS else \"public\")",
      "replace": "            proof_store.put_verified_object(store, data, role=\"public\")",
      "why": "Independent judge material must stay in its own privacy partition, inaccessible to proposal/build roles (SPEC 3.5). Detector: tests/factory/test_factory_attestation_companions.py"
    },
    {
      "id": "companion-artifact-digest-unchecked",
      "file": "factory_kernel/evidence_closure.py",
      "find": "            if sha256_bytes(data) != ref.sha256:\n                raise ValueError(f\"materialized artifact {ref.path} differs from the manifest digest\")",
      "replace": "            if False:\n                raise ValueError(f\"materialized artifact {ref.path} differs from the manifest digest\")",
      "why": "A companion attests the bytes on disk against the manifest digest; a tampered artifact must not be retained under the manifest's identity. Detector: tests/factory/test_factory_attestation_companions.py"
    },
    {
      "id": "companion-incomplete-claim-passes",
      "file": "factory_kernel/evidence_closure.py",
      "find": "            outcome={\"verdict\": \"pass\" if complete else \"incomplete\", \"reason_codes\": reasons},",
      "replace": "            outcome={\"verdict\": \"pass\", \"reason_codes\": reasons},",
      "why": "An incomplete spine claim is attested as incomplete; a companion cannot report a success the closure did not reach (C04). Detector: tests/factory/test_factory_attestation_companions.py"
    },
    {
      "id": "wrapper-companions-dropped",
      "file": "scripts/factory_evidence_spine.py",
      "find": "        final[\"attestations\"] = emit_companions(manifest=manifest, index=index, artifact_root=artifact_root,\n                                                head=head, kernel_root=HERE.parent, candidate_root=ROOT)",
      "replace": "        final[\"attestations\"] = {\"index_sha256\": \"0\" * 64, \"count\": 21, \"verified\": 21,\n                                 \"shadow_current\": 21, \"proof_reuse_allowed\": False}",
      "why": "Companions are emitted at the trusted wrapper boundary for every full run; a wrapper that writes a plausible summary without emitting them leaves no shadow record (WP05, R05). Detector: tests/factory/test_factory_attestation_companions.py"
    },
    {
      "id": "currency-unobserved-subject-is-current",
      "file": "factory_kernel/proof_dependencies.py",
      "find": "    subject = observations.get(\"subject_match\")\n    if subject is None:\n        codes.append(\"subject_unobserved\"); details.append(\"the exact subject under query was not compared\")\n        return finish()",
      "replace": "    subject = observations.get(\"subject_match\", True)\n    if subject is None:\n        codes.append(\"subject_unobserved\"); details.append(\"the exact subject under query was not compared\")\n        return finish()",
      "why": "C04: authenticate the exact subject before anything else; an observer that never compared the subject is insufficient, never current. Detector: tests/factory/test_proof_dependencies.py"
    },
    {
      "id": "companion-reader-ignores-store",
      "file": "factory_kernel/evidence_closure.py",
      "find": "        try:\n            retained = proof_store.get_by_digest(store, sha256_bytes(data))\n        except proof_store.ProofStoreRefused:\n            return None\n        return data if retained == data else None",
      "replace": "        return data",
      "why": "Verification loads retained objects; bytes on disk that the proof store does not hold are not retained evidence (SPEC 3.5 'no object address grants proof'). Detector: tests/factory/test_factory_attestation_companions.py"
    },
    {
      "id": "worker-job-env-runner-context",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "      ANTHROPIC_BASE_URL: https://openrouter.ai/api\n    services:",
      "replace": "      ANTHROPIC_BASE_URL: https://openrouter.ai/api\n      FACTORY_DIAGNOSTICS_DIR: ${{ runner.temp }}/dark-factory/diagnostics\n    services:",
      "why": "A job-level env may not use the runner context; GitHub refuses the whole workflow file at load time and the hourly schedule silently starts nothing (17 Sept 2026 outage after WP00). Detector: tests/factory/test_factory_workflow_contexts.py"
    },
    {
      "id": "scheduler-budget-outranks-stop",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "    control_block = None\n    if not o[\"control_observed\"]:\n        control_block = \"control_unobserved\"\n    elif o[\"stopped\"]:\n        control_block = \"stopped\"",
      "replace": "    control_block = None\n    if not o[\"control_observed\"]:\n        control_block = \"control_unobserved\"\n    elif o[\"stopped\"] and o[\"budget\"] is not False:\n        control_block = \"stopped\"",
      "why": "C04: a known stopped state takes precedence over budget; an exhausted allowance must not turn a stop into a budget refusal. Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "scheduler-unobserved-control-proposes",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "    if not o[\"control_observed\"]:\n        control_block = \"control_unobserved\"\n    elif",
      "replace": "    if False:\n        control_block = \"control_unobserved\"\n    elif",
      "why": "C04: an unavailable control observer blocks every action; proposing under an unobserved control plane is a defect. Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "scheduler-merge-without-full-closure",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "        incomplete = tuple(f\"obligation_incomplete:{claim_id}\" for claim_id in policy_ids\n                           if not isinstance(proof.get(claim_id), Mapping) or proof[claim_id].get(\"status\") != CURRENT)",
      "replace": "        incomplete = tuple(f\"obligation_incomplete:{claim_id}\" for claim_id in policy_ids\n                           if isinstance(proof.get(claim_id), Mapping) and proof[claim_id].get(\"status\") not in (CURRENT, \"stale\", \"insufficient\"))",
      "why": "Merge requires every existing obligation current for the exact head, not merely the absence of rejections (C04). Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "scheduler-merge-exact-head-unchecked",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "        exact = () if o[\"exact_head_checked\"].get(pr) == head else (\"exact_head_unverified\",)",
      "replace": "        exact = ()",
      "why": "Merge requires the exact-head check for the head the proof names (C04 'every existing obligation and exact-head check'). Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "scheduler-missing-predecessor-passes",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "                    if proof is None or any(not isinstance(proof.get(c), Mapping) or proof[c].get(\"status\") != CURRENT for c in policy_ids):\n                        codes.append(f\"predecessor_incomplete:{blocker}\")",
      "replace": "                    if False:\n                        codes.append(f\"predecessor_incomplete:{blocker}\")",
      "why": "Missing predecessors block downstream actions (SPEC 4.1); a blocked programme item must not be proposed before its predecessor is proved. Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "scheduler-paid-action-without-allowance",
      "file": "factory_kernel/claim_scheduler.py",
      "find": "        if o[\"budget\"] is None:\n            return (\"budget_unobserved\",)\n        return () if o[\"budget\"] else (\"budget_required\",)",
      "replace": "        return ()",
      "why": "A paid action needs a declared, observed allowance; an unobserved or exhausted allowance blocks it (C04 'required resources declared'). Detector: tests/factory/test_claim_scheduler.py"
    },
    {
      "id": "graph-unverified-proof-shown-green",
      "file": "factory_kernel/project_graph.py",
      "find": "                  currentness=currency if verified else \"unverified\",\n                  gaps=([] if verified else [\"verification-refused\"]) + ([] if item_id else [\"subject-unlinked\"]),",
      "replace": "                  currentness=currency,\n                  gaps=([] if item_id else [\"subject-unlinked\"]),",
      "why": "Stale or unverified proof is never shown as current (SPEC 10 'Prove' view: show all blockers, not a green badge). Detector: tests/factory/test_project_graph.py"
    },
    {
      "id": "graph-proposed-component-shown-observed",
      "file": "factory_kernel/project_graph.py",
      "find": "        _node(nodes, comp_id, \"implementation-component\", subject, source_refs=[f\"binding:{row.get('source')}\"],\n              currentness=\"observed\" if standing == \"observed\" else \"proposed\",",
      "replace": "        _node(nodes, comp_id, \"implementation-component\", subject, source_refs=[f\"binding:{row.get('source')}\"],\n              currentness=\"observed\",",
      "why": "A model-proposed mapping stays a proposal; the graph must not show a predicted component as implemented (SPEC 10). Detector: tests/factory/test_project_graph.py"
    },
    {
      "id": "graph-certifies-across-subjects",
      "file": "factory_kernel/project_graph.py",
      "find": "            item_id = heads.get(str(subject.get(\"candidate_commit\")))",
      "replace": "            item_id = next(iter(items), None)",
      "why": "An attestation certifies only the obligations of the item whose exact head it names; recorded dependency match is labelled separately from current trusted proof (WP04). Detector: tests/factory/test_project_graph.py"
    },
    {
      "id": "graph-truncation-keeps-dangling-edges",
      "file": "factory_kernel/project_graph.py",
      "find": "    ordered_edges = [e for e in ordered_edges if e[\"source\"] in kept and e[\"target\"] in kept][:max_edges]",
      "replace": "    ordered_edges = ordered_edges[:max_edges]",
      "why": "A bounded snapshot must stay a graph over the nodes it carries; a dangling edge fabricates a relation to an absent node. Detector: tests/factory/test_project_graph.py"
    },
    {
      "id": "claim-views-plan-record-unchecked",
      "file": "factory_kernel/claim_views.py",
      "find": "        if not isinstance(plan, dict) or plan.get(\"schema\") != \"dark-factory/dispatch-plan\":\n            raise ClaimViewRefused(\"dispatch plan is not a plan-dispatch record\")",
      "replace": "        if not isinstance(plan, dict):\n            raise ClaimViewRefused(\"dispatch plan is not a plan-dispatch record\")",
      "why": "The shadow comparison must compare against a recorded plan-dispatch decision, not any JSON object handed to it. Detector: tests/factory/test_claim_views.py"
    },
    {
      "id": "claim-views-unverified-proof-counted",
      "file": "factory_kernel/claim_views.py",
      "find": "        if subject.get(\"candidate_commit\") != head or not (verification.get(aid) or {}).get(\"verified\"):\n            continue",
      "replace": "        if subject.get(\"candidate_commit\") != head:\n            continue",
      "why": "Only verified companions enter the proof the scheduler sees; an unverified record cannot make merge proposable. Detector: tests/factory/test_claim_views.py"
    },
    {
      "id": "lease-guard-expiry-boundary",
      "file": "factory_kernel/lease_store.py",
      "find": "    if now >= expires_at:\n        return GuardResult(\"refused\", (\"lease_expired\",))",
      "replace": "    if now > expires_at:\n        return GuardResult(\"refused\", (\"lease_expired\",))",
      "why": "C05: the expiry boundary is now >= expires_at; a lease at its expiry instant has no permission. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-guard-identity-after-state",
      "file": "factory_kernel/lease_store.py",
      "find": "    if epoch != current_epoch or owner != current_owner or generations != current_generations:\n        return GuardResult(\"refused\", (\"lease_identity_mismatch\",))\n    if not lease[\"active\"]:\n        return GuardResult(\"refused\", (\"lease_inactive\",))",
      "replace": "    if not lease[\"active\"]:\n        return GuardResult(\"refused\", (\"lease_inactive\",))\n    if epoch != current_epoch or owner != current_owner or generations != current_generations:\n        return GuardResult(\"refused\", (\"lease_identity_mismatch\",))",
      "why": "The guard's priority is schema, identity, live state, uncertainty, replay (conformance/README lease_guard); reporting a state reason for a bundle that is not even the same identity misattributes the refusal. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-acquire-partial-bundle",
      "file": "factory_kernel/lease_store.py",
      "find": "            held = self._live_holders(names, now)\n            if held:\n                raise LeaseRefused(\"resource held by a live lease: \" + \", \".join(sorted({f\"{r}@{o}\" for r, o, _ in held})))",
      "replace": "            held = self._live_holders(names, now)\n            if held:\n                names = [n for n in names if n not in {r for r, _, _ in held}]\n                if not names:\n                    raise LeaseRefused(\"resource held by a live lease: \" + \", \".join(sorted({f\"{r}@{o}\" for r, o, _ in held})))",
      "why": "C05: acquire all sorted resources or none; acquiring the free subset breaks bundle atomicity. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-heartbeat-revives-stale",
      "file": "factory_kernel/lease_store.py",
      "find": "            result, _ = self._guard(bundle, now)\n            if result.status != \"eligible\":\n                return result\n            self.connection.execute(\"UPDATE leases SET expires_at = ? WHERE lease_id = ?\", (now + ttl, bundle.lease_id))",
      "replace": "            result, _ = self._guard(bundle, now)\n            if result.status != \"eligible\" and result.reason_codes != (\"lease_expired\",):\n                return result\n            self.connection.execute(\"UPDATE leases SET expires_at = ? WHERE lease_id = ?\", (now + ttl, bundle.lease_id))",
      "why": "C05: a heartbeat updates expiry only before expiry; a stale heartbeat can never recreate ownership. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-release-clears-in-flight",
      "file": "factory_kernel/lease_store.py",
      "find": "            if lease.get(\"in_flight\"):\n                return GuardResult(\"refused\", (\"effect_uncertain\",))\n            self.connection.execute(\"UPDATE leases SET active = 0, ended_at = ?, end_reason = 'released' WHERE lease_id = ?\",",
      "replace": "            if False:\n                return GuardResult(\"refused\", (\"effect_uncertain\",))\n            self.connection.execute(\"UPDATE leases SET active = 0, ended_at = ?, end_reason = 'released' WHERE lease_id = ?\",",
      "why": "C05: release cannot clear an ambiguous in-flight operation. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-grant-conflict-ignored",
      "file": "factory_kernel/lease_store.py",
      "find": "                if prior_sha != request_sha256:\n                    raise LeaseRefused(\"operation id reused with different request bytes (conflict)\")",
      "replace": "                if False:\n                    raise LeaseRefused(\"operation id reused with different request bytes (conflict)\")",
      "why": "C05: reusing an operation id with different request bytes is a conflict, never a replay. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-grant-replays-execution",
      "file": "factory_kernel/lease_store.py",
      "find": "                if state in {\"observed_success\", \"observed_failure\"}:\n                    return {\"grant_id\": grant_id, \"state\": state, \"replayed\": True, \"result\": json.loads(result) if result else None,\n                            \"execute\": False}",
      "replace": "                if state in {\"observed_success\", \"observed_failure\"}:\n                    return {\"grant_id\": grant_id, \"state\": state, \"replayed\": True, \"result\": json.loads(result) if result else None,\n                            \"execute\": True}",
      "why": "C05: a grant may reference a previously observed result on an identical replay; it never executes again. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-terminal-state-reopened",
      "file": "factory_kernel/lease_store.py",
      "find": "            if row[0] != \"started\" and not (row[0] == \"uncertain\" and outcome != \"uncertain\"):\n                raise LeaseRefused(f\"grant in state {row[0]!r} cannot be observed as {outcome!r}\")",
      "replace": "            if False:\n                raise LeaseRefused(f\"grant in state {row[0]!r} cannot be observed as {outcome!r}\")",
      "why": "C05: terminal grant states never return; an observed operation cannot be re-observed as something else. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-unresolved-resource-reacquired",
      "file": "factory_kernel/lease_store.py",
      "find": "            pending = self._unresolved_operations(names)\n            if pending:\n                raise LeaseRefused(\"resource has an unresolved operation: \" + \", \".join(sorted(str(r[0]) for r in pending)))",
      "replace": "            pending = []\n            if pending:\n                raise LeaseRefused(\"resource has an unresolved operation: \" + \", \".join(sorted(str(r[0]) for r in pending)))",
      "why": "C05: a resource with a pending or uncertain operation cannot be acquired by another owner; expiry removes permission, it does not prove remote absence. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "lease-clock-rollback-accepted",
      "file": "factory_kernel/lease_store.py",
      "find": "        if now < last:\n            raise LeaseRefused(\"clock rollback observed; time-dependent authority blocked\")",
      "replace": "        if False:\n            raise LeaseRefused(\"clock rollback observed; time-dependent authority blocked\")",
      "why": "C05: clock rollback or unknown time blocks time-dependent authority. Detector: tests/factory/test_lease_store.py"
    },
    {
      "id": "meter-ceilings-exceed-bundle",
      "file": "factory_kernel/validation_meter.py",
      "find": "    if scope.ceiling_sum() + microusd > scope.limit_microusd:\n        raise MeterRefused(\"call ceilings would exceed the bundle reservation\")",
      "replace": "    if False:\n        raise MeterRefused(\"call ceilings would exceed the bundle reservation\")",
      "why": "C06: nested per-call ceilings must sum within the parent reservation; a bundle cannot be overdrawn by its own calls. Detector: tests/factory/test_validation_meter.py"
    },
    {
      "id": "meter-class-borrows-balance",
      "file": "factory_kernel/validation_meter.py",
      "find": "    if call_class != scope.scope_class:\n        raise MeterRefused(f\"scope {scope.scope_class!r} cannot spend for class {call_class!r}\")",
      "replace": "    if call_class not in SPEND_CLASSES:\n        raise MeterRefused(f\"scope {scope.scope_class!r} cannot spend for class {call_class!r}\")",
      "why": "SPEC 6.2: a scope cannot spend another scope's balance; classes are explicit and do not borrow. Detector: tests/factory/test_validation_meter.py"
    },
    {
      "id": "meter-start-repeats",
      "file": "factory_kernel/validation_meter.py",
      "find": "    if call.state != \"reserved\":\n        raise MeterRefused(\"call was already started; a lost response cannot be reissued\")",
      "replace": "    if call.state not in (\"reserved\", \"started\"):\n        raise MeterRefused(\"call was already started; a lost response cannot be reissued\")",
      "why": "C06: durable start then at most one upstream transmission; a lost response after start cannot be reissued. Detector: tests/factory/test_validation_meter.py"
    },
    {
      "id": "meter-unknown-receipt-summed-as-zero",
      "file": "factory_kernel/validation_meter.py",
      "find": "        elif call.state == \"observed\" and call.reported_microusd is None:\n            unknown += 1",
      "replace": "        elif call.state == \"observed\" and call.reported_microusd is None:\n            known += 0",
      "why": "An unknown receipt keeps the bundle unknown; summing it as zero would report a bill nobody observed (C06 'unknown bill freezes further spend'). Detector: tests/factory/test_validation_meter.py"
    },
    {
      "id": "meter-never-started-call-refunded",
      "file": "factory_kernel/validation_meter.py",
      "find": "    def ceiling_sum(self) -> int:\n        return sum(call.microusd for call in self.calls.values())",
      "replace": "    def ceiling_sum(self) -> int:\n        return sum(call.microusd for call in self.calls.values() if call.state != \"not_started\")",
      "why": "C06: never optimise away the recorded attempted allocation; a reserved call that never started keeps its ceiling counted. Detector: tests/factory/test_validation_meter.py"
    },
    {
      "id": "gateway-forwards-caller-auth",
      "file": "factory_kernel/provider_gateway.py",
      "find": "        if key in STRIPPED_HEADERS or key not in ALLOWED_HEADERS:\n            continue",
      "replace": "        if key not in ALLOWED_HEADERS and key not in STRIPPED_HEADERS:\n            continue",
      "why": "The gateway strips caller credentials and forwarding headers; a caller must never reach upstream with its own authorization (SPEC 5, C06). Detector: tests/factory/test_provider_gateway.py"
    },
    {
      "id": "gateway-model-from-caller",
      "file": "factory_kernel/provider_gateway.py",
      "find": "            if payload.get(\"model\") not in (None, route.model):\n                raise GatewayRefused(\"request names a model the route does not permit\")\n            payload[\"model\"] = route.model",
      "replace": "            payload.setdefault(\"model\", route.model)",
      "why": "The model and endpoint are fixed by the protected route; a caller-named model would swap providers or tariffs silently (C06). Detector: tests/factory/test_provider_gateway.py"
    },
    {
      "id": "gateway-replays-upstream-generation",
      "file": "factory_kernel/provider_gateway.py",
      "find": "    if previous is not None:\n        if previous.body_sha256 != body_sha:\n            raise GatewayRefused(\"request id collision with a different body\")\n        # A reconnect: what was recorded, never a second upstream generation.\n        return {**previous.to_dict(), \"replayed\": True, \"body\": previous.body}",
      "replace": "    if previous is not None and previous.body_sha256 != body_sha:\n        raise GatewayRefused(\"request id collision with a different body\")",
      "why": "C06: gateway stream reconnects cannot replay upstream generation; a reconnect returns the record, never a second transmission. Detector: tests/factory/test_provider_gateway.py"
    },
    {
      "id": "gateway-follows-redirect",
      "file": "factory_kernel/provider_gateway.py",
      "find": "        if 300 <= int(response.status) < 400:\n            detail = \"redirect refused\"\n            raise GatewayRefused(detail)",
      "replace": "        if False:\n            detail = \"redirect refused\"\n            raise GatewayRefused(detail)",
      "why": "The gateway disallows redirects (C06); following one could send the credential elsewhere. Detector: tests/factory/test_provider_gateway.py"
    },
    {
      "id": "gateway-partial-stream-reported-complete",
      "file": "factory_kernel/provider_gateway.py",
      "find": "    if partial:\n        record.outcome = \"partial\"\n        observe_call(session.scope, call.call_id, reported_microusd=None, outcome=\"partial\", telemetry={\"detail\": detail, \"bytes\": len(collected)})",
      "replace": "    if False:\n        record.outcome = \"partial\"\n        observe_call(session.scope, call.call_id, reported_microusd=None, outcome=\"partial\", telemetry={\"detail\": detail, \"bytes\": len(collected)})",
      "why": "C06: a cut stream is recorded as a partial with unknown spend; reporting it complete would invent a bounded receipt. Detector: tests/factory/test_provider_gateway.py"
    },
    {
      "id": "billing-conflict-within-ceiling-called-complete",
      "file": "factory_kernel/billing_reconciliation.py",
      "find": "        elif reported is not None and matched[0].billed_microusd != reported:\n            verdict, reason = \"conflicting\", \"billed amount differs from reported telemetry\"",
      "replace": "        elif False:\n            verdict, reason = \"conflicting\", \"billed amount differs from reported telemetry\"",
      "why": "Reconciliation distinguishes a conflicting receipt from a complete one; telemetry that disagrees with the bill stays unresolved (C06). Detector: tests/factory/test_billing_reconciliation.py"
    },
    {
      "id": "billing-adapter-enabled-by-default",
      "file": "factory_kernel/billing_reconciliation.py",
      "find": "    if not adapter[\"enabled\"] or not adapter[\"interface_verified\"]:\n        raise ReconciliationRefused(f\"billing adapter {adapter_id!r} is disabled: provider interface unverified\")",
      "replace": "    if False:\n        raise ReconciliationRefused(f\"billing adapter {adapter_id!r} is disabled: provider interface unverified\")",
      "why": "Provider reconciliation adapters are disabled by default until the actual interface is verified (WP02 'disabled-by-default provider reconciliation adapters'). Detector: tests/factory/test_billing_reconciliation.py"
    },
    {
      "id": "probe-bundle-launch-unmetered",
      "file": "factory_kernel/execution_probe.py",
      "find": "            if self.scope is not None:\n                return self._launch_metered(invocation_id, request, argv, kwargs, state)",
      "replace": "            if self.scope is not None:\n                state[\"phase\"] = \"launch\"\n                result = self._launch(argv, kwargs, state)\n                state[\"phase\"] = \"returned\"\n                self._record(invocation_id, state)\n                return result",
      "why": "A diagnostic launch under a bundle must take its per-call ceiling from the meter before a process exists; launching without reserving is the unmetered paid path WP00 removed (C06). Detector: tests/factory/test_probe_bundle.py"
    },
    {
      "id": "probe-bundle-failure-unobserved",
      "file": "factory_kernel/execution_probe.py",
      "find": "        except BaseException:\n            observe_call(self.scope, call.call_id, reported_microusd=None, outcome=\"failed\",\n                         telemetry={\"provider_started\": state[\"provider_started\"]})\n            raise",
      "replace": "        except BaseException:\n            raise",
      "why": "A launch that dies after its start is charged or unknown; leaving the call started forever hides that from the bundle receipt (C06). Detector: tests/factory/test_probe_bundle.py"
    },
    {
      "id": "probe-bundle-nonzero-exit-receipted",
      "file": "factory_kernel/execution_probe.py",
      "find": "        cost = receipts[0].get(\"total_cost_usd\") if len(receipts) == 1 and result.returncode == 0 else None\n        try:\n            reported = microusd(cost) if cost is not None else None",
      "replace": "        cost = receipts[0].get(\"total_cost_usd\") if len(receipts) == 1 else None\n        try:\n            reported = microusd(cost) if cost is not None else None",
      "why": "A nonzero exit cannot turn unknown spend into a bounded receipt; only a clean single receipt is telemetry (C06). Detector: tests/factory/test_probe_bundle.py"
    },
    {
      "id": "probe-script-three-reservations",
      "file": "scripts/factory_test_author_probe.py",
      "find": "        runner = runner.with_scope(scope)",
      "replace": "        close_validation_scope(scope); scope = None",
      "why": "The three nested calls run inside one prepaid bundle whose per-call ceilings sum within its reservation; falling back to per-call reservations after opening the bundle double-charges the ledger (WP02, C06). Detector: tests/factory/test_probe_bundle.py"
    },
    {
      "id": "lesson-unknown-gate-admits",
      "file": "factory_kernel/lessons.py",
      "find": "        if value is None:\n            # Unknown never admits: an unobserved gate is insufficient, a prohibition class or not.\n            return AdmissionResult(\"insufficient\", (code,), gates, refs, policy_sha)",
      "replace": "        if value is None:\n            continue",
      "why": "C10: unknown sample sufficiency or any unobserved gate never admits; skipping an unknown gate lets a model-authored proposal through on missing evidence. Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "lesson-missing-policy-admits",
      "file": "factory_kernel/lessons.py",
      "find": "    gates[\"policy\"] = policy is not None and gates[\"policy\"] is not False",
      "replace": "    gates[\"policy\"] = gates[\"policy\"] is not False",
      "why": "Missing policy permits proposed lessons and offline reports only (SPEC 11.1); an observation cannot stand in for the protected policy object. Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "lesson-regression-after-benefit",
      "file": "factory_kernel/lessons.py",
      "find": "    (\"hard_regression\", \"reject\", \"hard_regression\"),\n    (\"benefit\", \"reject\", \"benefit_not_met\"),",
      "replace": "    (\"benefit\", \"reject\", \"benefit_not_met\"),\n    (\"hard_regression\", \"reject\", \"hard_regression\"),",
      "why": "The admission order is fixed (C10): hard regression and negative-transfer constraints are checked before practical benefit. Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "lesson-rejected-still-retrievable",
      "file": "factory_kernel/lessons.py",
      "find": "        \"retrieval_eligible\": result.status == \"admit\",",
      "replace": "        \"retrieval_eligible\": result.status != \"reject\",",
      "why": "Only an admitted lesson is retrieval-eligible; insufficient evidence keeps a proposal a proposal (C10). Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "lesson-blind-role-gets-packet",
      "file": "factory_kernel/lessons.py",
      "find": "    if policy is None or role not in policy.eligible_roles or not current:\n        return False",
      "replace": "    if policy is None or not current:\n        return False",
      "why": "Retrieval eligibility checks admitted status AND current applicability AND role; blind judge roles get nothing (C10). Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "lesson-benefit-without-equal-budget",
      "file": "factory_kernel/lessons.py",
      "find": "            gates[\"benefit\"] = float(observations[\"effect\"]) >= policy.minimum_meaningful_effect and equal_budget",
      "replace": "            gates[\"benefit\"] = float(observations[\"effect\"]) >= policy.minimum_meaningful_effect",
      "why": "Admission requires independently observed benefit under equal total budgets (WP11); an effect bought with more search cost is not benefit. Detector: tests/factory/test_lessons.py"
    },
    {
      "id": "runtime-lesson-admission-skipped",
      "file": "factory_kernel/runtime.py",
      "find": "            outcome[\"lesson_admission\"] = self._evaluate_lesson(outcome[\"lesson_proposal\"], plan, selection)",
      "replace": "            outcome[\"lesson_admission\"] = None",
      "why": "Every investigation records the deterministic admission evaluation of its lesson proposal; without it a proposal has no recorded reason for staying a proposal (WP11). Detector: tests/factory/test_code_experiments.py"
    },
    {
      "id": "transition-phase-skip-allowed",
      "file": "factory_kernel/programme_transition.py",
      "find": "    if step[0] != current:\n        raise TransitionRefused(f\"{event!r} needs phase {step[0]!r}, transition is at {current!r}\")\n    return step[1]",
      "replace": "    return step[1]",
      "why": "C07: the phase sequence is the only legal forward order; an event applied from the wrong phase skips a durable phase. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-stop-permits-release",
      "file": "factory_kernel/programme_transition.py",
      "find": "    if stop and event in STOP_REFUSED_EVENTS:\n        raise TransitionRefused(f\"stop is asserted: {event!r} starts a new effect or retires work and is refused\")\n",
      "replace": "",
      "why": "SPEC 7: stop permits read-only reconciliation but prohibits every new remote request (fence, successor, release) and retirement. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-uncertain-recovered-by-any-event",
      "file": "factory_kernel/programme_transition.py",
      "find": "        if event == \"observe_no_effect\":\n            return before\n        raise TransitionRefused(f\"an uncertain {request!r} is resolved only by observing its effect or its definite absence\")",
      "replace": "        return before",
      "why": "C07: an uncertain remote request is resolved only by the recovery observer establishing the observed state or a definite no-effect refusal; any other event escaping uncertainty invents an outcome. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-release-unknown-passes",
      "file": "factory_kernel/programme_transition.py",
      "find": "def _known_bool(value: Any) -> bool | None:\n    return value if isinstance(value, bool) else None",
      "replace": "def _known_bool(value: Any) -> bool | None:\n    return True if value is None else (value if isinstance(value, bool) else None)",
      "why": "C07 last paragraph: if any release precondition becomes unknown, keep the fence. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-release-ignores-queued",
      "file": "factory_kernel/programme_transition.py",
      "find": "        \"drained\": queued == 0 and active == 0,",
      "replace": "        \"drained\": active == 0,",
      "why": "C07: drain requires independently observing queued AND active old-generation runs. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-release-consent-after-successor",
      "file": "factory_kernel/programme_transition.py",
      "find": "    (\"consent\", \"consent_invalid\"),\n    (\"successor\", \"successor_unobserved\"),",
      "replace": "    (\"successor\", \"successor_unobserved\"),\n    (\"consent\", \"consent_invalid\"),",
      "why": "Refusal precedence is fixed and reviewable (conformance/README transition_release): consent is judged before the successor. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-store-ahead-wins",
      "file": "factory_kernel/programme_transition.py",
      "find": "        if store_state in {\"started\", \"uncertain\"}:\n            return \"consistent\" if store_state == \"started\" else \"uncertain\"\n        return RECONCILIATION_REQUIRED",
      "replace": "        if store_state in {\"started\", \"uncertain\"}:\n            return \"consistent\" if store_state == \"started\" else \"uncertain\"\n        return \"consistent\"",
      "why": "C07: a journal/store mismatch after a crash means reconciliation_required, not selecting whichever store appears further ahead. Detector: tests/factory/test_programme_transition.py"
    },
    {
      "id": "transition-journal-observation-under-other-request",
      "file": "factory_kernel/transition_journal.py",
      "find": "                if open_request[\"request_id\"] != request_id or open_request[\"request_sha256\"] != request_sha256:\n                    raise IntentRefused(\"an observation must reuse the pending request's id and digest; a different request is a new transition, not a recovery\")\n",
      "replace": "",
      "why": "C07: reuse the same request id/digest when observing; an observation recorded under another request id closes a request that was never made. Detector: tests/factory/test_transition_journal.py"
    },
    {
      "id": "transition-verify-accepts-rewritten-phase",
      "file": "factory_kernel/transition_journal.py",
      "find": "        if receipt.get(\"phase_after\") != phase:\n            raise IntentRefused(f\"transition receipt {index} records phase {receipt.get('phase_after')!r}, derived {phase!r}\")\n",
      "replace": "",
      "why": "The verifier derives the phase from the legal sequence and refuses a receipt whose recorded phase differs; otherwise a rewritten receipt could advance a transition on paper. Detector: tests/factory/test_transition_journal.py"
    },
    {
      "id": "transition-status-writes-on-refusal",
      "file": "factory_kernel/transition_views.py",
      "find": "        print(f\"FACTORY_TRANSITION_REFUSED reason={str(exc)[:300]!r}\", flush=True)\n        return 1",
      "replace": "        print(f\"FACTORY_TRANSITION_REFUSED reason={str(exc)[:300]!r}\", flush=True)\n        _write(args.output, {\"schema\": \"dark-factory/transition-status\", \"refused\": str(exc)[:300]})\n        return 1",
      "why": "A refused projection writes no record: a partial status file could be read as a phase or release decision. Detector: tests/factory/test_transition_views.py"
    },
    {
      "id": "tcb-unclassified-module-ignored",
      "file": "factory_kernel/tcb.py",
      "find": "    if unclassified:\n        raise TcbRefused(f\"unclassified kernel modules: {unclassified}\")\n",
      "replace": "",
      "why": "WP06: every kernel module is classified in .factory/tcb.json; a new module outside the record would carry no TCB class. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "tcb-graph-drift-ignored",
      "file": "factory_kernel/tcb.py",
      "find": "    if graph_sha != record[\"graph_sha256\"]:\n",
      "replace": "    if False:\n",
      "why": "WP06/SPEC 15.1: the import closure is pinned; a new import edge must be described by a reviewed record change. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "tcb-unpermitted-root-import",
      "file": "factory_kernel/tcb.py",
      "find": "        if roots & set(deps) and name not in permitted:\n",
      "replace": "        if False:\n",
      "why": "WP06: privileged roots (credentials, mutation) are imported only by permitted entrypoints. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "tcb-unrecorded-violation-accepted",
      "file": "factory_kernel/tcb.py",
      "find": "    unknown = sorted(found - known)\n    if unknown:\n",
      "replace": "    unknown = sorted(found - known)\n    if False:\n",
      "why": "WP06: a proposal or ui module reaching a credential-bearing root by import is refused unless recorded as a known violation. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "tcb-stale-violation-kept",
      "file": "factory_kernel/tcb.py",
      "find": "    stale = sorted(known - found)\n    if stale:\n",
      "replace": "    stale = sorted(known - found)\n    if False:\n",
      "why": "WP06: the violation allowance only shrinks; a recorded violation that no longer exists must be removed, or the record silently permits its return. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "tcb-closure-bound-ignored",
      "file": "factory_kernel/tcb.py",
      "find": "        if total > bounds[\"total_max_bytes\"]:\n",
      "replace": "        if False:\n",
      "why": "SPEC 15.1: the authority closure the host reads is bounded in total bytes; exceeding it becomes a host refusal, not a silent pass. Detector: tests/factory/test_tcb.py"
    },
    {
      "id": "cli-spike-credential-leak-ignored",
      "file": "harness/cli_compatibility_spike.py",
      "find": "        if r[\"credential_in_output\"] or any(q[\"credential_in_body\"] for q in r[\"requests\"]):\n",
      "replace": "        if False:\n",
      "why": "SPEC 5: the spike checks authentication redaction; a CLI that prints or posts the credential must fail the gate. Detector: tests/factory/test_cli_compatibility_spike.py"
    },
    {
      "id": "cli-spike-echoed-prompt-accepted",
      "file": "harness/cli_compatibility_spike.py",
      "find": "    if not plain or not plain[\"requests\"] or plain[\"exit_code\"] != 0 or plain.get(\"result_text\") != PLAIN_SENTINEL:\n",
      "replace": "    if not plain or not plain[\"requests\"] or plain[\"exit_code\"] != 0:\n",
      "why": "SPEC 5: the plain exchange proves the CLI returned the provider's answer through the channel; accepting any exit 0 (an echoed prompt, an unrelated answer) passes a CLI that never used the channel. Detector: tests/factory/test_cli_compatibility_spike.py"
    },
    {
      "id": "cli-spike-tool-exchange-not-required",
      "file": "harness/cli_compatibility_spike.py",
      "find": "    if not tool or not any(q[\"tool_result\"] for q in tool[\"requests\"]):\n",
      "replace": "    if False:\n",
      "why": "SPEC 5: tool exchanges are part of the compatibility contract; a CLI that never returns a tool_result through the channel is incompatible. Detector: tests/factory/test_cli_compatibility_spike.py"
    },
    {
      "id": "cli-spike-missing-binary-passes",
      "file": "harness/cli_compatibility_spike.py",
      "find": "        print(f\"CLI_COMPATIBILITY_FAILED reason=binary_missing binary={args.binary}\")\n        return 1\n",
      "replace": "        print(f\"CLI_COMPATIBILITY_FAILED reason=binary_missing binary={args.binary}\")\n        return 0\n",
      "why": "SPEC 5: the gate fails explicitly; a missing CLI cannot pass it. Detector: tests/factory/test_cli_compatibility_spike.py"
    },
    {
      "id": "cli-spike-retry-counts-probe",
      "file": "harness/cli_compatibility_spike.py",
      "find": "    if not retry or sum(1 for q in retry[\"requests\"] if q[\"on_channel\"]) < 2 or retry[\"exit_code\"] != 0:\n",
      "replace": "    if not retry or len(retry[\"requests\"]) < 2 or retry[\"exit_code\"] != 0:\n",
      "why": "SPEC 5: the retry check counts model requests on the channel; counting every recorded request lets a CLI that probes once and never retries a 529 pass. Detector: tests/factory/test_cli_compatibility_spike.py"
    },
    {
      "id": "gateway-server-token-unchecked",
      "file": "factory_kernel/gateway_server.py",
      "find": "        if not presented or sha256_bytes(presented.encode()) != sha256_bytes(value.shared_token.encode()):\n",
      "replace": "        if False:\n",
      "why": "SPEC 6.2: the channel token is the only thing the process under validation holds; a gateway that answers without it would transmit for anyone on the loopback. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "gateway-server-redirect-followed",
      "file": "factory_kernel/gateway_server.py",
      "find": "        opener = urllib.request.build_opener(NoRedirect())\n",
      "replace": "        opener = urllib.request.build_opener()\n",
      "why": "SPEC 6.2: the transport never follows a redirect (the CLI spike showed a client forwarding its credential cross-origin on a 307); a gateway that follows one sends the injected credential wherever the upstream points. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "gateway-server-credential-kept-after-close",
      "file": "factory_kernel/gateway_server.py",
      "find": "    sessions = {f\"{m} {p}\": close_session(c.session) for (m, p), c in value.channels.items()}\n",
      "replace": "    sessions = {f\"{m} {p}\": c.session.to_dict() for (m, p), c in value.channels.items()}\n",
      "why": "SPEC 6.2: closing the channels drops the credential closure; a close that only summarises leaves the credential reachable from the session objects. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "gateway-server-query-widens-route",
      "file": "factory_kernel/gateway_server.py",
      "find": "        path = self.path.split(\"?\", 1)[0]\n",
      "replace": "        path = self.path\n",
      "why": "SPEC 6.2: a route is a method and path; the query string neither widens nor denies it (the CLI posts /v1/messages?beta=true). Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "gateway-server-plain-http-origin-accepted",
      "file": "factory_kernel/gateway_server.py",
      "find": "    if not isinstance(origin, str) or not origin.startswith(\"https://\") or origin.endswith(\"/\") or \"?\" in origin:\n",
      "replace": "    if not isinstance(origin, str) or origin.endswith(\"/\") or \"?\" in origin:\n",
      "why": "SPEC 6.2: the preregistered upstream is an https origin; a policy naming a plain-http origin would put the injected credential on the wire in clear. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "appproc-gateway-missing-policy-falls-back",
      "file": "harness/appproc.py",
      "find": "        if policy is None:\n            raise AppDidNotStart(\"FACTORY_VALIDATION_GATEWAY=1 but .factory/kernel.json has no validation.gateway policy \"\n",
      "replace": "        if policy is None:\n            return None\n            raise AppDidNotStart(\"FACTORY_VALIDATION_GATEWAY=1 but .factory/kernel.json has no validation.gateway policy \"\n",
      "why": "SPEC 6.2: asking for the gateway without the owner's preregistered routes and ceilings refuses the run; a launcher that quietly starts the app with the credential instead has removed the boundary it was asked for. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "appproc-gateway-child-keeps-real-key",
      "file": "harness/appproc.py",
      "find": "        env.update(self.gateway.process_environment(base_url_variable=\"OPENROUTER_BASE_URL\", key_variable=\"OPENROUTER_API_KEY\"))\n",
      "replace": "        env.update({\"OPENROUTER_BASE_URL\": self.gateway.base_url + \"/v1\"})\n",
      "why": "SPEC 6.2: the process under validation receives the channel token, never the credential; a launcher that only redirects the base URL leaves the real key in the child's environment. Detector: tests/factory/test_gateway_server.py"
    },
    {
      "id": "capabilities-evidence-head-unchecked",
      "file": "factory_kernel/capabilities.py",
      "find": "            for key, evidence_key in ((\"head_sha\", \"head_sha\"), (\"base_sha\", \"base_sha\"), (\"head_tree_sha\", \"head_tree_sha\"),\n",
      "replace": "            for key, evidence_key in ((\"base_sha\", \"base_sha\"), (\"head_tree_sha\", \"head_tree_sha\"),\n",
      "why": "SPEC 5 / WP06 acceptance: orchestration that lies about the subject gets no grant; a grant issued for a head the authority never judged would let the broker merge an unjudged head. Detector: tests/factory/test_capabilities.py"
    },
    {
      "id": "capabilities-expiry-ignored",
      "file": "factory_kernel/capabilities.py",
      "find": "    elif now >= grant.expires_at:\n        reasons.append(\"grant_expired\")\n",
      "replace": "    elif False:\n        reasons.append(\"grant_expired\")\n",
      "why": "SPEC 5: a grant carries an expiry so an old authorisation cannot be spent later against a moved world; an expired grant that still passes is a replayable credential. Detector: tests/factory/test_capabilities.py, tests/factory/test_effect_broker.py"
    },
    {
      "id": "capabilities-uses-unbounded",
      "file": "factory_kernel/capabilities.py",
      "find": "    elif uses >= grant.max_uses:\n        reasons.append(\"grant_uses_exhausted\")\n",
      "replace": "    elif False:\n        reasons.append(\"grant_uses_exhausted\")\n",
      "why": "SPEC 5: maximum uses is a grant field; a grant that can be spent without bound is a standing capability, not an exact one-use authorisation. Detector: tests/factory/test_capabilities.py, tests/factory/test_effect_broker.py"
    },
    {
      "id": "effect-broker-pending-reissued",
      "file": "factory_kernel/effect_broker.py",
      "find": "        if state in (\"started\", \"uncertain\"):\n            reasons.append(\"prior_operation_pending_or_uncertain\")\n",
      "replace": "        if False:\n            reasons.append(\"prior_operation_pending_or_uncertain\")\n",
      "why": "C05: a started or uncertain operation is never reissued; a broker that executes again after a lost response can merge twice or merge what an operator was reconciling. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "effect-broker-head-not-reobserved",
      "file": "factory_kernel/effect_broker.py",
      "find": "                    if observed[\"headRefOid\"] != grant.subject[\"head_sha\"]:\n                        reasons.append(\"subject_head_moved\")\n",
      "replace": "                    if False:\n                        reasons.append(\"subject_head_moved\")\n",
      "why": "C05: the broker independently loads the subject before consuming the grant; trusting the caller's head means a moved PR is merged on a stale authorisation before merge_squash's own check even runs. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "effect-broker-timeout-recorded-as-success",
      "file": "factory_kernel/effect_broker.py",
      "find": "        except subprocess.TimeoutExpired as exc:\n            self.journal.record(\"uncertain\", grant, at=self.clock(), remote_call=True, remote_state=\"unknown\",\n                                detail=f\"merge call timed out: {str(exc)[:300]}\")\n            raise EffectUncertain(f\"merge of PR #{number} at {head} timed out; the remote may have merged. Observe, do not replay.\") from exc\n",
      "replace": "        except subprocess.TimeoutExpired as exc:\n            pass\n",
      "why": "C05: a timeout or lost response is `uncertain`, never success and never a silent continue; recording it as returned would let the post-merge path report a merge nobody observed. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "runtime-merge-bypasses-broker",
      "file": "factory_kernel/runtime.py",
      "find": "        self._merge_squash_through_broker(pr_number, head=head, authorization=authorization, paths=paths)\n",
      "replace": "        self.github.merge_squash(pr_number, expected_head=head)\n",
      "why": "SPEC 5 / WP06: the exact-head merge is a brokered effect: grant derived from the authority's artifact, consumed once in a durable journal, controls rechecked immediately before the call. A direct spend skips all three and leaves no effect journal. Detector: tests/factory/test_factory_programme.py, tests/factory/test_factory_merge_authorized.py"
    },
    {
      "id": "effect-broker-dirty-candidate-published",
      "file": "factory_kernel/effect_broker.py",
      "find": "                if observed[\"dirty\"]:\n                    reasons.append(\"candidate_worktree_dirty\")\n",
      "replace": "                if False:\n                    reasons.append(\"candidate_worktree_dirty\")\n",
      "why": "WP06 acceptance: the broker re-derives dirty paths itself before publishing; a dirty worktree means the pushed commit is not what the run judged. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "effect-broker-candidate-head-unchecked",
      "file": "factory_kernel/effect_broker.py",
      "find": "                if observed[\"head_sha\"] != grant.subject[\"head_sha\"]:\n                    reasons.append(\"candidate_head_differs\")\n",
      "replace": "                if False:\n                    reasons.append(\"candidate_head_differs\")\n",
      "why": "WP06 acceptance: commit identity is re-derived, never taken from the caller; a worktree whose HEAD moved after the grant would publish an unjudged commit under a judged grant. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "effect-broker-created-pr-head-unchecked",
      "file": "factory_kernel/effect_broker.py",
      "find": "        if observation[\"headRefOid\"] not in (None, head):\n",
      "replace": "        if False:\n",
      "why": "C05: the effect is independently observed after the call; a PR reported at another head than the granted one is not the published candidate and must not be recorded as success. Detector: tests/factory/test_effect_broker.py"
    },
    {
      "id": "runtime-publication-stop-check-noop",
      "file": "factory_kernel/runtime.py",
      "find": "            stop_check=self.check_stop, epoch=str(DEFAULT_POLICY[\"revocation_epoch\"]), git=self._git,\n",
      "replace": "            stop_check=lambda: None, epoch=str(DEFAULT_POLICY[\"revocation_epoch\"]), git=self._git,\n",
      "why": "A stop or fence arriving between the publication step's first check and the push would be missed if the runtime handed the broker a no-op control check; the broker's recheck is the last observation before the branch and PR exist. Detector: tests/factory/test_factory_merge_handoff.py"
    },
    {
      "id": "experiments-unrunnable-family-runs",
      "file": "factory_kernel/experiments.py",
      "find": "    if not row[\"runnable\"]:\n        raise IntentRefused(f\"experiment family {spec['kind']} is registered but not runnable: {row['not_runnable_because']}\")\n",
      "replace": "    if False:\n        raise IntentRefused(f\"experiment family {spec['kind']} is registered but not runnable: {row['not_runnable_because']}\")\n",
      "why": "SPEC 8.2: a registration is not a capability; a family that needs an isolated probe view or a disposable database must refuse before any budget is reserved and say why, never fall through to another family's validator. Detector: tests/factory/test_experiments.py"
    },
    {
      "id": "experiments-unanalysed-paths-dropped",
      "file": "factory_kernel/experiments.py",
      "find": "        unanalysed = [p for p in strategy[\"touched_paths\"] if p not in files]\n",
      "replace": "        unanalysed = []\n",
      "why": "SPEC 8.2: coverage gaps are explicit; a touched path the frozen context does not hold is counted as unanalysed, never silently dropped so the metrics look complete. Detector: tests/factory/test_experiments.py"
    },
    {
      "id": "experiments-upward-import-not-a-violation",
      "file": "factory_kernel/experiments.py",
      "find": "            if src_layer is None or dst_layer is None or dst_layer < src_layer:\n",
      "replace": "            if src_layer is None or dst_layer is None:\n",
      "why": "repository-boundary-v1: an import from an inner layer to an outer one is the layer violation the family measures; without it the metric cannot falsify a layering claim. Detector: tests/factory/test_experiments.py, tests/factory/test_exploration.py"
    },
    {
      "id": "experiments-cycles-not-counted",
      "file": "factory_kernel/experiments.py",
      "find": "            if len(component) > 1 or node in edges.get(node, ()):\n                cycles[0] += 1\n",
      "replace": "            if False:\n                cycles[0] += 1\n",
      "why": "repository-boundary-v1: import cycles among the touched files are a registered metric; a runner that never counts them reports a clean boundary for a cyclic change. Detector: tests/factory/test_experiments.py"
    },
    {
      "id": "experiments-ambiguous-import-guessed",
      "file": "factory_kernel/experiments.py",
      "find": "        if matches:\n            return None\n",
      "replace": "        if matches:\n            return matches[0]\n",
      "why": "SPEC 8.2: lexical imports are never claimed to be a semantic module graph; a dotted name that matches several context modules names nothing rather than the first one found. Detector: tests/factory/test_experiments.py"
    },
    {
      "id": "exploration-metrics-not-bound-to-family",
      "file": "factory_kernel/exploration.py",
      "find": "            metrics = metrics_for(request[\"probe\"][\"kind\"])\n",
      "replace": "            metrics = metrics_for(request[\"probe\"][\"kind\"]) | metrics_for(\"lookup-workload-v1\")\n",
      "why": "SPEC 8: units must match criteria and the family that measures them; a target naming another family's metric on this spec would bind a measurement the runner never produces. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-candidate-not-bound-to-strategy",
      "file": "factory_kernel/exploration.py",
      "find": "                        or strategy_of(request[\"probe\"], candidate) is None\n",
      "replace": "                        or False\n",
      "why": "SPEC 8: every measurement target binds a candidate to the strategy the spec measures for it; an unbound candidate has no result to read and must be refused before reservation, not discovered after the run. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "predictions-stale-context-still-applicable",
      "file": "factory_kernel/predictions.py",
      "find": "    if not record[\"applicability\"][\"applicable\"] or record[\"applicability\"][\"context_identity\"] != context_identity:\n        return {**base, \"outcome\": \"inapplicable\",",
      "replace": "    if False:\n        return {**base, \"outcome\": \"inapplicable\",",
      "why": "SPEC 8.3: actuals are matched by exact subject and measurement contract under the forecast's data cutoff; a forecast made under another repository context cannot be scored within or outside by a measurement taken under this one. Detector: tests/factory/test_predictions.py"
    },
    {
      "id": "predictions-judgment-scored-as-number",
      "file": "factory_kernel/predictions.py",
      "find": "    if record[\"kind\"] != \"interval\":\n        return {**base, \"outcome\": \"not-comparable\", \"detail\": \"a judgment is not a number\"}\n",
      "replace": "    if False:\n        return {**base, \"outcome\": \"not-comparable\", \"detail\": \"a judgment is not a number\"}\n",
      "why": "SPEC 8.3: a qualitative judgment is stored as an uncalibrated judgment and is never compared to a measured number; scoring it would turn an opinion into a measurement outcome. Detector: tests/factory/test_predictions.py"
    },
    {
      "id": "predictions-unit-mismatch-compared",
      "file": "factory_kernel/predictions.py",
      "find": "    if record[\"measurement_contract\"][\"unit\"] != unit:\n        return {**base, \"outcome\": \"not-comparable\", \"detail\": \"measured unit differs from the forecast's contract\"}\n",
      "replace": "    if False:\n        return {**base, \"outcome\": \"not-comparable\", \"detail\": \"measured unit differs from the forecast's contract\"}\n",
      "why": "SPEC 8.3: the measurement contract (the criterion's unit) is part of what a forecast is matched by; a value in another unit is not the actual the forecast was about. Detector: tests/factory/test_predictions.py"
    },
    {
      "id": "predictions-later-round-assessment-taken-as-frozen",
      "file": "factory_kernel/predictions.py",
      "find": "            if assessment[\"round\"] == session[\"round\"] and key in assessment[\"candidates\"]:\n                predictions = assessment[\"candidates\"][key]\n                source = \"round-assessment\"\n",
      "replace": "            if key in assessment[\"candidates\"]:\n                predictions = assessment[\"candidates\"][key]\n                source = \"round-assessment\"\n",
      "why": "SPEC 8.3: the forecast in force is the one frozen for the current round before its results; an assessment from another round is not it, so a stale or post-hoc revision must not be scored as the forecast. Detector: tests/factory/test_predictions.py"
    },
    {
      "id": "exploration-prediction-outcomes-dropped",
      "file": "factory_kernel/exploration.py",
      "find": "            prediction_outcomes = outcomes_for(session, measurements=measurements,\n                                               context_identity=session[\"context\"][\"identity\"],\n                                               receipt_sha256=sha256_value(receipt))\n",
      "replace": "            prediction_outcomes = []\n",
      "why": "SPEC 8.3 / WP11: every measured actual is matched against the forecast frozen before it and both are retained side by side; an observation that records measurements without their forecast outcomes loses the calibration record the learning layer needs. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "outcome-router-unauthenticated-receipt-classified",
      "file": "factory_kernel/outcome_router.py",
      "find": "    if not isinstance(authenticated_receipt, Mapping) or authenticated_receipt.get(\"provenance\") != AUTHENTICATED_PROVENANCE:\n        gaps.append(\"receipt_not_authenticated\")\n",
      "replace": "    if False:\n        gaps.append(\"receipt_not_authenticated\")\n",
      "why": "SPEC 9: only an authenticated outcome may be classified; a hand-written observation carrying a reason code would otherwise become an implementation defect or a contradiction with no platform provenance. Detector: tests/factory/test_outcome_router.py"
    },
    {
      "id": "outcome-router-environment-code-judges-strategy",
      "file": "factory_kernel/outcome_router.py",
      "find": "    if code in ENVIRONMENT_CODES:\n        return record(\"environment-provider-failure\", detail=f\"the refusal was reported by {AUTHORITY[code]}; the strategy is unjudged\")\n",
      "replace": "    if False:\n        return record(\"environment-provider-failure\", detail=f\"the refusal was reported by {AUTHORITY[code]}; the strategy is unjudged\")\n",
      "why": "WP09 acceptance: a provider or identity failure leaves the strategy unjudged; treating it as unknown or as a defect attributes a cause no authority reported. Detector: tests/factory/test_outcome_router.py"
    },
    {
      "id": "outcome-router-unresolved-finding-is-a-defect",
      "file": "factory_kernel/outcome_router.py",
      "find": "        if any(row.get(\"status\") != \"supported\" for row in findings):\n            return record(\"evidence-gap\", detail=\"a registered predicate is unresolved; the strategy question stays open\")\n",
      "replace": "        if False:\n            return record(\"evidence-gap\", detail=\"a registered predicate is unresolved; the strategy question stays open\")\n",
      "why": "SPEC 9: unknown stays unknown; an unresolved predicate is an evidence gap, and falling through to the refusal code would classify the strategy question as an implementation defect the facts never decided. Detector: tests/factory/test_outcome_router.py"
    },
    {
      "id": "outcome-router-policy-change-ignored",
      "file": "factory_kernel/outcome_router.py",
      "find": "        if registered_policy is not None and facts.get(\"policy_sha256\") not in (None, registered_policy):\n",
      "replace": "        if False:\n",
      "why": "SPEC 9: a rule registered under one protected policy cannot be judged by facts observed under another; a changed rule or policy after the outcome refuses a verdict. Detector: tests/factory/test_outcome_router.py"
    },
    {
      "id": "strategy-rejection-classification-not-recorded",
      "file": "factory_kernel/strategy_rejection.py",
      "find": "                \"classification\": deepcopy(classification),\n",
      "replace": "                \"classification\": None,\n",
      "why": "SPEC 9: the assessment records what the outcome is evidence of, from structure; an assessment without its classification leaves the reconsideration and the retained alternatives with a verdict but no cause class. Detector: tests/factory/test_strategy_rejection.py"
    },
    {
      "id": "graph-transport-not-owner-only",
      "file": "factory_kernel/graph_transport.py",
      "find": "    if principal.role != \"owner\":\n        raise IntentRefused(\"the project graph is owner-only\")\n",
      "replace": "    if False:\n        raise IntentRefused(\"the project graph is owner-only\")\n",
      "why": "C11: the graph is a role-scoped projection; a proposal principal reading the owner's private intent and exploration wording is a privacy breach, not a view. Detector: tests/factory/test_project_graph_http.py"
    },
    {
      "id": "graph-transport-cursor-ahead-served-as-delta",
      "file": "factory_kernel/graph_transport.py",
      "find": "    if after_version > current:\n        return {**base, \"kind\": \"resnapshot\", \"reason\": \"cursor-ahead-of-project\", \"after_version\": after_version,\n",
      "replace": "    if False:\n        return {**base, \"kind\": \"resnapshot\", \"reason\": \"cursor-ahead-of-project\", \"after_version\": after_version,\n",
      "why": "C11: a stale or impossible cursor returns an explicit resnapshot result rather than silently mixing snapshots; a cursor ahead of the project answered with an empty delta would tell the client its view is current when it is from another lineage. Detector: tests/factory/test_project_graph_http.py"
    },
    {
      "id": "graph-transport-delta-span-unbounded",
      "file": "factory_kernel/graph_transport.py",
      "find": "    if current - after_version > MAX_DELTA_SPAN:\n",
      "replace": "    if False:\n",
      "why": "SPEC 10: a cursor gap beyond the bound returns a bounded resnapshot instruction; an unbounded delta re-projects arbitrarily old lineages on every poll. Detector: tests/factory/test_project_graph_http.py"
    },
    {
      "id": "graph-transport-details-before-authorization",
      "file": "factory_kernel/graph_transport.py",
      "find": "    events = _events(store, project, principal=principal)\n    if not isinstance(node_id, str) or not 1 <= len(node_id) <= 200:\n        raise GraphRefused(\"node id must be an opaque string\")\n",
      "replace": "    if not isinstance(node_id, str) or not 1 <= len(node_id) <= 200:\n        raise GraphRefused(\"node id must be an opaque string\")\n    events = _events(store, project, principal=principal)\n",
      "why": "C11: detail lookup checks authorisation before object existence or shape; answering about the id first tells an unauthorised caller something about the graph. Detector: tests/factory/test_project_graph_http.py"
    },
    {
      "id": "frontdoor-http-query-refusal-dropped",
      "file": "factory_kernel/frontdoor_http.py",
      "find": "            else:\n                return send(\"400 Bad Request\", {\"error\": \"query parameters are not supported\"})\n",
      "replace": "            else:\n                pass\n",
      "why": "The Front Door refuses query parameters on every route except the two graph routes; dropping the refusal lets a query string reach routes whose handlers never validated one. Detector: tests/factory/test_project_graph_http.py, tests/factory/test_frontdoor_http.py"
    },
    {
      "id": "calibration-forecast-joined-from-final-state",
      "file": "factory_kernel/calibration.py",
      "find": "        state = projection(list(events[:index]))\n",
      "replace": "        state = projection(list(events))\n",
      "why": "SPEC 11.1 / WP11 acceptance: a future outcome cannot leak into a prior forecast. Joining against the final projection lets an assessment recorded after the observation supply the forecast the outcome is scored against. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-duplicate-trajectory-counted-twice",
      "file": "factory_kernel/calibration.py",
      "find": "            if key in seen:\n                duplicates += 1\n                continue\n",
      "replace": "            if False:\n                duplicates += 1\n                continue\n",
      "why": "WP11 acceptance: duplicated runs are counted once. A copy of the same observation must not add a sample. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-unknown-cost-becomes-zero",
      "file": "factory_kernel/calibration.py",
      "find": "    if reservation.get(\"calls\") == 0 and reservation.get(\"usd\") == 0:\n        return 0\n    return None\n",
      "replace": "    if reservation.get(\"calls\") == 0 and reservation.get(\"usd\") == 0:\n        return 0\n    return 0\n",
      "why": "WP11 acceptance: missing cost stays unknown. An unreported cost after a paid call recorded as zero would make the known sum a fiction. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-unknown-outcome-scored-as-covered",
      "file": "factory_kernel/calibration.py",
      "find": "    compared = counts[\"within\"] + counts[\"outside\"]\n",
      "replace": "    compared = counts[\"within\"] + counts[\"outside\"] + counts[\"unknown\"]\n",
      "why": "SPEC 11.1: unknown-outcome rates are reported beside coverage, never folded into it; an unknown is not a compared interval. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-contamination-ignored",
      "file": "factory_kernel/calibration.py",
      "find": "    contaminated = any(receipts_by_cohort[a] & receipts_by_cohort[b] for i, a in enumerate(names) for b in names[i + 1:])\n",
      "replace": "    contaminated = False\n",
      "why": "SPEC 11.1: do not train/test on copies of the same task trajectory. The same receipt in two cohorts must mark the split contaminated so the admission evaluator's clean_split gate can refuse it. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-brier-not-squared",
      "file": "factory_kernel/calibration.py",
      "find": "        total += (Fraction(probability) - int(outcome)) ** 2\n",
      "replace": "        total += abs(Fraction(probability) - int(outcome))\n",
      "why": "SPEC 11.1 names the Brier score for binary outcomes: the mean squared error, computed exactly. Detector: tests/factory/test_calibration.py"
    },
    {
      "id": "calibration-unmeasured-candidate-invented",
      "file": "factory_kernel/calibration.py",
      "find": "    final = projection(list(events))\n    measured = {(r[\"session_id\"], r[\"candidate_id\"]) for r in rows} | {(u[\"session_id\"], u[\"candidate_id\"]) for u in unjoined}\n",
      "replace": "    final = projection(list(events))\n    measured = {(r[\"session_id\"], r[\"candidate_id\"]) for r in rows} | {(u[\"session_id\"], u[\"candidate_id\"]) for u in unjoined}\n    for sid, session in final[\"sessions\"].items():\n        for cid in session[\"candidates\"]:\n            if (sid, cid) not in measured:\n                rows.append({\"project\": project, \"session_id\": sid, \"round\": session[\"round\"], \"event_index\": None, \"recorded_at\": None,\n                             \"receipt_sha256\": None, \"candidate_id\": cid, \"criterion_id\": None, \"task_family\": session[\"candidates\"][cid].get(\"family\"),\n                             \"method_version\": \"unknown\", \"model\": UNRECORDED_MODEL, \"status\": \"complete\", \"failure\": None,\n                             \"outcome\": \"within\", \"distance\": 0, \"actual\": {\"value\": 0}, \"cost_usd\": 0, \"forecast\": None})\n                measured.add((sid, cid))\n",
      "why": "WP11 acceptance: an unbuilt or unmeasured candidate has no invented actual. A row fabricated for it would be a sample nothing measured. Detector: tests/factory/test_calibration.py"
    }
  ]
}
