{
  "_what_this_is": [
    "Adversarial regressions for the durable builder-provenance and protected 21-claim evidence spine.",
    "Each mutation is injected only into an isolated copied trust root and must make the focused authority suite fail."
  ],
  "defects": [
    {
      "id": "preflight-decision-budget-not-applied",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "request = replace(request, max_budget_usd=min(reserved, request.max_budget_usd),\n                              timeout_seconds=min(remaining, request.timeout_seconds))",
      "replace": "request = request",
      "why": "test_preflight_prepare requires both requests to respect the smaller per-decision dollar and wall reservation."
    },
    {
      "id": "preflight-registration-binding-disabled",
      "file": "factory_kernel/preflight.py",
      "find": "value[\"registration_sha256\"] != sha256_value(registration) or value[\"candidates_sha256\"] != sha256_value(pool)",
      "replace": "False or value[\"candidates_sha256\"] != sha256_value(pool)",
      "why": "test_preflight rejects criteria changed after the frozen comparison was challenged."
    },
    {
      "id": "preflight-candidate-binding-disabled",
      "file": "factory_kernel/preflight.py",
      "find": "or value[\"candidates_sha256\"] != sha256_value(pool):",
      "replace": "or False:",
      "why": "test_preflight rejects a candidate rewritten after its assessment."
    },
    {
      "id": "preflight-uncertainty-ignored",
      "file": "factory_kernel/preflight.py",
      "find": "blocked |= uncertain",
      "replace": "blocked |= False if assessment[\"uncertainties\"] else uncertain",
      "why": "test_preflight retains a plausible reversal on the losing candidate instead of manufacturing dominance."
    },
    {
      "id": "preflight-judgment-promoted-to-dominance",
      "file": "factory_kernel/preflight.py",
      "find": "if not blocked and not has_judgment:",
      "replace": "if not blocked:",
      "why": "test_preflight permits provisional preferences but does not turn opinions into established Pareto facts."
    },
    {
      "id": "preflight-hard-conflict-traded-away",
      "file": "factory_kernel/preflight.py",
      "find": "conflicts = [row[\"id\"] for row in assessment[\"constraints\"] if row[\"status\"] == \"conflict\"]",
      "replace": "conflicts = []",
      "why": "test_preflight screens out a constraint conflict even when it has the smallest plan."
    },
    {
      "id": "preflight-registered-priority-ignored",
      "file": "factory_kernel/preflight.py",
      "find": "for criterion in policy[\"priorities\"]:",
      "replace": "for criterion in []:",
      "why": "test_preflight resolves a real tradeoff only with the registered priority and records why the alternative lost."
    },
    {
      "id": "preflight-baseline-erases-tradeoff",
      "file": "factory_kernel/preflight.py",
      "find": "and all(vectors[key] == vectors[remaining[0]] for key in remaining)):",
      "replace": "and True):",
      "why": "test_preflight permits baseline preference only on equal assessed dimensions."
    },
    {
      "id": "preflight-replay-identity-unbound",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "if record[\"identity\"] != identity or record[\"registration\"] != registration:",
      "replace": "if record[\"registration\"] != registration:",
      "why": "test_preflight_prepare refuses a new request key for the same frozen decision."
    },
    {
      "id": "preflight-final-repository-currency-disabled",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "if current != review or validate_context(self.context()) != context:",
      "replace": "if False:",
      "why": "test_preflight_prepare withholds the handoff when the repository changed during the final challenge."
    },
    {
      "id": "preflight-final-stop-disabled",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "def _finish(self, project, command, principal, review, context, record, selected):\n        self.check_stop()",
      "replace": "def _finish(self, project, command, principal, review, context, record, selected):\n        pass",
      "why": "test_preflight_prepare checks stop again immediately before emitting an advisory handoff."
    },
    {
      "id": "preflight-unknown-spend-repeated",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "if type(cost) not in {int, float} or not math.isfinite(cost) or not 0 <= cost <= reserved:",
      "replace": "if False:",
      "why": "test_preflight_prepare never deepens exploration after unknown or over-reserved spend."
    },
    {
      "id": "preflight-total-wall-ignored",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "if self.clock() > deadline:",
      "replace": "if False:",
      "why": "test_preflight_prepare refuses a challenge returned after the cumulative decision deadline."
    },
    {
      "id": "preflight-handoff-self-qualifies",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "\"qualification_status\": \"UNPROVEN\", \"proof_reuse_allowed\": False,\n                \"activation\":",
      "replace": "\"qualification_status\": \"QUALIFIED\", \"proof_reuse_allowed\": False,\n                \"activation\":",
      "why": "test_preflight_prepare requires every recommendation to enter normal admission as UNPROVEN."
    },
    {
      "id": "preflight-generated-deep-trigger-ignored",
      "file": "factory_kernel/preflight_prepare.py",
      "find": "if set(record[\"observed_signals\"]) & DEEP_SIGNALS:",
      "replace": "if False:",
      "why": "test_preflight_prepare stops before more spend when the generated plan exposes a security-sensitive surface."
    },
    {
      "id": "evidence-retention-dispatch-failure-masked",
      "file": ".github/workflows/dark-factory-worker.yml",
      "find": "        id: dispatch\n",
      "replace": "        id: dispatch\n        continue-on-error: true\n",
      "why": "test_factory_continuation permits only named retention steps to ignore errors, never actual dispatch."
    },
    {
      "id": "evidence-retention-symlink-read",
      "file": "factory_kernel/evidence_retention.py",
      "find": "if path.is_symlink():",
      "replace": "if False:",
      "why": "test_evidence_retention refuses evidence read through a symlinked parent."
    },
    {
      "id": "evidence-retention-byte-bound-disabled",
      "file": "factory_kernel/evidence_retention.py",
      "find": "if total + len(raw) > MAX_BYTES or len(files) >= MAX_FILES:",
      "replace": "if len(files) >= MAX_FILES:",
      "why": "test_evidence_retention requires byte omissions even below the file-count bound."
    },
    {
      "id": "evidence-retention-source-binding-disabled",
      "file": "factory_kernel/evidence_retention.py",
      "find": "or value.get(\"source\") != binding):",
      "replace": "or False):",
      "why": "test_evidence_retention refuses indexes from a different run, attempt, revision or phase."
    },
    {
      "id": "evidence-retention-proof-upgrade-accepted",
      "file": "factory_kernel/evidence_retention.py",
      "find": "or value.get(\"proof_reuse_allowed\") is not False",
      "replace": "or False",
      "why": "test_evidence_retention refuses an index claiming proof-reuse authority."
    },
    {
      "id": "evidence-retention-duplicate-path-accepted",
      "file": "factory_kernel/evidence_retention.py",
      "find": "or parts[1] not in EVIDENCE_PATHS or name in seen):",
      "replace": "or parts[1] not in EVIDENCE_PATHS):",
      "why": "test_evidence_retention requires unique retained evidence identities."
    },
    {
      "id": "evidence-retention-platform-head-unbound",
      "file": "factory_kernel/evidence_retention_archive.py",
      "find": "or run[\"head_sha\"] != source[\"head_sha\"] or run[\"head_branch\"] != \"main\"",
      "replace": "or run[\"head_branch\"] != \"main\"",
      "why": "test_evidence_retention refuses an artifact attributed to a different source revision."
    },
    {
      "id": "evidence-retention-expiry-hidden",
      "file": "factory_kernel/evidence_retention_archive.py",
      "find": "if observed[\"expired_at_observation\"]:",
      "replace": "if False:",
      "why": "test_evidence_retention preserves raw expiry and never downloads an expired index."
    },
    {
      "id": "evidence-retention-overwrite-race-hidden",
      "file": "factory_kernel/evidence_retention_archive.py",
      "find": "if artifact_observation(fresh, source=source, name=name, limit=MAX_RECORD + 10000) != observed:",
      "replace": "if False:",
      "why": "test_evidence_retention detects artifact replacement during index download."
    },
    {
      "id": "claim-explanation-stale-identity-hidden",
      "file": "factory_kernel/claim_explanation.py",
      "find": "\"current\" if recorded_value == expected_value else \"stale\"",
      "replace": "\"current\" if True else \"stale\"",
      "why": "tests/factory/test_claim_explanation.py must expose wrong revisions and stale spine policy."
    },
    {
      "id": "claim-explanation-dependency-propagation-disabled",
      "file": "factory_kernel/claim_explanation.py",
      "find": "if state == \"stale\" or prior[\"recorded_dependency_status\"] == \"stale\":",
      "replace": "if False:",
      "why": "tests/factory/test_claim_explanation.py must propagate changed inputs through actual spine edges."
    },
    {
      "id": "claim-explanation-forged-authority-hidden",
      "file": "factory_kernel/claim_explanation.py",
      "find": "gaps.append(\"authority-envelope-mismatch\")",
      "replace": "pass",
      "why": "tests/factory/test_claim_explanation.py coherently rehashes a forged authority and still requires a gap."
    },
    {
      "id": "claim-explanation-self-certification-hidden",
      "file": "factory_kernel/claim_explanation.py",
      "find": "gaps.append(\"independent-origin-unestablished\")",
      "replace": "pass",
      "why": "tests/factory/test_claim_explanation.py detects a builder judgement rewrapped as independent certification."
    },
    {
      "id": "claim-explanation-missing-evidence-hidden",
      "file": "factory_kernel/claim_explanation.py",
      "find": "gaps.append(\"artifact-\" + status)",
      "replace": "pass",
      "why": "tests/factory/test_claim_explanation.py requires absent and tampered bytes to remain explicit."
    },
    {
      "id": "claim-explanation-unknown-current-identity-hidden",
      "file": "factory_kernel/claim_explanation.py",
      "find": "gaps.append(\"current-claim-identity-unavailable\")",
      "replace": "pass",
      "why": "tests/factory/test_claim_explanation.py distinguishes omitted current identity from observed equality."
    },
    {
      "id": "decision-history-owner-visibility-disabled",
      "file": "factory_kernel/decision_history.py",
      "find": "if principal.role != \"owner\":",
      "replace": "if False:",
      "why": "tests/factory/test_decision_history.py must prevent proposal principals from reading private history."
    },
    {
      "id": "decision-history-replayed-event-accepted",
      "file": "factory_kernel/decision_history.py",
      "find": "if key in seen or command.get(\"expected_project_version\") != event[\"project_version\"] - 1:",
      "replace": "if False:",
      "why": "tests/factory/test_decision_history.py rejects duplicate events even after the chain is rehashed."
    },
    {
      "id": "provenance-exact-head-binding-disabled",
      "file": "factory_kernel/provenance.py",
      "find": "if expected_head_sha is not None and head != expected_head_sha:",
      "replace": "if False and expected_head_sha is not None and head != expected_head_sha:",
      "why": "A stale builder provenance note from a different PR head could be accepted by a fresh validator."
    },
    {
      "id": "provenance-artifact-rehash-disabled",
      "file": "factory_kernel/provenance.py",
      "find": "if expected != sha256_value(content):",
      "replace": "if False and expected != sha256_value(content):",
      "why": "Tampered embedded builder artifacts could survive the fresh-validator handoff."
    },
    {
      "id": "provenance-builder-publication-removed",
      "file": "factory_kernel/runtime.py",
      "find": "\"python\", \"scripts/factory_provenance.py\", \"publish\",",
      "replace": "\"python\", \"scripts/factory_provenance.py\", \"disabled-publish\",",
      "why": "The builder could label a PR ready for fresh validation without first publishing durable exact-head provenance."
    },
    {
      "id": "evidence-spine-production-route-falls-back",
      "file": "factory_kernel/worker_runtime.py",
      "find": "routed[1] = \"scripts/factory_evidence_spine.py\"",
      "replace": "routed[1] = \"scripts/factory_evidence.py\"",
      "why": "Production validation could silently bypass 21-claim spine closure and emit only the legacy Evidence Bundle."
    },
    {
      "id": "merge-spine-global-completion-disabled",
      "file": "harness/merge_verify.py",
      "find": "if spine.get(\"completion_level\") != 100:",
      "replace": "if False and spine.get(\"completion_level\") != 100:",
      "why": "Merge authorization could proceed even though the canonical evidence spine never reached global 100 percent completion."
    },
    {
      "id": "merge-spine-policy-binding-disabled",
      "file": "harness/merge_verify.py",
      "find": "if spine.get(\"policy_sha256\") != policy.sha256():",
      "replace": "if False and spine.get(\"policy_sha256\") != policy.sha256():",
      "why": "A bundle compiled against a weaker or stale evidence policy could authorize merge."
    },
    {
      "id": "exploration-owner",
      "file": "factory_kernel/exploration_records.py",
      "find": "if not isinstance(principal, Principal) or principal.role != \"owner\":",
      "replace": "if False:",
      "why": "A proposal principal cannot write exploration events. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-current-intent",
      "file": "factory_kernel/exploration_records.py",
      "find": "if approval[\"source_intent_version\"] != latest[\"version\"]:",
      "replace": "if False:",
      "why": "New intent cannot inherit an old approval. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-causal-frontier",
      "file": "factory_kernel/exploration_records.py",
      "find": "affected = set(changed)",
      "replace": "affected = set()",
      "why": "Invalidation must walk recorded causal dependencies. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-probe-falsifier",
      "file": "factory_kernel/exploration_records.py",
      "find": "if observation[\"outcome\"] == \"contradicted\":",
      "replace": "if False:",
      "why": "A preregistered failed experiment invalidates its exploratory assumption. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-repository-currency",
      "file": "factory_kernel/exploration.py",
      "find": "if not allow_stale and session[\"context\"] != self._context():",
      "replace": "if False:",
      "why": "Changed repository context prevents a stale handoff. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-shared-budget",
      "file": "factory_kernel/exploration.py",
      "find": "if previous and previous[\"limits\"] != request[\"policy\"][\"budget\"]:",
      "replace": "if False:",
      "why": "New questions cannot reset the approved-scope budget. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-probe-budget",
      "file": "factory_kernel/exploration.py",
      "find": "if budget[\"probe_units\"] + units > budget[\"limits\"][\"probe_units\"]:",
      "replace": "if False:",
      "why": "Reserve experiment work before executing it. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-measurement-unit",
      "file": "factory_kernel/exploration.py",
      "find": "or criterion[\"unit\"] != target[\"metric\"] or identity in seen",
      "replace": "or identity in seen",
      "why": "A result cannot be relabelled into a different measurement unit. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-current-recommendation",
      "file": "factory_kernel/exploration.py",
      "find": "if session[\"status\"] != \"recommended\":",
      "replace": "if False:",
      "why": "Invalidated recommendations cannot produce a programme handoff. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-pending-work",
      "file": "factory_kernel/exploration.py",
      "find": "if any(row[\"status\"] == \"pending\" for row in session[\"reservations\"].values()):\n                raise IntentRefused(\"uncertain reserved work cannot be hidden by a recommendation\")",
      "replace": "if False:\n                raise IntentRefused(\"uncertain reserved work cannot be hidden by a recommendation\")",
      "why": "Unknown work cannot be hidden by a recommendation. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-reasoning-budget",
      "file": "factory_kernel/exploration_reasoner.py",
      "find": "if budget[\"calls\"] + 1 > budget[\"limits\"][\"calls\"] or budget[\"usd\"] + cap > budget[\"limits\"][\"usd\"]:",
      "replace": "if False:",
      "why": "Different sessions share the same cumulative model-call and dollar budget. Detector: tests/factory/test_exploration_reasoner.py"
    },
    {
      "id": "exploration-unknown-spend",
      "file": "factory_kernel/exploration_reasoner.py",
      "find": "if budget[\"uncertain\"]:",
      "replace": "if False:",
      "why": "Unknown or excess reported spend blocks further calls. Detector: tests/factory/test_exploration_reasoner.py"
    },
    {
      "id": "exploration-reasoning-replay",
      "file": "factory_kernel/exploration_reasoner.py",
      "find": "if not created:",
      "replace": "if False:",
      "why": "Replayed reasoning must not invoke a provider again. Detector: tests/factory/test_exploration_reasoner.py"
    },
    {
      "id": "exploration-measurements-win",
      "file": "factory_kernel/exploration_policy.py",
      "find": "for measurement in observation.get(\"measurements\", []):",
      "replace": "for measurement in []:",
      "why": "Later model opinions cannot erase measured experimental results. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-handoff-export",
      "file": "factory_kernel/exploration.py",
      "find": "if session[\"status\"] != \"recommended\" or not session[\"handoffs\"]:",
      "replace": "if False:",
      "why": "A changed context or invalidated recommendation must not be exported as current. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-replay-currency",
      "file": "factory_kernel/exploration_records.py",
      "find": "replay_guard(projection(events), approved_scope(self.store, events), old[\"payload\"])",
      "replace": "pass  # stale replay guard removed",
      "why": "A changed context or invalidated recommendation must not be exported as current. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "exploration-stale-prediction",
      "file": "factory_kernel/exploration_policy.py",
      "find": "if stale_prediction:",
      "replace": "if False:",
      "why": "A changed context or invalidated recommendation must not be exported as current. Detector: tests/factory/test_exploration.py"
    },
    {
      "id": "programme-strategy-identity",
      "file": "factory_kernel/programme.py",
      "find": "identity[\"strategy\"] = strategy",
      "replace": "pass  # advice omitted from identity",
      "why": "Changing strategy must invalidate programme identity and issue membership. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-proof-status",
      "file": "factory_kernel/programme_strategy.py",
      "find": "if value[\"qualification_status\"] != \"UNPROVEN\" or value[\"proof_reuse_allowed\"] is not False:",
      "replace": "if False:",
      "why": "Planning advice cannot self-certify or authorise proof reuse. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-scope",
      "file": "factory_kernel/programme_strategy.py",
      "find": "if value[\"spec_sha256\"] != sha256_value(spec):",
      "replace": "if False:",
      "why": "Strategy must name the same approved scope. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-causal-closure",
      "file": "factory_kernel/programme_strategy.py",
      "find": "if closure(claims, roots) != claims.keys():",
      "replace": "if False:",
      "why": "Unrelated project claims do not belong in planning advice. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-claim-currency",
      "file": "factory_kernel/programme_strategy.py",
      "find": "or claim[\"status\"] != \"active\"",
      "replace": "",
      "why": "Challenged or invalidated assumptions cannot be exported as current advice. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-delivery",
      "file": "factory_kernel/runtime.py",
      "find": "context=issue_context + planning_advice(admission), env=env)",
      "replace": "context=issue_context, env=env)",
      "why": "The selected strategy must actually reach the initial planner. Detector: tests/factory/test_programme_strategy.py"
    },
    {
      "id": "programme-strategy-isolation",
      "file": "factory_kernel/runtime.py",
      "find": "contract_context = issue_context\n",
      "replace": "contract_context = issue_context + planning_advice(admission)\n",
      "why": "Advice must not be added to the contract source of truth. Detector: tests/factory/test_programme_strategy.py"
    }
  ]
}
