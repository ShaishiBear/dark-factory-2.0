{
  "_what_this_is": [
    "The stack-specific half of DynaChat's harness. `ci.py` owns the ladder; this file",
    "owns the repo-specific commands and the locked browser regression fixture.",
    "",
    "The numerical floors are observed values, not aspirations. Adding checks does not",
    "raise a floor until the validation host has actually measured the new total."
  ],

  "static": "python harness/static.py",
  "unit": "python harness/unit.py",
  "_unit_count_pattern": [
    "unit.py requires backend, frontend, and factory trust-boundary suites to run.",
    "Floor is 1033, observed by quick-authority on ubuntu for the tree on main (2026-09-04)."
  ],
  "unit_count_pattern": "UNIT_PASSED tests=(\\d+)",

  "driver": "http",
  "_where_the_e2e_watchdog_lives_now": [
    "`e2e_timeout_s` was here, and it was the only rung deadline this file owned while the",
    "other four lived as literals in the code. Every duration in the ladder now comes from one",
    "place, harness/budgets.json, where the scope that bounds a rung is checked against the",
    "scopes that bound what runs inside it: the journey's watchdog is the `e2e-rung` scope",
    "(360 s, the same number, composed from the two streams bounded by response_timeout_s",
    "below, plus login probes and browser startup, D-051). D-075."
  ],
  "http": {
    "start": "python harness/serve.py --port {port} --with-frontend",
    "health_path": "/api/health",
    "health_contains": "ok",
    "boot_timeout_s": 90
  },

  "browser": {
    "install_check": "agent-browser --help",
    "install_hint": "npm i -g agent-browser && agent-browser install",
    "fixture_video_id": "pjF-0dliYhg",
    "_question_template": [
      "Rendered by harness/e2e.py with the fixture's `{title}` (and `{description}`) exactly",
      "as GET /api/videos lists them, after the catalog probe and before the stream probe",
      "and the browser; the rendered question is printed as E2E_QUESTION. The earlier",
      "literal question (`Based on the video I just added, ...`) invited the model to answer",
      "that it cannot see uploads and never search, so whether a citation appeared was luck",
      "(run 34020965800 made no tool call, D-062). A `question` key without this one is",
      "still honoured as a literal, for a configuration from before D-062."
    ],
    "question_template": "Summarize the main topic of the video titled \"{title}\" and cite the source.",
    "response_timeout_s": 90,
    "_credentials": [
      "DARK_FACTORY_E2E_EMAIL and DARK_FACTORY_E2E_PASSWORD live only in the external",
      "validation environment. Signup is rate-limited, so the gate uses a dedicated",
      "seeded validation account instead of manufacturing a new user on every run."
    ]
  }
}
