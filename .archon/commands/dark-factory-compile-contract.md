---
description: Compile one accepted issue into an ambiguity-free machine Task Contract.
---
Use the preloaded `to-spec`, `domain-modeling`, and `codebase-design` skills. Read the fetched issue, research/investigation or plan, `CONTEXT.md`, relevant ADRs, `MISSION.md`, `CLAUDE.md`, and `FACTORY_RULES.md`.

Produce only the structured object requested by the workflow. Every acceptance criterion must be observable. Select exactly one public test seam for this tracer-bullet issue; if the work genuinely requires multiple independent seams, the issue is not factory-sized and must be decomposed. `ambiguities` must be empty: resolve from repository evidence or stop rather than invent a product decision.

`likely_paths` and `symbols` are hypotheses for deterministic context assembly, not permission to modify those paths. Record hard invariants and explicit out-of-scope boundaries.
