{
  "version": "1.0",
  "_what_this_is": [
    "Protected memory for defect classes the factory has already learned. Active entries are merge preconditions, not documentation.",
    "The factory may propose a new lesson, but changing this registry changes the judge and therefore requires human-reviewed trust-root work.",
    "Each assertion is evaluated against the exact current repository by harness/immunity.py. Equivalent refactors that remove an assertion require updating this protected registry rather than silently forgetting the lesson."
  ],
  "entries": [
    {
      "id": "IMM-001",
      "status": "active",
      "source": {
        "kind": "decision",
        "ref": "D-002"
      },
      "failure_class": "canonical end-to-end behavior split across competing definitions",
      "lesson": "The canonical E2E journey must remain in harness/ci.py and emit positive counted evidence.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "harness/ci.py",
          "value": "from e2e import run_e2e"
        },
        {
          "kind": "text_contains",
          "path": "harness/ci.py",
          "value": "E2E_PASSED steps={steps}"
        }
      ]
    },
    {
      "id": "IMM-002",
      "status": "active",
      "source": {
        "kind": "decision",
        "ref": "D-003"
      },
      "failure_class": "adversarial defects caught only inside the builder-visible optimization loop",
      "lesson": "Independent and deterministic-security mutation coverage must retain a structural floor and explicit security-channel probes.",
      "assertions": [
        {
          "kind": "json_number_min",
          "path": ".factory/locks/floor.json",
          "pointer": "/mutations_independent_caught",
          "minimum": 3
        },
        {
          "kind": "json_number_min",
          "path": ".factory/locks/floor.json",
          "pointer": "/mutations_security_caught",
          "minimum": 3
        },
        {
          "kind": "json_array_match_min",
          "path": "harness/mutations/defects.json",
          "pointer": "/defects",
          "field": "must_catch",
          "contains": "security",
          "minimum": 3
        }
      ]
    },
    {
      "id": "IMM-003",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-08-24-stop-repo-default"
      },
      "failure_class": "remote emergency-stop check defaulted to a different repository",
      "lesson": "The remote stop half must target this repository by default and remain fail-closed when GitHub stop state is unreadable.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "scripts/factory-stop.sh",
          "value": "REPO=\"${FACTORY_REPO:-ShaishiBear/dark-factory-2.0}\""
        },
        {
          "kind": "text_contains",
          "path": "scripts/factory-stop.sh",
          "value": "if ! HITS=$(gh issue list -R \"$REPO\" --label \"$STOP_LABEL\" --state open"
        }
      ]
    },
    {
      "id": "IMM-004",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-08-30-circular-design-governor-certification"
      },
      "failure_class": "builder-produced evidence reused to fill an independent certification slot",
      "lesson": "Independence is structural, not a label. Evidence closure once filled the independent slots of the pre-code design and architecture-governor claims with the builder's own post-code architecture-conformance artifact. Every independence-required claim must name a registered authority, and any certificate whose judgement is an artifact from the builder provenance path must be refused before verdict or bindings are considered.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/independence.py",
          "value": "if judgement in builder_origin:"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/independence.py",
          "value": "a builder claim cannot certify itself"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/evidence_closure.py",
          "value": "authority_for(claim_id)"
        },
        {
          "kind": "text_contains",
          "path": "harness/merge_verify.py",
          "value": "has no independent certification hash"
        }
      ]
    },
    {
      "id": "IMM-005",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-08-30-contract-certified-by-code-holdout"
      },
      "failure_class": "an independent authority answering a different question than the claim it certifies",
      "lesson": "Validator-side is not sufficient for independence; the authority must answer the claim's own question. The contract claim's independent slot was filled by the blinded code holdout, which judges the diff against the contract while presupposing the contract is right - anti-correlated with contract faithfulness, since a contract that drops requirements makes that holdout more likely to pass. The contract claim keeps a dedicated certifier that sees the issue and no diff, and every externally supplied certificate must declare its own subject from inside the hashed judgement.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/independence.py",
          "value": "authority_id=\"blinded-contract-certifier\""
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/independence.py",
          "value": "if spec.externally_supplied and certificate.get(\"judgement_certifies\") != claim_id:"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "did not declare its own subject"
        }
      ]
    },
    {
      "id": "IMM-006",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-08-30-candidate-authored-genesis-standard"
      },
      "failure_class": "the subject of a certification authoring the standard it is judged by",
      "lesson": "The pull request that rewrites the trust root cannot be certified by that trust root, and it must not author the bar either: a candidate can satisfy an artificially weak standard honestly. Minimums, required markers, mutation families, trust-root scope and pinned-policy scope come from an external genesis policy supplied at ceremony time and pinned by hash; the candidate manifest may only describe the tree. Observations are read out of a real run's log rather than believed from a JSON document, and the human creates the authorization rather than confirming a string the candidate wrote.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "harness/bootstrap_verify.py",
          "value": "must not define the standard"
        },
        {
          "kind": "text_contains",
          "path": "harness/bootstrap_verify.py",
          "value": "outside the repository under test"
        },
        {
          "kind": "text_contains",
          "path": "harness/bootstrap_verify.py",
          "value": "no fixed point exists"
        },
        {
          "kind": "text_contains",
          "path": "harness/bootstrap_verify.py",
          "value": "narrows the trust-root scope"
        },
        {
          "kind": "text_contains",
          "path": "harness/bootstrap_verify.py",
          "value": "does not match the digest the evidence commits to"
        }
      ]
    },
    {
      "id": "IMM-007",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-03-pr-judged-by-its-own-guard"
      },
      "failure_class": "a change judged by the copy of the judge it carries",
      "lesson": "The required PR check ran scripts/factory_security.py from the PR head, so a PR that rewrote the guard, or the workflow that invokes it, would have been judged by its own copy. The kernel's commit envelope was the only thing keeping the factory out, and a head-run guard was defence in depth pretending to be an authority. The authority that grants trust-root permission now executes from a commit already on the protected branch: the trust-root workflow is a pull_request_target workflow that checks out github.sha and never the head, the guard refuses to run from the PR head and binds to the expected base and head, and the kernel validator runs the same trusted-base mode from its main checkout. Unattended merge is armed only from that trusted decision, only for the maintainer lane, and only against the exact judged head.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-trust-root.yml",
          "value": "pull_request_target:"
        },
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-trust-root.yml",
          "value": "ref: ${{ github.sha }}"
        },
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-trust-root.yml",
          "value": "expectedHeadOid: $head"
        },
        {
          "kind": "text_contains",
          "path": "scripts/factory_security.py",
          "value": "refuses to run from the PR head"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "\"--trusted-base\", \"--expect-head\", head,"
        }
      ]
    },
    {
      "id": "IMM-008",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-holdout-readable-by-the-builder"
      },
      "failure_class": "a holdout the subject can read",
      "lesson": ".factory/holdout/run.py said the builder was blocked from reading it, but protection from modification is not protection from reading: build-side workers hold Read/Glob/Grep over the checkout, and the scenarios sat in it. An assertion the subject can read is inside its optimisation loop and is a regression suite, not a holdout. The kernel now creates every build worktree as a sparse checkout with the holdout programs absent from disk (BUILDER_BLIND_PATHS), verifies the blind took, and never blinds the validator worktree where the full harness runs them.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/worker_policy.py",
          "value": "BUILDER_BLIND_PATHS: tuple[str, ...] = (\".factory/holdout/**/*.py\",)"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/worktree.py",
          "value": "blinded paths are still on disk"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "blind=BUILDER_BLIND_PATHS,"
        }
      ]
    },
    {
      "id": "IMM-009",
      "status": "active",
      "source": {
        "kind": "decision",
        "ref": "D-006"
      },
      "failure_class": "a second representation of the lifecycle that nothing executes, presented as authority",
      "lesson": "factory_kernel/state.py was listed as the kernel's state machine while the runtime never consulted it; the evidence spine was the lifecycle the runtime actually executed and the merge verifier actually enforced. A representation nobody executes is a claim, not an authority, and it drifts. There is one lifecycle definition, the ordered required_claims of the evidence spine, and the documentation names it.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "FACTORY.md",
          "value": "the only executable lifecycle definition"
        },
        {
          "kind": "text_contains",
          "path": "harness/merge_verify.py",
          "value": "exact required claim sequence"
        },
        {
          "kind": "json_array_match_min",
          "path": ".factory/evidence-spine.json",
          "pointer": "/required_claims",
          "field": "id",
          "contains": "green-proof",
          "minimum": 1
        }
      ]
    },
    {
      "id": "IMM-010",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-preflight-proved-a-different-request"
      },
      "failure_class": "a preflight that proves a different request than the one the worker makes",
      "lesson": "The worker preflight curled the literal OpenRouter path and passed for weeks while the worker itself, through the Anthropic SDK that appends /v1/messages to ANTHROPIC_BASE_URL, requested /api/v1/v1/messages and got an HTML 404 for every model. The first canary dispatch was the first time the real request was ever made. A preflight must exercise the same binary, flags and URL construction as the thing it clears: the base URL is defined once at job level, the raw probe builds its URL the way the SDK does, and the pinned CLI is run as the kernel runs it against every configured model before dispatch.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-worker.yml",
          "value": "ANTHROPIC_BASE_URL: https://openrouter.ai/api\n"
        },
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-worker.yml",
          "value": "-X POST \"${ANTHROPIC_BASE_URL}/v1/messages\""
        },
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-worker.yml",
          "value": "FACTORY_PREFLIGHT_REFUSED worker CLI cannot reach model $model"
        },
        {
          "kind": "text_contains",
          "path": ".github/workflows/dark-factory-worker.yml",
          "value": "FACTORY_PREFLIGHT_MODEL_ROUTE_OK model=$model"
        }
      ]
    },
    {
      "id": "IMM-011",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-script-owned-heartbeat-needed-a-token-it-must-not-have"
      },
      "failure_class": "a subprocess that runs model-authored commands and also needs a repository credential",
      "lesson": "factory_protocol.py and factory_proof.py started and touched the issue lease themselves, which needs gh and therefore GH_TOKEN, while the kernel launched them with no credentials because factory_proof.py executes checkpoint commands the model wrote. The third canary dispatch died at LEASE_ERROR. The two responsibilities cannot share a process: the kernel alone heartbeats the lease with GitHub scope, contract/context/RED/GREEN run with none, and the checkpoint runner scrubs both token names from its child even if a caller forgets the scope.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "scripts/factory_proof.py",
          "value": "env=checkpoint_env()"
        },
        {
          "kind": "text_contains",
          "path": "scripts/factory_proof.py",
          "value": "CHECKPOINT_SCRUBBED_ENV=('GH_TOKEN','GITHUB_TOKEN')"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "def _lease_heartbeat("
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "transcript=paths.transcripts / f\"lease-{stage}.log\""
        }
      ]
    },
    {
      "id": "IMM-012",
      "status": "active",
      "source": {
        "kind": "decision",
        "ref": "D-013"
      },
      "failure_class": "a capability the documentation claims the worker has but the launcher disables",
      "lesson": "docs/agents/matt-skills.md told every context worker that the builder used the real mattpocock-skills plugin and that a preflight failed closed without it. Workers launch with --bare and load no plugins; no such preflight existed. The isolation is right and stays. The disciplines now arrive as pinned text from .factory/methods/, validated fail-closed and injected by the kernel per role, and the documentation says exactly that.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/worker_runtime.py",
          "value": "methods=method_block(cwd, role),"
        },
        {
          "kind": "text_contains",
          "path": "docs/agents/matt-skills.md",
          "value": "do **not** load the `mattpocock-skills` plugin"
        },
        {
          "kind": "text_contains",
          "path": "scripts/factory_security.py",
          "value": "or path.startswith(\".factory/methods/\")"
        }
      ]
    },
    {
      "id": "IMM-013",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-allowlisted-program-name-treated-as-a-sandbox"
      },
      "failure_class": "an allowlisted program name treated as a sandbox for model-authored commands",
      "lesson": "The repro loop accepted any argv whose program was one of python/uv/bun/npx/pytest, built the child environment by removing two GitHub tokens from the parent's, and ran inside the builder worktree with no check that the tree was unchanged afterwards. python -c is arbitrary code; a denylist misses every secret added later; and a repro that edits a file then prints the symptom rewrites what the next stage reads. A model-authored command is bounded by command shapes (test runners only, no eval flags, no metacharacters, no absolute paths), an environment built from an allowlist, and a byte-identical worktree before and after. None of that makes the command trusted; it bounds the damage to reading and printing.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/repro.py",
          "value": "ALLOWED_SHAPES: tuple[tuple[str, ...], ...] = ("
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/repro.py",
          "value": "child = {key: base[key] for key in REPRO_ENV_KEYS if key in base and base[key]}"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "raise NeedsHuman(\"bug repro refused: repro modified the worktree\")"
        }
      ]
    },
    {
      "id": "IMM-014",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-ratchet-monotonicity-documented-not-enforced"
      },
      "failure_class": "a ratchet whose monotonicity was documented but never enforced",
      "lesson": "The floor file said a second check asserted floor(head) >= floor(base). No such program existed; PR #44 corrected the note and FACTORY_RULES section 13 recorded that a maintainer PR lowering a floor would pass every check. A ratchet that can be lowered by anyone with the maintenance lane is a dial, not a ratchet. The base-anchored trust-root guard now compares every numeric floor at base and head whenever the file is in the diff and refuses any lowered or removed key on both lanes; the check is protected and mutation-tested.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "scripts/factory_security.py",
          "value": "def ratchet_regressions("
        },
        {
          "kind": "text_contains",
          "path": "scripts/factory_security.py",
          "value": "findings.extend(ratchet_regressions(base_floor, head_floor))"
        },
        {
          "kind": "json_array_match_min",
          "path": "harness/factory_mutations/defects.json",
          "pointer": "/defects",
          "field": "id",
          "contains": "security-ratchet-regression-unchecked",
          "minimum": 1
        }
      ]
    },
    {
      "id": "IMM-015",
      "status": "active",
      "source": {
        "kind": "audit",
        "ref": "2026-09-04-repro-gate-refused-behavioural-bugs"
      },
      "failure_class": "a red-loop gate shaped only for bugs that already crash",
      "lesson": "The kernel-executed repro (D-015) required an existing test-runner command to fail on the unchanged tree. Canary attempt 4 (run 33908589032) was a wrong return value that no test covered; the investigate worker correctly refused to invent a passing command and the build escalated at its first stage, though the factory proves exactly that red loop two stages later when the independent acceptance tests go RED. A gate that only crashes can pass rejects the commonest bug. The red loop is still mandatory: an executed repro when one exists, otherwise a deferred record whose promised symptom must appear in a RED checkpoint's output or the build stops.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/repro.py",
          "value": "def verify_deferred_in_red"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "self._close_deferred_repro(paths.artifacts)"
        },
        {
          "kind": "text_contains",
          "path": ".factory/prompts/investigate.md",
          "value": "repro-deferred.json"
        }
      ]
    },
    {
      "id": "IMM-016",
      "status": "active",
      "source": {
        "kind": "decision",
        "ref": "D-023"
      },
      "failure_class": "a refusal whose cause was never recorded",
      "lesson": "Every validation refusal reached the failure handler as a bare exception, the PR comment recorded only the exception's class name, and the two logs that carried the reason stayed on an ephemeral runner. A security-guard veto and a base that moved under the PR looked identical, so the factory rebuilt certified work from scratch for a problem no model needed to touch, and nobody could size a repair loop because there was no failure data. A refusal now carries a stable reason code and its authority, a scrubbed record travels in the uploaded artifacts, a moved base does not charge the issue's rebuild budget, and the only consumer of a reason code is a model-free re-head that reuses nothing validator-side. A model repair loop stays unbuilt until the recorded codes say which class deserves one.",
      "assertions": [
        {
          "kind": "text_contains",
          "path": "factory_kernel/refusal.py",
          "value": "def classify(stage: str, exc: BaseException) -> str:"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/refusal.py",
          "value": "STALE_BASE_PATTERNS"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "validation-refusal.json"
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "reason_code != \"stale_base\""
        },
        {
          "kind": "text_contains",
          "path": "factory_kernel/runtime.py",
          "value": "push_branch(branch, force_with_lease=head)"
        }
      ]
    }
  ]
}
